# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
out/

# Environment files
.env
.env.local
.env.*.local
apps/web/.env.test

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Playwright
test-results/
apps/web/tests/results/

# Prisma
packages/db/prisma/migrations/*.sql.bak

# Vercel
.vercel

# Railway
.railway
.lighthouseci/

# Operational runbooks — local-only artifacts (DB snapshots, dump files,
# rollback references). Frequently contain bcrypt hashes, OAuth IDs, and
# other sensitive data that must never enter git history.
docs/runbooks/

# OQ-GD-13 archivist-relic design prototypes — regeneratable via
# `npx tsx scripts/relic-*.ts`. HTML outputs are large (dither SVGs) and
# the iterations/ folder is a working snapshot history, not source.
relic-prototype.html
relic-halftone.html
relic-composition.html
relic-vocabulary.html
relic-bottom-variants.html
iterations/
