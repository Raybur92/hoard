# Hoard — Claude Code Working Guide

Hoard is a personal game tracking PWA. It syncs libraries from Steam, PSN, Xbox, and GOG into one unified interface with a terminal/collector aesthetic. Personal tool, not SaaS.

Before starting any task, check `docs/PLAN.md` for the current phase and whether anything is blocked.

---

## Commands

```bash
# Development
npm run dev          # all apps in parallel (web port 5173, api port 3001)
npm run dev:web      # frontend only
npm run dev:api      # backend only

# Quality
npm run typecheck                          # TypeScript check across all packages
npm run lint                               # ESLint + Prettier across all packages
npm run test                               # all tests (Vitest + Jest, both workspaces)
npm run test --workspace=apps/api          # Jest + Supertest only (backend integration)
npm run test --workspace=apps/web          # Vitest only (frontend unit + component)
npm run test:e2e                           # Playwright E2E

# Database
npx prisma migrate dev        # create + apply migration (packages/db)
npx prisma migrate deploy     # apply migrations in production
npx prisma studio             # GUI browser for DB
npx prisma db seed            # seed with mock data
```

---

## Key File Locations

| What | Where |
|---|---|
| Execution plan + phase status | `docs/PLAN.md` |
| **Pages functional analysis (v2 spec — 2026-05-29..30)** | **`docs/PAGES_PLAN.md`** — forward-looking functional spec across all 8 pages. 5 benchmark sources × 22 capability IDs. 4 identity rejections, 1 reversal (affiliate routing for cost-recovery). All OQ-GD/LIB/REL/EV/DASH/DEALS open questions tracked. Dashboard bento-box layout locked. GameDetail v2 four-state architecture locked. Events + Deals as new top-level surfaces. **Source of truth for the v2 ambition; convert into workstreams as they're picked up.** |
| Settings audit + sync log (complete 2026-05-08) | `docs/SETTINGS_AUDIT_PLAN.md` (PR A + PR B shipped same day; decisions S1–S4 + L1–L4 locked) |
| Trophies & achievements (complete 2026-05-08) | `docs/TROPHIES_PLAN.md` (T1–T6 all shipped same day; decisions T-D1…T-D10 locked in §1) |
| Releases page rework (complete 2026-05-07) | `docs/RELEASES_PLAN.md` + handoff `docs/Hoard_releases_handoff.md` + mocks `project/Hoard_releases_mocks.html` |
| Interaction debt + audits + PR plan (complete 2026-05-06) | `docs/INTERACTION_DEBT_PLAN.md` |
| Performance & UX workstream (complete 2026-05-04) | `docs/PERFORMANCE_PLAN.md` |
| Environment variables reference | `docs/ENV.md` |
| Design source of truth | `project/` — HTML/CSS/JS prototypes |
| Design system CSS | `project/styles.css` → port to `apps/web/src/styles/` |
| Shared primitives (design) | `project/primitives.jsx` |
| Screen components (design, phases 1–4) | `project/Hoard.html` — full hi-fi mockup (dashboard, library, upcoming, game detail, settings, platform connect, PSN guided flow, delete account) |
| Earlier screen sketches | `project/screens-dashboard.jsx`, `screens-library.jsx`, `screens-upcoming.jsx` |
| CSS tokens | `apps/web/src/styles/tokens.css` |
| Global CSS + utilities | `apps/web/src/styles/global.css` |
| Shared TypeScript types | `packages/types/` |
| Prisma schema | `packages/db/schema.prisma` |
| API routes | `apps/api/src/routes/` |
| Platform integrations | `apps/api/src/services/platforms/` |
| IGDB + HLTB services | `apps/api/src/services/` |

---

## Hard Rules

These are non-negotiable. Do not deviate without explicit instruction.

1. **Match the designs exactly.** Read `project/` source before touching any UI. Do not improvise layout, colors, spacing, or typography. The design files are the spec.
   
   **Corollary — for new top-level surfaces without a `project/` mockup** (Events, Deals, Admin sub-pages, future workstreams): before writing a single line of JSX, read **at least two existing top-level screens of the same class** (list page, detail page, settings sub-page) and replicate their shell convention verbatim. The shell convention is: `<TopBar crumbs={[...]}>` → optional `<BackBar>` → sub-header strip (if any) → `<div className="thin-scroll" style={{ flex: 1, overflow: 'auto', padding: '...' }}>`. Match padding tokens (`'24px 32px 40px'` for lists, `'40px 36px 60px'` for content-centric detail pages, `'12px 36px'` for BackBar) exactly — do not vary by a single pixel. Reuse existing primitives (`<BackBar>`, `<TopBar>`, `<Btn>`, `<Icon>`) — do not introduce parallel components that do the same job with different spacing. If a candidate pattern doesn't exist as a primitive yet and you find yourself writing it twice, extract it before shipping. **Reason:** EV-PR1 first-pass centered content with `maxWidth: 960; margin: 0 auto` instead of using the standard `<TopBar crumbs>` shell every other page uses; second-pass invented a fresh `padding: '12px 32px'` back-button row instead of reusing the GameDetail family's `12px 36px` pattern. Both errors were preventable by reading two sibling screens first. Pattern drift is the dominant failure mode when there's no mockup — treating the existing screen set as the spec is the fix.
2. **No Tailwind.** The design system is custom CSS variables + utility classes. All tokens live in `tokens.css`. Extend that file, never work around it.
3. **No `any` in TypeScript.** Strict mode is on. All props and return types must be explicit. All interfaces must be exported from `packages/types`.
4. **No localStorage for tokens.** JWT lives in HTTP-only cookies only. Never expose auth tokens to JavaScript.
5. **Login screen has no design file.** Implement `/login` using design system conventions (same tokens, fonts, and utility classes as every other screen). No external reference — match the terminal aesthetic.
6. **Manual-add is the fallback, not the rule.** All 7 platform codes (`ST`/`PS`/`XB`/`GG`/`IT`/`EP`/`NT`) have auto-sync paths as of the M-series workstream (2026-05-28..29). Manual-add via IGDB search + `platformLabel` remains the path for individual games the sync can't resolve (jam entries, regional releases, unreleased titles) — the form is the floor, not the canonical path. Don't build scrapers; new sync integrations follow the M-series pattern (hand-rolled HTTP client per M-D2 + token-paste guided flow + IGDB external_games URL-pattern resolution). Originally "Nintendo and Epic are manual-only"; rewritten 2026-05-29 (M-D14, see `docs/SYNC_EXPANSION_PLAN.md`).
7. **Never test against the production database.** Backend integration tests mock Prisma at the module level (each test file declares its own mock with `jest.mock('@hoard/db', ...)`). No live DB connection is established during `npm test`. A separate `hoard-test` Supabase project was originally planned but proven unnecessary — the value at this layer is in route logic, not SQL behavior.
8. **HLTB failures must be silent.** If `howlongtobeat` throws or returns nothing, store `null` and show "—" in the UI. Never surface an error to the user for HLTB.
9. **Keep docs current after every phase.** When a phase completes (or any meaningful chunk of work lands), update `docs/PLAN.md` before closing the session:
   - Check off all completed deliverables with `[x]`
   - Update the Phase Status table
   - Record any design decisions made during the phase — what was decided, why, and any trade-offs — directly in the relevant phase section under a `**Decisions:**` block
   - If a decision affects the overall architecture or is permanent for v1, mirror it in `AGENT.md` under Key Decisions
   - Update the `## Current Phase` section in this file (`CLAUDE.md`) to name the next active phase explicitly — never leave it as a bare pointer to PLAN.md
10. **Stop-and-confirm cadence on multi-phase workstreams.** When a workstream is structured as a sequence of phases (R1–R6 on the Releases rework, PRs A–D on Interaction Debt, F1–F14 on Performance, the original Phases 1–8, etc.), do **not** chain into the next phase automatically after finishing one. Instead, after every completed phase:
    1. Land all changes (commit + push if applicable).
    2. Update every relevant doc (`docs/RELEASES_PLAN.md` / `docs/INTERACTION_DEBT_PLAN.md` / `docs/PERFORMANCE_PLAN.md` / etc. status table; `docs/PLAN.md` Phase Status row; `CLAUDE.md` Current Phase + Recent Fixes; `AGENT.md` if any architectural decision landed; any other file the phase touched).
    3. Confirm completion to the user in **one** brief message — what shipped, what's the next phase, and a one-line note on doc currency.
    4. Stop and wait for the user's explicit go-ahead before starting the next phase.
    
    The user runs the workstream's pacing; the agent runs the phase's execution. This protects context, keeps the user in the loop on architectural surface, and prevents the agent from steamrolling into the next phase before findings or scope adjustments can land.

---

## Design System Quick Reference

**Fonts** (loaded via Google Fonts):
- `var(--mono)` — JetBrains Mono — primary font for all UI text
- `var(--sans)` — IBM Plex Sans — prose, descriptions
- `var(--display)` — Major Mono Display — hero numbers, logo

**Core palette:**
- `--void` `#07090a` — page background
- `--ink` `#0d1012` — card/panel background
- `--ink-2` `#14181b` — raised panel, hover state
- `--paper` `#ece8de` — primary text
- `--paper-dim` `#a9a89e` — secondary text
- `--paper-faint` `#6b6f72` — labels, captions
- `--rule` `#23292d` — hairlines, borders
- `--amber` `#d4a017` — wishlist, highlights, CTA
- `--green` `#5fc26a` — playing, active, completed, ok
- `--red` `#e2553a` — dropped, errors
- `--blue` `#69a1d4` — on hold, info

**Typography scale (tokens.css, added in Phase 8 PR 1):**
`--text-3xs: 10px` · `--text-2xs: 11px` · `--text-xs: 12px` · `--text-sm: 13px` · `--text-base: 14px` · `--text-md: 17px` · `--text-lg: 22px` · `--text-xl: 28px` · `--text-2xl: 44px` · `--text-display-sm: 56px` · `--text-display: 96px`

**Floor rules** (enforced by audit, not by lint):
- Interactive text ≥ `--text-xs` (12 px)
- Body content ≥ `--text-sm` (13 px)
- `--paper-faint` is banned as a *text* color anywhere font-size < `--text-md` (17 px) — the `.t-faint` utility class is mapped to `--paper-dim` (~9.4:1 contrast). `--paper-faint` is still legitimate for decorative borders / dividers.
- Receipt internals (`.receipt`, `.barcode`, `.bignum`) and platform glyphs (`.plat`) are documented exceptions.

**Line-height tokens:**
`--lh-tight: 1.15` · `--lh-snug: 1.3` · `--lh-normal: 1.5` · `--lh-relaxed: 1.7`

**Key utility classes:**
`.t-display` `.t-mono` `.t-sans` `.t-up` `.t-tnum`
`.t-dim` `.t-faint` `.t-ghost` `.t-amber` `.t-green` `.t-red`
`.hoard-noise` `.thin-scroll` `.panel` `.panel.raised` `.panel.flat`
`.chip` `.chip.on` `.chip.amber` `.chip.green`
`.btn` `.btn.primary` `.btn.amber` `.btn.sm`
`.plat` `.plat.lg` `.cover-ph` `.cover-ph.bright`
`.prog` `.prog.green` `.gauge` `.heat-cell`
`.receipt` `.receipt.zigzag` `.marker` `.bignum` `.shelf-label`
`.hr-dot` `.hr-dash` `.hr-solid` `.hr-double`
`.kv` `.ascii` `.spark` `.status-sigil`
`.sr-only` `.skip-link`
`.sidebar` `.topbar` `.m-tabbar`

---

## Responsive Breakpoint

- `≥ 1024px` → desktop layout (sidebar + topbar)
- `< 1024px` → mobile layout (mobile header + tab bar)
- Use the `useBreakpoint()` hook — do not use media queries directly in component logic
- Mobile shell uses `100dvh` + `viewport-fit=cover` + `env(safe-area-inset-*)` (notch / Dynamic Island / home indicator). The OS status bar is painted by `apple-mobile-web-app-status-bar-style="black-translucent"`; the app draws no chrome of its own there.

---

## Game Statuses

`Playing` (green) · `Backlog` (paper-faint) · `Completed` (paper) · `On Hold` (blue) · `Dropped` (red) · `Wishlist` (amber)

## Platform Codes

`ST` Steam · `PS` PSN · `XB` Xbox · `GG` GOG · `NINTENDO` manual · `EPIC` manual

---

## Testing Environment

| Context | Database | Seeded? |
|---|---|---|
| Local dev | Supabase production (Option A — shared) | Manual (`prisma db seed`) |
| CI / integration tests | None — Prisma is mocked per test file | N/A |
| Production | Supabase production (same as dev) | Never seeded |

Tests run fast (no network) because every `prisma.*` call is intercepted by the test's `jest.mock('@hoard/db', ...)` block.

Visual regression baselines: `apps/web/tests/snapshots/` — committed to repo. Regenerate with `npx playwright test --update-snapshots` only when intentional visual changes are made.

---

## Current Phase

**GD-PR4 (a + b + polish) complete 2026-06-01** — final piece of the GD-series. Ships the OQ-GD-13 archivist relic for Completed games as a **button-triggered read-only full-screen overlay** (architectural reversal mid-PR after Andrea's eyeball — first-attempt S4-as-default was wrong since the user fills in sub-status/rating/notes BEFORE consecration, not on the relic). **GD-PR4a (`1727b84`):** `Game.relicDitherSvg String?` schema + `relicDither.ts` server-side render service (sharp downsample → 120×68 cell grid → 7-bucket tonal shapes, per-cell radial-wave `animation-delay` baked in starting at stage-3 offset 600ms) + `relicSigils.ts` API-side classifier (GENRE/THEME/PERSPECTIVE 3-dim, 24 unique sigils, 1 sigil = 1 value globally) + GameDetail response extension (`relicDitherSvg` + `sigils`) + self-healing invalidation via `<!-- src=URL;fmt=2 -->` XML comment embedded in each SVG (validator compares to current heroImageUrl, mismatch triggers re-render — no per-writer cache-bust needed across the 6+ Game.upsert call sites) + 25 unit tests. Backfilled 87/87 Completed-game Completed games at avg ~775KB per SVG (~70MB total in DB, manageable). **GD-PR4b (`9971b05` + polish `d01c490`):** frontend bundle of 24 sigil SVG paths keyed by name; `RelicCard.tsx` with 5 layers (top band / dither / lockup / receipt / bottom cartouche + sigil row) and a `readonly` prop swap (rating → `8/10` amber text, sub-status/completions as plain text, notes as inscribed paragraph, no inline editors when readonly); `useRelicAnimation` hook gating the 5-stage consecration animation via `hoard:relic-consecrated:${userGameId}` localStorage key (first-visit only); `relic.css` 5-stage choreography (card materialise 0-300ms → label band imprints 300-600ms → dither engraves outward from centroid 600-1500ms → title clip-path writes 1500-1750ms → receipt + cartouche + sigil stamps 1900-2400ms; `prefers-reduced-motion` collapses to 200ms cross-fade); `RelicOverlay.tsx` full-screen view (`role="dialog"`, focus trap, Esc/scrim close, body-scroll lock, fetches GameDetailGameInfo via existing `useGameByIgdb` hook — cache-hit when dispatcher already loaded the page); amber `[★ see relic]` chip on the legacy GameDetailDesktop / Mobile action row when `status === 'Completed'`; auto-open the overlay once when `changeStatus` flips from any other status → Completed (consecration moment; subsequent Completed → Playing → Completed cycles don't re-trigger because the localStorage flag persists). **Mid-PR architectural reversal (Andrea 2026-06-01):** the just-shipped S4-as-default dispatcher routing was REMOVED — Completed games stay on the legacy editable GameDetail. The relic is now an artifact view, not the default. RelicCard's `onMutated` call removed on success path (relied on api.patchGame's existing write-through to the V2 cache key — calling refetch was causing visible page-reload flashes). S4Desktop / S4Mobile components are kept around (tree-shaken from production bundle) in case Andrea ever wants them. **Cache version bumped v4→v5** for the new `relicDitherSvg` + `sigils` fields per the persisted-cache shape-change rule. 20 frontend tests (14 RelicCard + 6 useRelicAnimation). Production build clean. **B-Art-1 v3 complete 2026-06-01 (`c9d0740`)** — smarter hero-image picker, dropped in-pool scoring per Andrea's two eyeball rounds: stage 1 picks the FIRST artwork that's not cover-duplicate AND not portrait; stage 2 falls through to first non-cover-dup screenshot. IGDB array order wins inside stage 1 (community curation beats algorithmic taste). 34 swaps applied to live DB via `npx tsx scripts/rescore-game-hero-image.ts` (no `--dry-run`). Companion comparison script `scripts/compare-hero-rescore.ts` shipped for future image-picker tuning eyeball loops.

**R2 complete 2026-06-01** — remap durability surgical fix (follow-up to GD-PR3). The existing `[wrong game?]` remap endpoint rewrote `UserGame.gameId` but didn't touch the source Game's platform-side IDs; next platform sync re-resolved those IDs to the source Game and undid the remap. Andrea reported the same wrong match resurfacing 10+ times. **What shipped (apps/api/src/routes/games.ts):** new `computeRemapFold(source, target)` helper + `PLATFORM_ID_FIELDS` set (`steamAppId` / `xboxTitleId` / `psnConceptId` / `psnNpCommunicationId` / `itchGameId` / `epicCatalogItemId` / `nintendoTitleId` / `gogAppId`). Rule: move a field's value from source → target IFF source has non-null AND target has null. Never overwrite an existing target value (implies a legitimate different mapping). Both remap branches (non-collision + collision-merge) wrap the fold + UserGame update in `$transaction` so clear-source happens BEFORE set-target to release `@unique` constraints cleanly. **Tests:** +3 new R2 test cases (single-field move, no-op when target has a different value, multi-field move with mixed null/non-null source state); existing remap tests updated to mock `$transaction` since the non-collision path is now transactional too. **702/702 backend pass** (+3 vs GD-PR3's 699). **Sticky property for R1 implementor** (the larger durability layer, still deferred): syncRunner should pre-check `UserGame.findFirst` by the incoming platform-side ID before IGDB resolution. If found, reuse that UserGame regardless of Game-row drift. Touches every sync writer (steam.ts, psn.ts, xbox.ts, gog.ts, epic.ts, itch.ts, nintendo.ts) so it's the bigger workstream — R2 should buy Andrea most of the relief without needing R1 yet. **GD-PR3 complete 2026-06-01** — third PR of the GD-series per PAGES_PLAN §3.6. Enriches the legacy `GameDetailDesktop` / `GameDetailMobile` (the S3 surface for Playing / Backlog / OnHold / Dropped games) with the four locked features from §3.5: sub-status (OQ-GD-2), rating UI (OQ-GD-4), HLTB user-vs-community pace row (OQ-GD-5), and times-beaten counter (OQ-GD-3). Wishlist games stay on the S1 surface from GD-PR1 — Andrea's lock. **Schema:** `UserGame.subStatus String?` + `UserGame.completionsCount Int?` via migration `20260531230000_user_game_substatus_completions` applied to Supabase via the Node `$executeRaw` fallback recipe (the documented `prisma db execute` pgbouncer hang recurred). **Backend:** new `apps/api/src/lib/subStatus.ts` enforces valid combos at write time via `SUB_STATUS_VARIANTS` + `isValidSubStatus(status, subStatus)`; locked variants per spec — Playing → `infinite`/`paused`, Completed → `main`/`+side`/`100%`, other statuses → no variants. `PATCH /api/games/:id` extended with `subStatus` + `completionsCount` Zod fields; 400 `INVALID_SUB_STATUS` on mismatched combos; auto-clears stale `subStatus` when status changes without an explicit override (e.g. row has `paused` under Playing, user moves to Completed without touching subStatus → server clears it). completionsCount range 0–999 (defensive against double-click runaways). **Frontend (new files under `apps/web/src/components/screens/gameDetail/`):** `SubStatusPicker` (chip + WAI-ARIA listbox popover, auto-hides when status has no variants), `RatingGrid` (10-box click-to-fill, hover preview, click active to clear), `CompletionsCounter` ([− ×N +], hidden on non-Completed games when value is 0), `HltbPaceRow` (`pace: your 47h vs main 38h (+24%)` with green/amber/red coding). All four wired into both GameDetailDesktop + GameDetailMobile. **Pre-existing bug fixed (mobile):** `changeStatus` + `saveNote` were cache-only and never called `api.patchGame` — mobile status changes / notes never persisted. Fixed while wiring GD-PR3 handlers; mobile now mirrors the desktop optimistic-then-server pattern. **Andrea-driven mid-PR fixes:** (i) sub-status changes were reverting on refresh because `tsx watch` hadn't picked up the regenerated Prisma client → restart-and-retry resolved; (ii) page refresh-feel after every action — root cause was `invalidateLibrary` blindly dropping `game:igdb:` (the dispatcher's cache key) on every PATCH, forcing a loading-skeleton flash. Fixed by splitting `invalidateLibrary` into `invalidateLibraryCounts` (lighter, for in-page edits) + `invalidateLibrary` (full, for membership changes); `api.patchGame` now writes the server response THROUGH to the V2 cache entry (`game:igdb:${updated.game.igdbId}`) and only invalidates the cross-cutting list caches (shelves / dashboard / counts). Actions now feel instant — no skeleton flash. **Tests:** +12 unit (subStatus lib) + 10 PATCH route tests; **699/699 backend pass** (+22 vs GD-PR2's 677). Typecheck + lint + production build all clean. **Sticky property for next workstream (R2 — remap durability):** the existing `[wrong game?]` remap endpoint rewrites `UserGame.gameId` but does NOT fold the source Game's platform-side IDs into the target. Andrea reported the same wrong-game match has now reappeared 10+ times after she remaps it — sync re-resolves the platform-side ID to the source Game, undoing the remap. R2 is the documented small fix; R1 (sync-time pre-check by user's existing UserGame for platform-side ID) is the larger durability layer. **GD-PR2 complete 2026-05-31** — second PR of the GD-series per PAGES_PLAN §3.6. Ships the dedicated S2 (upcoming, not owned) surface, replacing the GD-PR1 S1 fallback for state==='S2'. New `getGameDetailExtras(igdbId)` backend helper fetches per-region × per-platform release dates + IGDB screenshot image_ids + YouTube video_ids; runs parallel with `getReleaseDetails` via `Promise.all`; both cached 24h with graceful per-call `.catch(() => null)`. `GameDetailGameInfo` extended (`releaseDates: ReleaseDateEntry[]`, `screenshotIds: string[]`, `videoIds: string[]`). New S2Desktop / S2Mobile + 4 sub-components (`ReleaseDatesPanel`, `PreorderLinks`, `ScreenshotsRail`, `VideosRail`): hero with giant `T-N` countdown + live 1Hz d/h/m/s tick + `[+ wishlist]` amber dominant + `[+ add to library (preorder)]` alt; collapsible per-region × per-platform release dates panel; Steam + GOG + PSN preorder links (Xbox/Epic/Nintendo deferred per OQ-GD-14 — need slugs we don't capture); horizontal scrollers for screenshots (open `t_screenshot_huge` in new tab) + videos (link out to YouTube — embed iframe deferred to polish PR for CSP carve-out); `PriceOffersCard` reused from GD-PR1 (MSRP for upcoming per Andrea's call); HLTB intentionally absent (§3.3). Dispatcher routes `state==='S2'` to S2 components; S1/S3/S4 paths unchanged from GD-PR1. **Andrea-driven mid-PR fix:** stale SWR cache entries pre-dating GD-PR2's new fields crashed S2 sub-components with `entries.length` on undefined; fixed via cache version bump (v3 → v4) + defensive `?? []` guards at consumer call sites. 3 new backend tests (677/677 pass, +3 vs GD-PR1). Bundle delta: GameDetailV2Desktop 23.18 → 28.03 kB (+1.06 kB gzipped), GameDetailV2Mobile 18.34 → 22.86 kB (+1.13 kB gzipped). Sticky property for GD-PR3: dispatcher's state-branch pattern + optional `userGameId` prop on legacy components are the clean seams for adding S3 enrichments in a future PR. **GD-PR1 complete 2026-05-31** — first PR of the GD-series. Per PAGES_PLAN §3.6 the first PR of the GD-series. Establishes the 4-state architecture: route migrates from `/game/:userGameId` to `/game/:igdbId` (cuid form 301s during a transition window per OQ-GD-1); state-aware shell detects S1 (released-not-owned) / S2 (upcoming-not-owned) / S3 (owned-in-progress) / S4 (owned-completed) per OQ-GD-12 (Wishlist-status game with future release → S2; everything else routes by status + UserGame existence); S1 ships fully with cover + meta + `[+ add to library]` (dominant) + `[+ wishlist]` (alt) + description + ITAD price-offers card consuming the DEALS-PR1 pipeline per §8.6 sequencing recommendation + lateral-nav / Collections / events placeholders; S2/S3/S4 fall back to today's existing rendering as a transition strategy (GD-PR2/PR3/PR4 fill them in). **Post-deploy fixes shipped with DEALS-PR1 (commit `f51b496`):** ITAD `/games/prices/v3` returns `shop.name` not `shop.title` (fixed in client type + sync + probe — silent failure surfaced via the new diagnostic probe `/api/admin/deals/probe`); `classifyShop` now defensively returns `'excluded'` on undefined input so a future field-shape surprise can't crash the entire sync; removed `useAutoSync` from `AppShell` — Hoard no longer fires platform syncs just because the app opened (Andrea: "I think i want to remove the rule that it does a sync every time we open up hoard"); added `/api/admin/deals/status` row-counter for diagnosing in-flight refreshes. **DEALS-PR1 live verification 2026-05-31:** 2087 scanned, 2050 resolved (98% ITAD match), 933 deals upserted, 4377 snapshots created, 0 failed. **DEALS-PR1 complete 2026-05-31** — first entry in the DEALS-series per PAGES_PLAN §8. New `/deals` top-level surface backed by ITAD; locale-aware pricing via new `User.marketCode`; Tier-1 first-party + Tier-2 reseller allow-list (Humble · Instant Gaming · GMG · Kinguin · CDKeys) classification; per-reseller affiliate router (env-var-keyed, passthrough when unset); CM12 already-owned-exclusion-unless-wishlisted; `// historical low` + `// trending down` chips powered by `PriceSnapshot` history; Sidebar nav + MobileTabBar widened to 5; Settings → Account market picker; Dashboard alerts-strip extended with wishlist-deals chip. Pre-deploy: Andrea provisions `ITAD_API_KEY` + per-reseller affiliate IDs out-of-band. **Library overview card restructure complete 2026-05-31** — Andrea-flagged follow-up after B-IGDB-3b2. Library overview shelves now use landscape cards with proper IGDB hero images (artworks[0] preferred, screenshots[0] fallback) instead of the portrait box art used everywhere else. Now Playing shelf cards are 280px wide vs 220px for other shelves (Andrea: "the same horizontal space supports 4 cards" vs 5). Filtered views (`/library/:status`, `/library/by-X/:slug`) keep portrait `ShelfItem`. New `Game.heroImageUrl` schema column (migration `20260531120000_game_hero_image_url` applied to Supabase); new `LibraryOverviewCard` component (~80 lines, 6 tests). Backfill via `scripts/backfill-game-hero-image.ts` running for ~2185 existing games (re-fetches IGDB artworks/screenshots; ~12 min total). **B-IGDB-3b2 + Steam-style sidebar browse-by complete 2026-05-31** — final piece of the IGDB-tag-triple workstream. Promotes genre/theme/perspective from secondary filters into PRIMARY lens types: three new routes `/library/by-genre/:slug` / `/library/by-theme/:slug` / `/library/by-perspective/:slug`; `// browse by` collapsible groups in the Sidebar (Steam-style always-visible left rail — per Andrea's feedback the overview-bottom panel was buried; mobile keeps the inline panel since no sidebar there); `[change lens ▾]` chip on filtered views to pivot between primary lenses; find input (`?q=`) on filtered views; secondary FilterPopover visibility filtered per primary lens. New `useLensRoute()` hook centralizes URL → lens parsing; new `GET /api/games/lens-index` backend endpoint serves all tag values + counts (used by browse-by + slug→canonical-name resolution). Andrea also flagged the NEXT workstream as **game card restructure** — landscape-orientation Library overview cards, larger Now Playing shelf cards (4 across vs 5 for other shelves); deferred as focused follow-up after this lands. **Library secondary filter UX rework (FilterPopover) complete 2026-05-31** — replaces the B-IGDB-3b1 chip strips on `/library/:status` with three inline single-select dropdown triggers `[genre: any ▾] [theme: any ▾] [persp: any ▾]` sitting on the same filter bar row as `[sort: ▾]`. Resolves Andrea's live feedback that the chip strips spread options across the page. New `apps/web/src/components/library/FilterPopover.tsx` (~190 lines) with WAI-ARIA listbox keyboard a11y (Arrow Up/Down nav, Home/End, Enter/Space select, Escape close, click-outside close). Counts shown next to each option via new `pickTopTagCounts()` helper. URL state unchanged (`?genre=&theme=&perspective=`). **PERF-2 partial (Library grid paint perf) complete 2026-05-31** — second perf pass after PERF-1. **Honest scope:** only `React.memo` on `ShelfItem` landed; the `content-visibility: auto` + `contain-intrinsic-size` half was reverted same-day after Andrea's live eyeball showed visual breakage on flex-wrap children. `React.memo` half still useful (chip toggles no longer re-render ~500 prop-identical cards). PERF-3 (full virtualization with `@tanstack/react-virtual`) reserved if in-shelf interaction still feels slow. **PERF-1 (SWR cache localStorage persistence) complete 2026-05-31** — page reloads were cold (in-memory SWR Map only); now persists to `localStorage` under `hoard:cache:v1:` namespace, hydrates on module load, writes through on every `set()`, and removes matching keys on `invalidate(prefix)`. **B-IGDB-3b1 (Library secondary filter chip strips) complete 2026-05-30** — sixth implementation PR from PAGES_PLAN v2 spec, follows B-IGDB-3 partial the same day. Wires the IGDB-tag triple foundation into the Library Status-lens secondary filter UI via three new chip rows (genre · theme · persp) bound to `?genre=&theme=&perspective=` URL params. Pure-frontend follow-up — server filter surface was already live. Library primary-lens routes (`/library/by-genre/:slug` etc.) + browse-by panel still deferred to **B-IGDB-3b2** (structurally bigger; needs URL design + Library page IA changes). Plan docs: `docs/SYNC_EXPANSION_PLAN.md` (M-series) + `docs/ADMIN_POLISH_PLAN.md` (A-series A1+A2) + **`docs/PAGES_PLAN.md` (the v2 spec — source of truth for upcoming workstreams; DASH-PR1+PR2+PR3 + REL-PR1 + B-IGDB-3 partial + B-IGDB-3b1 marked Done with landing notes in §7.6 + §5.6 + §2)**.

**B-IGDB-3b1 complete 2026-05-30 (this commit).** Sixth implementation PR derived from PAGES_PLAN v2 spec §4.4. Wires the IGDB-tag triple foundation (shipped in B-IGDB-3 partial earlier today) into the Library Status-lens secondary filter UI. **Pure-frontend follow-up** — server filter surface (`/api/games?genre=&theme=&perspective=`) was already live from B-IGDB-3 partial. **What shipped:** new `apps/web/src/lib/pickTopTags.ts` (~50 lines) — `pickTopTags(games, dimension, cap)` returns top-N distinct tags ordered by occurrence count desc + name asc tiebreak (deterministic); `filterByTag(games, dimension, value)` is the composable client-side predicate. Both dimension-agnostic across genre/theme/perspective via a `TagDimension` union. **LibraryDesktop + LibraryMobile** — three new chip rows (genre · theme · persp) below the existing platform row on `/library/:status`. Each row only renders when the loaded games carry that tag (progressive disclosure pre-backfill). Chip values derived from the FULL shelf pre-filter (NOT the post-filter slice), so the user sees all options even after narrowing on another dimension; if the active filter is a tag not in the top-N (edge case after backfill changes the distribution), it's prepended so the user can still see + un-toggle it. **URL-state-resident** via `useSearchParams`: `?genre=` / `?theme=` / `?perspective=` — composable with each other + the existing `?sort=` param; cleared on `statusParam` change (same React Router 6 reuse-across-route-params reset as `platFilter`). `applyFilters` extended to compose three `filterByTag` calls after the platform filter, before sort. **Tests:** 11 unit in new `pickTopTags.test.ts` (count-desc + name-asc ordering / cap / per-dimension reads / empty edge cases / null-undefined no-op / dimension-specific filtering). Targeted vitest **44/44** (11 pickTopTags + 1 LibraryDesktop + 14 api-invalidation + 18 Dashboard.bento). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** LibraryDesktop 16.09 → 17.29 kB (+0.38 kB gzipped), LibraryMobile 14.35 → 15.53 kB (+0.38 kB gzipped). Modest. **Still deferred to B-IGDB-3b2:** primary-lens routes `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug` per PAGES_PLAN §4.4.1; browse-by panel on Library overview. These need URL design (e.g. `/library/by-genre/RPG?perspective=First-person` — primary vs. secondary precedence) + Library page IA changes; structurally distinct from chip strips. No `AGENT.md` entry — B-IGDB-3b1's decisions constrain the secondary-filter shape specifically. **Sticky property for B-IGDB-3b2 implementor:** the helper file is dimension-agnostic — extending `TagDimension` to include Collection / Developer / Publisher / Series (post B-Stash-4 + B-Stash-5) is a one-line union addition. Chip-row JSX in both Library components is pure repeat-for-each-dimension; new lenses thread in without restructuring. Same goes for the URL state — `setTagFilter('genre', ...)` already generalizes by dimension.

**B-IGDB-3 partial complete 2026-05-30 (this commit).** Fifth implementation PR derived from PAGES_PLAN v2 spec §2 B-IGDB-3 + §7 Dashboard. Ships the **foundation + Dashboard consumer**; Library consumer (filter chip strips + primary-lens routes + browse-by panel) split into a follow-up **B-IGDB-3b**. **Foundation:** `Game.themes String[]` + `Game.playerPerspectives String[]` schema columns via migration `20260530120000_game_themes_perspectives` (hand-written SQL with `ADD COLUMN IF NOT EXISTS` for prod idempotency). IGDB service factories (`mapToSearchResult` + 3 `IgdbUpcomingRelease` factories + `getReleaseDetails`) extended to fetch + map both axes; query strings include `themes.name, player_perspectives.name`. All `Game.upsert` call sites threaded through: `syncRunner` (main path) + `wishlistImport` + `routes/upcoming` (wishlist toggle) + `routes/games` (remap). `WishlistRelease` NOT extended — its snapshot doesn't carry themes/perspectives (Releases cards don't display them today); wishlist-scope branch + `useUpcoming` fallback default to `[]`. **Types:** `Game.themes` + `Game.playerPerspectives` (required `string[]`); `IgdbSearchResult.themes` + `playerPerspectives`; `IgdbUpcomingRelease.themes` + `playerPerspectives`; `DashboardStats.themes` + `playerPerspectives` (`{name, count}[]` same shape as `genres`). **Backfill script** at `scripts/backfill-game-tags.ts` — re-fetches existing Game rows from IGDB; throttled ~3 req/s; resumable; supports `--dry-run`; not auto-run (Andrea triggers post-deploy). **Dashboard consumer:** server iterates `aggUserGames` once tallying genres + themes + perspectives in one pass (one extra column on the existing `select`; no new round-trip); both desktop + mobile breakdown cards render a 3-tab strip bound to a single parent-owned `breakdownTab` state. Active tab uses `.chip.solid.amber` (same pattern as DASH-PR2 `PeriodToggle`); disabled when a dimension is empty in the user library. **Server `/api/games` extended** with `?genre=&theme=&perspective=` query params + Prisma `array.has` filter assembly — **API surface ready for B-IGDB-3b** Library consumer; frontend client `GamesParams` extended in lockstep. **Deferred to B-IGDB-3b:** Library secondary filter chip strips on `/library/:status` (consumer of the now-ready server params); primary-lens routes `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug`; browse-by panel on Library overview. **Tests:** +4 frontend in `Dashboard.bento.test.tsx` (3 tabs render; defaults to genre; theme-click swaps body to themes; disabled state when series empty). 9 fixture-factory backfills with `themes: []` + `playerPerspectives: []` defaults across `bucketing.test.ts`, `empty-state.test.tsx`, `mobile.test.tsx`, `primitives.test.tsx`, `pickWishlistedPlatformChips.test.ts`, `ReleasesRecentDesktop.test.tsx`, `AddGameModal.test.tsx`, `RemapGameModal.test.tsx`, `igdbMatch.test.ts`. **18/18 Dashboard.bento + 112/112 releases tests + 39 backend suites / 589 tests pass.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** DashboardDesktop chunk 16.21 → 17.45 kB (+0.39 kB gzipped — 3-tab UI logic), DashboardMobile 12.24 → 13.32 kB (+0.34 kB gzipped). Library bundles unchanged (consumer deferred). No `AGENT.md` entry — B-IGDB-3 partial's decisions constrain the IGDB-tag triple specifically. **Sticky property for B-IGDB-3b implementor:** server-side filter surface is live (`/api/games?genre=&theme=&perspective=`); client `GamesParams` type already extended. Adding chip strips on the existing `/library/:status` view consumes the server filter (or filters loaded games client-side for symmetry with the existing `platFilter` pattern — same trade-off as platform chips). The full primary-lens-route rework (`/library/by-X/:slug`) is structurally distinct from chip strips (URL design + Library page IA); can sequence as B-IGDB-3b1 (chips on Status lens) → B-IGDB-3b2 (new primary-lens routes + browse-by panel). **Pre-deploy operational task:** apply migration via `npx prisma db execute --file packages/db/prisma/migrations/20260530120000_game_themes_perspectives/migration.sql --schema packages/db/prisma/schema.prisma && npx prisma migrate resolve --applied 20260530120000_game_themes_perspectives --schema packages/db/prisma/schema.prisma`. Then run `npx tsx scripts/backfill-game-tags.ts --dry-run` once to sanity-check IGDB rate limit + sample output before the real run.

**REL-PR1 complete 2026-05-30 (this commit).** Fourth implementation PR derived from PAGES_PLAN v2 spec §5.4 #2. Surfaces the user's per-platform wishlist subset on Releases cards (`// wishlisted: PS5 · Switch` desktop / `wish:` mobile / `wishlisted on` HeroCountdown) when `UserGame.wishlistedPlatforms ⊊ game.platforms`. Closes the CM12 follow-through gap on the Releases surface. **What shipped:** `packages/types/src/index.ts` — `IgdbUpcomingRelease.wishlistedPlatforms: string[]` field added (required); server factories in `apps/api/src/services/igdb.ts` (3 places: `getUpcomingReleases` / `getRecentlyReleased` / `getReleaseDetails`) default to `[]`; route layer enriches — `userGameMap` in `apps/api/src/routes/igdb.ts` widened from `Map<igdbId, userGameId>` to `Map<igdbId, { id, wishlistedPlatforms }>`; both wishlist + other-scope branches plus `routes/releases.ts` (both `starred` and `hyped` branches) thread the field through Prisma `select`. **Client helper** `pickWishlistedPlatformChips(release)` in `apps/web/src/components/screens/releases/utils.ts` returns `{ mode: 'generic' | 'wishlist', platforms: string[] }`. Edge cases pinned in tests: empty → generic; full-set match → generic; strict subset → wishlist; data-drift (entries not in IGDB platforms) → wishlist (OQ-REL-3 v1 lock); order preserved from `wishlistedPlatforms`. **Components updated:** `ReleaseCard` (all 3 variants), `MobileReleaseRow`, `HeroCountdown`. **Skipped per spec:** `AgendaRail` (single-line UI, no space — analogy with OQ-REL-2); `WishlistEmptyRecommendation` (recommendations are pre-wishlist by construction, helper naturally returns generic). `useUpcoming` IGDB-outage fallback defaults `wishlistedPlatforms: []` (acceptable degradation — WishlistRelease doesn't carry the per-platform context, lives on UserGame.wishlistedPlatforms requiring the join). **Tests:** 9 unit in new `pickWishlistedPlatformChips.test.ts` (every truth-table row + edge case) + 3 visual regression in `primitives.test.tsx` + 2 in `mobile.test.tsx` + 5 fixture factory backfills (bucketing / empty-state / mobile / primitives / ReleasesRecentDesktop). **112/112 releases tests pass** + **39 backend suites / 589 tests pass** (no regressions). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** ReleasesDesktop +0.04 kB gzipped; ReleasesMobile unchanged. No `AGENT.md` entry — REL-PR1's decisions constrain the Releases card surface specifically. **Sticky property for OQ-REL-3 v2 follow-up:** data-drift case currently renders identically to strict-subset (v1 lock); helper return shape is ready to extend (add `drift: boolean` flag or split `mode` into 3 values) if cohort signal demands distinct visual treatment.

**DASH-PR3 complete 2026-05-30 (this commit).** First entry into the bento alerts-strip surface (span-12 at top of grid per PAGES_PLAN §7.4) — resolves OQ-DASH-11. Surfaces any connected Platform with `syncStatus === 'error'` as a slim red-bordered chip. **Two design calls locked via AskUserQuestion before write:** (1) **single aggregated chip** for multi-platform errors — lists codes inline at 1-2 errored (`⚠ steam · psn — sync error`), collapses to a count at 3+ (`⚠ 3 platforms — sync error`); one click → `/settings/platforms`. Avoids chip-overflow when many platforms error simultaneously; (2) **not dismissible** — auto-disappears when the underlying error clears on the next successful sync. No localStorage state to manage; no risk of dismissing-and-forgetting. **What shipped:** new `apps/web/src/components/screens/dashboard/AlertsStrip.tsx` (~75 lines) — pure component, returns `null` when no errors to surface. Inside the bento grid context spans `gridColumn: '1 / -1'`; outside (mobile) the parent wraps in a padding container that's also conditionally rendered. Visual treatment: `Icon name="warn"` (red) + `// sync error` (red) + ` · steam · psn` (paper-dim) + `view in settings →` (faint, right-aligned). `border: 1px solid var(--red-dim)` on `var(--ink)` background. Accessibility: `role="region"` + `aria-label="Dashboard alerts"`; chip is a real `<button>` with descriptive aria-label spelling out the count for screen readers. Wired into DashboardDesktop (first child of bento grid; reflows when null) + DashboardMobile (parent-level conditional render avoids blank-space padding). **Tests:** 8 unit tests in new `apps/web/src/components/screens/dashboard/__tests__/AlertsStrip.test.tsx` (null when no platforms / no errored platform; correct copy for 1 / 2 / 3+ errored; click navigates to `/settings/platforms`; NOT dismissible regression guard with exactly one button + no dismiss affordance; edge case of `syncable=false` with `syncStatus='error'` still surfaces). Plus 2 integration tests in `Dashboard.bento.test.tsx` (strip hidden by default; renders at top of bento as first child when errored platform present). **22/22 across both files.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** DashboardDesktop 16.10 → 16.21 kB (+0.01 kB gzipped), DashboardMobile 12.01 → 12.24 kB (+0.05 kB gzipped). Negligible. No `AGENT.md` entry — DASH-PR3's decisions constrain the alerts-strip surface specifically. **Sticky property for next alerts-strip workstream** (Q-series pending-review / EV-PR3 events-missed / Deals callout): add new chip kinds INSIDE `AlertsStrip.tsx` rather than mounting parallel strip components. The `null`-when-empty contract is what keeps the bento layout collapsing cleanly when no alerts exist; multiple parallel components would break the bento spacing. Aggregation pattern (list at 1-2, count at 3+) + chip styling (warn icon · semantic label · `view in settings →` affordance) generalize to other chip kinds with different icons + nav targets.

**DASH-PR2 complete 2026-05-30 (this commit).** Added the `// this year / this month / all time` toggle in a **combined `card-progress` (span-8)** that holds completion + achievements as two halves divided by a hairline, with one toggle in the card header. **Semantics locked before write** via AskUserQuestion: engagement-scoped (`this year` / `this month` = stats AMONG UserGames whose `lastPlayedAt` falls in the period). Schema-additive on `DashboardStats` — new `period: 'all' | 'year' | 'month'` + `periodStats: { completedCount, totalGames, completionPct, achievementsRollup }` carry the scoped variants; top-level all-time fields stay untouched (greeting header reads them). When `period === 'all'`, server collapses `periodStats` to mirror all-time — frontend has no branching. **Andrea-driven iteration after first-pass eyeball:** v1 put a separate copy of the toggle on each of two span-4 cards (`card-completion` + `card-achievements`). Andrea's feedback: *"if this is the intended logic then the two widgets don't have to be separated, but unified so the control is shown once"* + *"it reload the whole page"*. Two fixes in the same iteration: (a) **merged into one `card-progress` (span-8)** with completion + achievements as two halves; **DASH-PR1's row 2 layout updated** to `backlog-picker (span-4) | card-progress (span-8)` (was `(4) | (4) | (4)`); (b) **fixed the loading-skeleton flash on chip click** via `useRef<DashboardResponse | null>(null)` for stale-while-revalidate — once any payload has loaded, period switches fall back to the previous payload while the new one fetches. Toggle chip flips immediately; numbers update when fetch resolves. **What shipped:** `packages/types/src/index.ts` (new `DashboardPeriod` type + `DashboardPeriodStats` interface; `DashboardStats` extended); `apps/api/src/routes/dashboard.ts` (`parsePeriod` silently collapses unknown values to `'all'` — toggle UI only emits known values; `periodStart` returns UTC start-of-current-calendar-month/year/null); refactor — dropped the separate `achievementsRows` query since its `achievementsByPlatform` select rides along on `aggUserGames` (period semantics need `status` + `achievementsByPlatform` + `lastPlayedAt` on the same row; single iteration tallies all-time + period in one pass); `apps/web/src/components/screens/dashboard/PeriodToggle.tsx` (new — `role="radiogroup"` + `role="radio"` three-state chip group, one instance per Dashboard component in the combined card's header); `apps/web/src/hooks/useDashboard.ts` accepts `DashboardPeriod` arg (default `'all'`) with cache keys `'dashboard'` (legacy) / `'dashboard:year'` / `'dashboard:month'` — `cache.invalidate('dashboard')` prefix-matches all three; `apps/web/src/lib/api.ts` `api.dashboard(period?)` omits `?period=` for the default; DashboardDesktop + DashboardMobile both lift `[period, setPeriod] = useState<DashboardPeriod>('all')` AND `lastGoodRef` at the parent — combined `card-progress` mounts the sole `PeriodToggle` bound to that state; halves read from `stats.periodStats.*`, greeting header still reads top-level all-time. When achievements data is absent for the active period, the hairline + right half are hidden and completion fills the card. **Tests:** +7 backend (default + unknown collapses + year denominator + month boundary + null-lastPlayedAt excluded + scoped achievements + top-level unchanged) → **39 backend suites / 589 tests pass** (+7 vs DASH-PR1's 582). +4 frontend post-iteration: one shared toggle in `card-progress` (not duplicated per half) + total of exactly one `[role="radiogroup"]` on the page; defaults to `period='all'` and reads from `periodStats` not top-level (sample fixture intentionally diverges the two so the binding source is unambiguous); year-chip click refetches with `?period=year` and both halves update together; **no-skeleton-flash regression guard** — bento stays mounted while a new period is in flight, previous values remain visible. Plus 4 api-dashboard URL encoding + 1 invalidation regression. **48/48 across targeted vitest scope.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta (post-iteration):** DashboardDesktop 15.18 → 16.10 kB (+0.19 kB gzipped), DashboardMobile 11.41 → 12.01 kB (+0.17 kB gzipped), initial JS index 257.05 → 257.08 kB (+30 bytes gzipped — type-only). No `AGENT.md` entry — DASH-PR2's decisions constrain Dashboard specifically. **Sticky property for future DASH workstreams that add period-aware cards:** the toggle owner is the parent (single source of truth). New period-scoped cards either consume the same parent state OR call `useDashboard(period)` themselves — don't introduce a second period-state owner. AND if a card's data shares the same period scope as another card, **merge them rather than duplicating the toggle** — Andrea's lock (the v1 split was the visual mistake caught on first eyeball).

**DASH-PR1 complete 2026-05-30 (this commit).** Refactored `DashboardDesktop` from flat single-column to a 12-column CSS bento grid per PAGES_PLAN §7.4 (card *size* communicates importance: span-12 alerts strip · span-6 hero · span-3 countdowns · span-4 stat cards · span-6 breakdowns · span-12 temporal). Mobile collapses to 1-col in OQ-DASH-1 #1 fixed-by-importance order. **Two decisions locked before write** via AskUserQuestion: (1) the `// the hoard · in numbers` 8-tile stat grid **removed entirely** (informationally redundant with sidebar shelf counts + new dedicated cards); (2) the multi-card `// wishlist · dropping soon` panel **replaced by a single dominant `NextReleaseCountdown` (span-3)** + `see all →` link to `/releases` — Dashboard answers "what's next?" with the next single release; the multi-item view lives on `/releases`. New `apps/web/src/components/screens/dashboard/NextReleaseCountdown.tsx` (compact span-3 card with live 1Hz d/h/m/s tick via `useNow(1000)` + `countdownParts`); inline `BentoCard` wrapper in `DashboardDesktop.tsx` applying `gridColumn: span N` + `data-bento-span` introspection attr. Bento layout: Row 1 — `now-playing+rotation (span-6)` · `next-release (span-3)` · empty 3-col tail reserved for EV-PR1 next-event countdown; Row 2 — `backlog-picker (span-4)` · `completion-gauge (span-4)` · `achievements-gauge (span-4)`; Row 3 — `genre-breakdown (span-6)` · `hours-by-platform (span-6)`; Row 4 — `activity-heatmap (span-12)`. Slim alerts strip (span-12) deliberately absent — threads in via Q-series / EV-PR3 / DASH-PR3 / Deals as callouts arrive. Now-playing card adds "active rotation · playing × N" sub-section below the hero when `nowPlaying.length > 1` (compact rows for the other Playing games — API already returns up to 3 sorted by `lastPlayedAt desc`). Greeting + bignum + system status hero row stays **above** the bento grid (terminal-aesthetic-y personal touch). GD-PR3 inline edits explicitly out-of-scope per §7.4 matrix. **Bonus:** fixed a pre-existing React key warning in the system status `.map` (Fragment children get keys, not the Fragment — moved to `<Fragment key={p.code}>`). **Tests:** 8 new in `Dashboard.bento.test.tsx` (card presence + correct spans per §7.4 + active rotation conditional + NextReleaseCountdown surfaces next wishlist title + removed sections stay removed as anti-regression pins + mobile asserts the fixed-by-importance card order). Targeted vitest covering my changes: 46/46 pass (8 bento + 10 layout + 5 shell-persistence + 7 cache + 13 api-invalidation + 3 useNow). Typecheck + lint + rename-rule + production build (`✓ built in 2.06s`) all clean. DashboardDesktop chunk 15.18 kB (gzip 4.23 kB), DashboardMobile 11.41 kB (gzip 3.48 kB). No `AGENT.md` entry — DASH-PR1's decisions constrain Dashboard specifically. **Sticky property for next Dashboard contributor:** add new cards via `<BentoCard span={N} testId="card-foo">`; mobile auto-folds in `[data-testid^="card-"]` order — the test pins desktop layout + mobile order, so adding cards to the desktop grid in a different position will fail both axes (caught early). Reserve the alerts strip as `span-12` at top of grid (no slot today; render at `gridColumn: '1 / -1'` when callouts arrive).

Natural next-step candidates (post-DEALS-PR1):
- **GD-PR1 — GameDetail v2 (S1 + ITAD price-offers card)** — biggest single unlock from PAGES_PLAN. Shares the ITAD client + affiliate router + locale-currency pipeline DEALS-PR1 just shipped (B-Storefront-2). Now is the natural time per the §8.6 recommendation: "DEALS-PR1 first (more dependent data shape), then GameDetail v2 GD-PR1 just reads the existing infrastructure."
- **DEALS-PR2 — Library completion + sale-event grouping** — `SaleEvent` table + `/deals/event/:slug` sub-route; library-completion section gated on B-Stash-5 series reference data (so this might land as just the sale-event half first).
- **DEALS-PR3 — Physical deals** — Amazon Product API integration; needs Andrea's Amazon Associate sign-up first.
- **DEALS-PR4 — Filter + mobile polish** — discount-range filter + sort options + mobile-compression refinements per OQ-DEALS-4.
- **Mobile shelves landscape port** (deferred from card restructure 2026-05-31). LibraryDesktop overview now uses landscape `LibraryOverviewCard`; mobile shelves still render portrait via `MobileShelf`. Revisit if Andrea reports the desktop landscape look should propagate to mobile — the swap is structurally small (mirror `Shelf` refactor in `MobileShelf` with mobile-appropriate dims, e.g. 160×90 / 200×113 for the Now-Playing-vs-others split).
- **Change-lens UX rework** (Andrea flagged 2026-05-31, see Known gaps). Defer until after the card restructure + status-as-secondary on tag lenses lands. Consider hiding the chip when no pivots are available, renaming the label, or moving the affordance onto the FilterPopover values themselves.
- **PERF-3 — Full grid virtualization (back-pocket).** Reserved if PERF-2's `React.memo` (the only surviving piece — `content-visibility` was reverted) proves insufficient on Andrea's largest shelves. Add `@tanstack/react-virtual` (~3 kB) over a CSS-grid-with-computed-`repeat(N, ${coverW}px)` (drop the `flex-wrap` layout to avoid the same content-visibility-style coordination problems). Cover dimensions are bounded by the `coverDensity` preference (3 tiers) so row-height estimation is straightforward.
- **B-IGDB-3b2 — Library primary-lens routes** `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug` + browse-by panel on Library overview. Structurally bigger; needs URL design + Library page IA changes.
- **REL-PR2 — `// out today` chip for same-day releases** (OQ-REL-5; small standalone).
- **Bigger structural unlocks:** DEALS-PR1 (§8 new top-level surface), EV-PR1 (§6 Events foundation), GD-PR1 (§3 GameDetail v2).
- **B-IGDB-3 — Genre + theme + perspective filter** (lightest "real feature" from the spec; lets the bento breakdown panel become a 3-tab strip).
- **DEALS-PR1 — Deals foundation** (PAGES_PLAN §8 new surface; Dashboard alerts-strip callout slot reserved).
- **EV-PR1 — Events foundation** (PAGES_PLAN §6 new surface; fills the empty 3-col tail on bento row 1 with next-event countdown).
- **GD-PR1 — GameDetail v2 route migration + S1** (PAGES_PLAN §3 four-state architecture; biggest unlock).

**M3 complete 2026-05-29** (this commit + backend WIP in `2bb75f0`; post-deploy hotfixes `3393fd6` + `2b9967f`). Nintendo Switch auto-sync via the Parental Controls "Moon" API. `Game.nintendoTitleId String? @unique` added (16-char hex applicationIds — opaque, like Epic's catalogItemId). Migration `20260528170000_nintendo_title_id` applied via `prisma db execute` + Node `$executeRaw` migration-record fallback. Backend `apps/api/src/services/platforms/nintendo.ts` (~462 lines) implements the full Nintendo Account OAuth + PKCE chain: PKCE pair generation, redirect-URL parsing (handles npf:// custom-scheme + query-string + bare code), session_token + access_token + ensureFresh refresh, Moon /fetchOwnedDevices + /fetchLatestMonthlySummary, per-applicationId aggregation across players + devices. Moon API version constants mirror pynintendoparental's const.py (v2.4.0 / build 660 / Android 14) — Nintendo bumps the floor every ~2-3 months and gates on an (X-Moon-Os, version, build) allowlist. New 7-step `NintendoGuidedFlow{Desktop,Mobile}` at `/settings/platforms/nt/connect`: explainer + parent-as-own-child diagram, Parental Controls app install (App Store / Play Store deep links), Switch pairing, auth URL open (desktop renders a QR code for desktop→mobile sign-in handoff via the `qrcode` npm package), copy npf:// redirect URL from the "site can't be reached" error page, paste into Hoard, success. Skip-ahead link on steps 2+3 for returning users. Auth URL + verifier fetched lazily on first reach of step 4. NT flipped from `syncable: false → true` across PlatformDetail* + Settings* + Mobile platforms list. **Hard Rule #6 amendment shipped** per M-D14 — rewritten from "Nintendo and Epic are manual-only" to "Manual-add is the fallback, not the rule" (architectural decision mirrored in AGENT.md). Tests: **39 backend suites / 578 tests pass** (+44 from M2's 534 — 32 nintendo.ts + 11 platforms route + 1 derived); +6 NintendoGuidedFlow frontend + 1 api-invalidation. Typecheck + lint + rename-rule + production build all green. Production bundle adds ~41 KB / ~14.6 KB gzipped as the lazy-loaded NintendoGuidedFlowDesktop chunk. **Operational tax to plan for:** Moon API version constants need a refresh every ~2-3 months — `errorCode=update_required` is the canary; source of truth is pynintendoparental's const.py. Captured as the M-series' sole AGENT.md decision because it's architectural-shaped (constrains the maintenance shape of any M-class platform).

**M2 complete 2026-05-28** (`f2d22ed` + `7d9d9e8`). Epic Games Store auto-sync via the official Epic Online Services APIs. `Game.epicCatalogItemId String? @unique` added (opaque hex string — unlike Steam/PSN/Xbox/itch which use Int IDs). Migration `20260528160000_epic_catalog_item_id` applied. New `apps/api/src/services/platforms/epic.ts` (~330 lines) implements the full auth flow (`exchangeEpicAuthCode` / `refreshEpicToken` / `ensureFreshEpicCredentials` — identity-equality pattern from GOG / `getEpicUsername` / `syncEpicLibrary`). Uses public "Fortnite Android" client credentials (env-vars-only: `EPIC_CLIENT_ID` + `EPIC_CLIENT_SECRET`) — same pattern as GOG's Galaxy creds. Cursor-paginated library walker; human-readable titles resolved via a secondary `catalog/.../bulk/items` batch call (Epic's library endpoint returns opaque `appName` + `catalogItemId` only). syncRunner cascade extended: `steamAppId → psnConceptId → xboxTitleId → gogAppId → itchGameId → epicCatalogItemId → title-search → localization`. New 5-step guided flow at `/settings/platforms/ep/connect` (open Epic login → sign in → copy JSON code → paste → all set); `extractCode()` handles bare code, full URL, or JSON blob paste shapes. EP flipped from `syncable: false → true` across all 5 UI surfaces. Backend 534 tests pass (+23 from M1's 511); typecheck + lint clean. **Pre-deploy env-var setup required:** `EPIC_CLIENT_ID` + `EPIC_CLIENT_SECRET` need to be on Railway → API → Variables before connect works.

**M1 complete 2026-05-28** (`8005ed6` + `12c5991`). itch.io auto-sync via the official server-side API. `IT` added to `PlatformCode` enum + `Game.itchGameId Int? @unique` (migration `20260528150000_itch_platform_code`). New `apps/api/src/services/platforms/itch.ts` paginates `/my-owned-keys` (200ms polite delay, 50-page cap, throws on 401/403). syncRunner resolution cascade extended for itchGameId. New 4-step guided flow at `/settings/platforms/it/connect`; `api.connectItch(apiKey)` validates against itch.io's `/me` endpoint before persisting. Live-verified end-to-end. **Match-rate caveat:** most itch.io games are jam entries / hobby releases not in IGDB; manual-add still works for those.

**M0 complete 2026-05-28** (`18a46a8` + `36fcf6d`). Foundational data-model fix: cross-platform games (e.g. Cyberpunk owned on both Steam + PSN — 44 Steam achievements vs 45 PSN trophies, entirely different sets) were having their flat `achievementsEarned/Total/Percent/UpdatedAt` columns overwritten on every sync cycle. New `UserGame.achievementsByPlatform Json @default('{}')` replaces the 4 flat columns. Migration applied to Supabase via documented `prisma db execute` + `migrate resolve --applied` recipe; backfill heuristic uses Postgres `?` operator for JSONB key-presence. Q0 ambiguity probe: 33/1222 ambiguous (2.7%) — self-healing within one sync cycle. Post-migration verification via `scripts/check-trophies.ts`: 887 ST + 335 PS = 1222 entries (exact match with probe predictions). PSN trophy writer + Steam achievement writer now merge into the new column preserving entries they don't own; `promoteWishlistOnEngagement` reads ANY entry. GameDetail Desktop + Mobile render one row per entry via new `buildAchievementRows(abp)` helper. Post-sync state verified: 9 cross-platform games rendering two rows with correct labels + colors (Hogwarts Legacy PS 100% + ST 62% the canonical test case). **Next: M2 Epic** when Andrea gives the go-ahead.

Natural next-step candidates (post-M-series):
- **Post-add GameDetail edit affordances** (referenced in `docs/USER_RESEARCH.md` O14 last bullet — locked constraint: manual playtime editor is per-row, available only for non-synced platform codes `{ST, PS, XB, GG}` excluded). Adjacent to the deferred Stash-audit bundle (status-contextual sub-statuses, rating-as-data, Collections, Series/Developer/Publisher reference entities, Session lifecycle).
- **R-series remap durability + Q-series pending-review queue** — designed but deferred during this session. R1+R2: syncRunner pre-checks UserGame by platform-side ID before IGDB resolution + remap endpoint folds source Game's platform IDs into target Game so user-manual remaps survive future syncs. Q-series adds `UserGame.needsReview` flag + Library callout for low-confidence title-search matches. Both well-scoped in the conversation; pick up when remap-class regressions resurface.
- **R4 reactive async DMs** — driven by TL1 telemetry signals (`docs/USER_RESEARCH.md` §6.5).
- **`/layers-user-needs`** — refine the 7 candidate job stories, productive after ~2 weeks of TL1 baseline (earliest re-trigger 2026-06-04).
- **F1-PR4 (freeform-fallback) un-deferral** — only if the GameDetail-edit workstream surfaces a real "I have a game IGDB doesn't have" cohort signal. Schema columns (`Game.igdbId` nullable + `Game.userOriginated`) are still on prod from the deferred attempt, so the schema work is half-done if revived.
- **Test-infra workstream (deferred candidate)** — pair `apps/api/.env.test` + Jest `setupFiles` (eliminates per-file `dotenv/config` mocks) with a single real-HTTP-level auth-chain smoke test.

**Previous workstream — Platform sync resolution overhaul (complete 2026-05-27).** Single sustained session across ~20 commits. Triggered by Luigi's PSN connect + Andrea's "the auto-promote isn't firing for Lego Batman" observation; expanded through diagnostic-driven iteration into a full overhaul of how sync resolves platform titles to IGDB games. No formal `*_PLAN.md` doc — decisions made inline through probe-driven diagnosis.

Sub-workstreams shipped in order:

1. **G-series — GOG library sync (#5.1 → #5.5).** OAuth (Galaxy desktop-client credentials via env-vars-only per Andrea 2026-05-27: "Lets do the approach correctly right away so we dont have to remember that in the future"), `ensureFreshGogCredentials` identity-equality refresh pattern (returns same object when token valid, new object when refreshed — caller persists only on identity change), `syncGogLibrary` paginated through `embed.gog.com/account/getFilteredProducts`, GogGuidedFlow 5-step paste-code UI mirroring PsnGuidedFlow with auto-extract of `code=` from full-URL pastes, `Game.gogAppId` threading through syncRunner (NOT `@unique` because HLTB lookups already populate it). Smoke-tested live against Andrea's real GOG library.

2. **Username capture (Steam/PSN/Xbox/GOG).** Four fail-silent fetchers (`getSteamUsername` via Steam Web `personaname`, `getPsnUsername` via psn-api `getProfileFromUserName('me')`, `getXboxGamertag` via OpenXBL `/account` + settings array, `getGogUsername` via `embed.gog.com/userData.json`). Wired into all 4 connect endpoints AND each sync branch as a backfill (re-reads platform row before write so GOG mid-sync refresh can't clobber username). Bonus fix: GOG refresh-persist now merges `{ ...creds, ...fresh }` instead of overwriting credentials with just the fresh OAuth fields. Surfaces as "signed in as X" in PlatformDetail + Settings.

3. **P-series — CM13 auto-promote fires on trophy/achievement evidence.** Triggered by Andrea: "I started playing lego batman that currently is in my wishlist, but it didnt moved away from the wishlist." Diagnosed: trophies API surfaces engagement near-instantly via PSN's `getUserTitles`, but `getUserPlayedGames` (playtime) lags 24-72h for new releases. CM13's `promoteWishlistOnOwnership` only listened to playtime — never the trophy signal. New helper `promoteWishlistOnEngagement(status, earned, percent)` in `apps/api/src/lib/achievements.ts`: returns `Completed` at 100% (folds in T-D2), `OnHold` for earned > 0 below 100%, null otherwise. Wired into `applyPsnTrophyAggregates` (PSN), `triggerSteamAchievementsBackground` (Steam — filter widened to include Wishlist UserGames with `steamAppId`), and `applyXboxPlaytimeBackground` (re-evaluates Wishlist when side-pass surfaces real minutes post-syncRunner).

4. **P-FIX — P2002 recovery on `psnNpCommunicationId` + platform-key backfill on trophy/achievement match.** P5's improved trophy-fetch error log surfaced the failure: "Unique constraint failed on the fields: (`psnNpCommunicationId`)". Cross-region duplicate trophy titles share npCommunicationId; previously aborted the whole trophy loop. Catch + skip the npId persistence (trophy data still writes). Also: trophy/achievement aggregation now backfills `playtimeByPlatform.PS = 0` (or `.ST = 0`) when missing, so Library platform filter + Cover platform tag (which reads `entries[0]` of playtimeByPlatform) work for games whose only engagement signal is trophies.

5. **Diagnostic instrumentation (3 rounds).** Activity log progressively extended: (a) `library: N imported, K skipped (titles: …, +M more)` — surfaces which titles fall out of sync; (b) `(errored: "X" → <error reason>)` — captures per-title error message in the `details` JSON; (c) `(skipped: "X" [psnConceptId=N])` — captures platform-side IDs for skipped titles so we can distinguish "psn-api didn't return concept" (Sony-side gap) vs "concept returned but IGDB filtering missed" (IGDB-side gap). All three landed; all visible in production activity log per platform. Trophy-fetch P5 also shipped: drops the swallowed `trophies.failed` warn for the actual exception message.

6. **L-series → L-FIX — IGDB game_localizations tertiary fallback.** First L-series query used `search "..."` on `game_localizations`. Probe diagnostics revealed IGDB rejects search on this endpoint (400: "Searchable endpoints: Characters, Collections, Games, Platforms, Themes"). L-FIX swapped to `where name ~ *"..."*` wildcard substring. Coverage is sparse (IGDB's localization table is community-maintained), but kept as the tertiary fallback after platform-id and title search.

7. **N-series + N-FIX — Sony concept ID / Xbox titleId / GOG appId via IGDB external_games (URL-pattern matched, not category-numbered).** The headline architectural win. New `Game.psnConceptId Int? @unique` (migration `20260527140000_game_psn_concept_id` applied via Prisma Client `$executeRaw` script — `scripts/apply-psn-concept-id-migration.ts`). `psn.ts` captures `concept.id` from psn-api's `titles[].concept`. New IGDB helpers `getGameByPsnConceptId` / `getGameByXboxTitleId` / `getGameByGogAppId` all wrap a shared `getGameByExternalUid(urlPattern, uid)` core. syncRunner resolution order per game: Steam-id → PSN-id → Xbox-id → GOG-id → title search → localization fallback. **N-FIX:** original N-series filtered IGDB external_games by `category = 36` (PSN) / `1` (Steam) / `5` (GOG) / `(31, 54)` (Xbox) but Andrea's probe revealed IGDB is migrating from the `category` enum to a newer `external_game_source` schema — many rows (including LEGO Batman's PSN row, uid=10008537) have `category` set to NULL, so `category = 36` returned `[]` even though the row existed. Switched to URL-pattern: `store.playstation.com` for PSN, `store.steampowered.com` for Steam, `gog.com` for GOG, `microsoft.com` for Xbox. URL prefixes are stable across IGDB's migration and survive both old + new rows.

**End-state metrics (Andrea's live PSN sync, 2026-05-27 15:36):**
- `library: 147 imported, 0 skipped` (previously `138 imported, 9 skipped`).
- `trophies: 124 matched, 0 auto-completed, 55 missed` (previously `118 matched, 61 missed` — improvement because newly-resolved Games now have their `psnNpCommunicationId` persisted on first sync, so primary-match path picks them up next sync).
- Lego Batman: real **17h playtime**, status **OnHold**, trophies **16/52 (31%)**, cover shows **PS**, all Italian-localized titles now resolve to canonical English IGDB titles.
- All previously-skipped Italian titles (Mafia: Terra Madre, LEGO Star Wars: La saga degli Skywalker, MATRIX il risveglio, Sapere è Potere, UNCHARTED Raccolta, Kingdom Hearts ReMIX, Dimmi Chi Sei!) resolve via N-series Sony concept ID path.

**Test counts at workstream close:** 477+ backend tests passing across affected suites; typecheck + lint clean. Pre-existing GOOGLE_CLIENT_ID env-dependent flake in `auth.test.ts:262` untouched (documented Known Gap).

**Sticky property worth knowing for future contributors:** IGDB's `category` enum on `external_games` is being deprecated in favour of the newer `external_game_source` field. Don't rely on `category = N` filters — they return `[]` for migrated rows even when the row exists. Use URL-pattern matching (`url ~ *"<storefront-domain>"*`) for any future IGDB external_games lookups. Same pattern works across PSN / Steam / GOG / Xbox. Confirmed 2026-05-27 via `scripts/probe-igdb-external-games.ts` against IGDB game id 361855.

**Sticky property #2 — trophy data is faster than playtime data on PSN.** For any future feature that wants to detect engagement on PSN, use `getUserTitles` (trophy API) signals before `getUserPlayedGames` (playtime API). The first surfaces near-realtime; the second lags 24-72h on new releases. Same applies to Steam achievements vs `playtime_forever`.

**Operational gotcha added 2026-05-27:** IGDB rate-limiting on cold cache — N-series adds one external_games call per game, so a 138-game library makes 138 IGDB calls in a single sync. First sync after deploy hits IGDB's 4 req/s wall and ~30% of calls 429. They're caught + return null → falls through to title search → games either resolve via fallback (counted as "imported via title path") or fail (counted as "errored"). On the second sync the per-concept cache (24h TTL) is warm → calls drop dramatically → errors → 0. Cache warming is the de-facto throttle. If we ever scale to multiple users syncing simultaneously, may need explicit token-bucket throttling on the IGDB client.

**Earlier workstream — F1 manual-add — full sequence PR1 → PR6 (complete 2026-05-24).** Plan at `docs/INTERACTION_FLOW.md` F1 (6-PR sequence locked 2026-05-22 with full Layers-framework deliverables — problem-space §1–5, breadboard §6, Mermaid diagram, OQ-F1-1 → OQ-F1-10 resolutions). **F1-PR1 (2026-05-22)** — four commits ending `0acebb9`: status-first P2 layout, manufacturer-bucketed 50-option PlatformPicker (PC / PlayStation / Xbox / Nintendo / Sega / Other), IGDB-suggested platforms (OQ-F1-9), entry-intent threading, P5 pattern (b) summary with auto-close timer (3s → 10s → 15s after smoke tests), platform pin across `[+ add another]` (S7 collector-batch UX), `[pick different]` rename per OQ-F1-10. **F1-PR2 schema + backend (`3cec709` + `2dfd0af` + `1b56097`, 2026-05-22)** — new UserGame columns (`mediaType` / `condition` / `region` / `wishlistedPlatforms`); CM13 wishlist auto-promotion in syncRunner; Wishlist shelf + count widened via OR predicate so per-platform-wishlist entries (GTA-class) appear; migration applied via Prisma Client `$executeRaw` fallback after `prisma migrate resolve` hung on pgbouncer. **F1-PR2 frontend (3 commits 2026-05-23):** `5baca70` modal collector chip strips (mediaType always, condition/region only on PHYSICAL, switching off hides AND clears stale values); `155b461` GameDetail OWNED ON → PLATFORMS rename + new `buildPlatformRows()` helper merging owned + wishlist-only entries (ownership wins on dedupe) + amber `wishlisted` marker; `35e8199` per-platform un-wishlist endpoint (`DELETE /api/games/:id/wishlist-platforms/:code`, idempotent + user-scoped) + GameDetail `[× un-wishlist]` affordance + cache invalidation. **OQ-S-13 reversal locked in PR2:** Releases-page wishlist toggle does NOT populate `wishlistedPlatforms` — collector opt-in only. **F1-PR3 (3 commits 2026-05-23):** `00bdcf7` backend adds `manualPlaytimeMinutes` to `POST /api/games/manual` (whole-minute int [0, 600000] / 10000h ceiling, first-write-only on update path so synced playtime survives manual re-add); `cb504cf` modal collapsible `[+ more details ▼]` panel (Stash add+ pattern) with hours+minutes inputs (empty → omitted from body, either-filled treats other as 0) + times-beaten architectural placeholder ("// coming soon · v2") — closes S7 "playtime: —" downgrade; `c31d262` scroll fix: P2 block capped at `calc(80vh - 160px)` with `thin-scroll` so the panel toggle is reachable on shorter viewports. **F1-PR4 DEFERRED 2026-05-23** — freeform-fallback (G7) marked "extremely marginal" after a 400 surfaced on the live smoke test. 4 commits hard-reset to `ba58b4b`; schema columns (`Game.igdbId` nullable + `Game.userOriginated`) stay on prod as benign dead weight, providing a half-done foundation if freeform is ever revisited. **F1-PR5 (3 commits 2026-05-24):** `30b4725` extracted `promoteWishlistOnOwnership` helper into `apps/api/src/lib/promoteWishlist.ts` + refactored syncRunner to consume it (pure refactor — locks CM13 policy in one place so manual-add and sync paths can't drift); `5902230` replaced naive overwrite-on-update upsert with the full 6-row CM12 + CM13 conflict matrix on `POST /api/games/manual` (no row → create, P-in-playtime → no-op write, P-not-in-playtime → merge, existing=Wishlist + new=owned → CM13 auto-promote overriding user's status pick, existing≠Wishlist + new=Wishlist → no-op on status). Returns full `UserGameDetail` shape (was a flat payload) so the modal can read `userGameId` from `response.id`. 200 on merge, 201 on create. Closes G5 (silent merge instead of 409 on `@@unique([userId, gameId])`). `2fe250f` OQ-F1-5 closeout: wishlist + create path now wraps `userGame.create` + `wishlistRelease.create` in `$transaction` after best-effort `getReleaseDetails(igdbId)` (graceful degradation on null/throw — UserGame still created, just no Releases page surface). **F1-PR6 (`e49b389`, 2026-05-24):** P5 deep-links — `[view game]` → `/game/{userGameId}` and `[+ rate / note]` → `/game/{userGameId}?focus=notes` (reuses post-8 PR A focus-on-notes pattern per OQ-F1-7 — auto-opens notes editor on land). Both cancel the auto-close timer + call `onClose()` before `navigate()`. Gated on `payload.userGameId !== null` as defensive fallback. Test harness now wraps `render()` in `MemoryRouter` + `LocationProbe` so deep-link assertions read the post-navigate URL via `getByTestId('location')`. **CM12 + CM13 + simplified MediaType (DIGITAL/PHYSICAL only)** ratified in `docs/CONCEPTUAL_MODEL.md` before backend ship; design rationale at `docs/Design_decision_log.md`. **Workstream-close test counts:** 32 backend suites / 363 tests pass; 58/58 across the directly-affected web surface (AddGameModal + buildPlatformRows + api-invalidation + RemapGameModal); typecheck + lint clean. **No AGENT.md entries** — F1's decisions constrain F1-series specifically (CM12 + CM13 + simplified-MediaType are CONCEPTUAL_MODEL-layer; per-row helpers + Zod refinements are tactical). One key sticky property worth knowing for future GameDetail-edit work: F1-PR5's `addManualGame` returns `UserGameDetail` on both 200 and 201, so any future modal/flow that consumes the response can read the full game state without a refetch.

**Earlier workstream — L1 event-log telemetry (TL-series, complete 2026-05-21).** Plan at `docs/TELEMETRY_PLAN.md`. R3 in `docs/USER_RESEARCH.md` §6.5; promoted from R4-deferred to R3-active per D10 (no calendar chats — telemetry replaces scheduled JTBD chats as the primary behavioural-gap instrument). **5-commit single-PR workstream, all shipped 2026-05-21:** **TL1.1** schema + migration + types + mapper (3 mapper tests; migration applied to Supabase out-of-band via the documented `prisma db execute` + manual `_prisma_migrations` INSERT recipe after `migrate resolve` hung — see operational gotcha below). **TL1.2** `logEvent()` helper + 8 server-side write hooks (`session.opened` w/ daily throttle, `signup.pending` x3 providers, `signup.completed`, `platform.connected` x3 platforms, `sync.first` w/ pre-update `wasFirstSync` capture, `remap.used` both branches, `wishlist.toggled` star+un-star, `error.surfaced` w/ truncated message + requestId) + 12 tests (6 helper unit + 6 sampled integration: wishlist add/remove + sync.first first-vs-resync + error.surfaced positive/anonymous-skip). **TL1.3** `GET /api/admin/events` cursor-paginated route w/ optional `?userId=` + `?event=` filters + 6 API tests (admin-only 404, cursor stability + ordering, both filters, empty shape, same-timestamp tiebreaker). **TL1.4** `useAdminEvents` SWR hook mirroring `useAdminFeedback` exactly + EVENTS section at the bottom of `/admin` (after INVITE CODES per TL-D7) + 6 web tests. **TL1.5** doc closeouts (this commit). **27 new tests against ~16 target** — overshoot is from the 6 web tests being mandatory (vs ~5 budgeted) + the same-timestamp belt-and-suspenders test + helper coverage that pulled its weight. Every scope-edge guard respected: no client-side `POST /api/events` (TL-D8 held), events immutable with no PATCH/DELETE (TL-D10 held), no filter/sort UI in the admin section (TL-D6 URL-only filters held), no generic `useAdminList<T>` abstraction (three call sites still below the threshold). **F-D1 cascade decision parallel** holds: TL-D1 cascades UserEvent with User on delete for the same GDPR-shaped reasoning. No `AGENT.md` entry — TL1's decisions constrain TL-series specifically, not future work. Two operational issues surfaced during TL1.2 and now documented in both the operational-gotchas list (CLAUDE.md, with full guidance) and Recent Fixes below (one-liner cross-references): dotenv-via-transitive-import trap (any middleware/route test importing `@hoard/db` loads the prod `.env`), Express 4 async-handler-rejection plumbing (rejections don't reach `app.use((err, ...))` without `express-async-errors`, fixed via direct middleware invocation in tests). **TL1.1 migration apply hit a new pgbouncer quirk:** `prisma migrate resolve` hung after writing the `_prisma_migrations` row but before printing the completion line; reliable fallback recipe (one-off Node script with `prisma.$executeRaw\`INSERT INTO _prisma_migrations ... ON CONFLICT DO NOTHING\``) added to the operational-gotcha guidance. Channel is live on production: `UserEvent` table populated by every authed request (session.opened daily-throttled) and by every touchpoint event going forward; `/admin` EVENTS section reads the feed via cursor pagination. Deferred items in plan §4 (8 items): client-side `POST /api/events` for `library.first_open` + `releases.scope_changed` (closed-set enum constraint locked for when that lands), aggregated analytics views, CSV/JSON export, real-time SSE stream, retention policy, push notifications, per-event-class admin filter UI (URL query string already supports it from day 1; only the UI affordance deferred).

**Previous workstream — User-research observation channel + Feedback channel (F-series, complete 2026-05-13).** Triggered by `/layers-orient` → `/layers-observed-behaviour` session 2026-05-12, after the bottleneck analysis identified observed-behaviour as the lowest layer with thin state (N=1 Andrea-only to N=6 friendly-fire cohort happened in 5 days; "Luigi tells Andrea on chat → Andrea writes a commit" was the only observation channel). New canonical doc `docs/USER_RESEARCH.md` captures the existing observed-behaviour corpus (11 observations + 5 patterns + 7 candidate job stories with Andrea-only flags + 8 research gaps) and designs a 3-layer channel: **L1 telemetry / L2 in-app form / L3 scheduled chats**. L2 became the F-series engineering workstream (plan at `docs/FEEDBACK_PLAN.md`). **5-commit single-PR workstream, all shipped 2026-05-13:** **F1.1** schema + migration + types + mapper (4 mapper tests; migration applied to Supabase via the documented `prisma db execute` + `prisma migrate resolve` recipe). **F1.2** backend routes (`POST /api/feedback` + `GET /api/admin/feedback` + `PATCH /api/admin/feedback/:id`) with two-tier per-user rate limit (10/h + 20/d, prod-only via `skipInDev`) + 9 API tests, plus an in-scope bonus fix for a latent router-middleware-without-prefix bug in `admin.ts` (full pattern note in Recent Fixes below). **F1.3** `FeedbackForm` component with 5-state machine (idle / expanded / sending / sent / error) + Settings → About section (new `SettingsSection` between Data Export and Danger Zone) + `api.feedback.submit()` that invalidates ONLY `admin:feedback` (narrower than I-series `admin:`) + 6 web tests. **F1.4** `useAdminFeedback` SWR hook + admin `FEEDBACK` section between PENDING REQUESTS and ALL USERS + unread chip on the header + `[mark read]/[mark unread]` button with `stopPropagation` guard so clicking the action doesn't toggle the row's expand state + [load more] cursor pagination + 6 web tests. **F1.5** doc closeouts (this commit). **22 new tests against ~22 target**, every scope-edge guard ("if you hit 9, something grew") respected. **F-D1 cascade decision** (FK `onDelete: Cascade` on `Feedback.userId`) is the only architectural-shaped call — but it constrains F-series specifically (GDPR-shaped "user deletes account, their data goes" + don't retain `userEmailAtSubmit` PII), not future work. No `AGENT.md` entry per Andrea 2026-05-13: "AGENT.md is for decisions that constrain future work; F1's decisions constrain F-series specifically." Deferred items in plan §4 (10 items total): push notification channel, sidebar global affordance, mobile tab-bar feedback access, workflow states beyond `read` boolean (promotion path: add `processedAt: DateTime?`), user-side edit/delete, reply threads, email digest, link-to-USER_RESEARCH, CSV/JSON export, server-side `unreadOnly` filter, cursor-aware optimistic reconciliation on mark-read.

**Previous workstream — E2E suite restoration E1 + E2 (E-series, complete 2026-05-21).** E2 (`welcome.integration.spec.ts` covering the I4 closed-beta welcome flow) shipped 2026-05-21 as a 4-commit chain on top of E1: `cb1876a` (docs §E2 expansion with PR-shaped detail, D4 component-spec drop, fixtures decision rule), `1ce96ec` (seed-e2e.ts +5 InviteCode rows mapped to test consumers, code-3 pre-redeemed), `6ef2e0f` (welcome spec + globalSetup ghost-purge + playwright.config wiring), `dbaced4` (quarantine test 5 + fixture cleanup defense from investigation). Plus Flag-A fix-up `1c4d2d9` — project-level `testIgnore: ['welcome.integration.spec.ts']` in the mobile project replaces the original runtime `testInfo.skip` pattern, eliminating the `6 skipped` noise in test output. **End state: 5/6 welcome tests passing (1-4 + 6).** Tests 3 and 4 explicitly click `$ I have a code →` first so the input renders via `inlinePanel === 'code'`; test 5 was the only test relying on `requestSent` (derived from `user.hasRequestedAccess`) for input visibility after a fresh `page.goto`. **Test 5 (friction-free flow: request-access → admin generates → redeem) is quarantined under #6** pending diagnosis of a UserProvider `loading → authed` mount race — at failure-time the page snapshot shows the BASE welcome state (no input rendered) and the DB confirms the admin-generated invite code stays at `usedById = NULL`, meaning the redeem POST never reached the server. **Server-level flow verified working** via direct `page.request.get('/api/auth/me')` calls at both step 1 and step 3 of the failing test, returning 200 + `hasRequestedAccess: true` with byte-identical session cookies between the two points; manual product use is also confirmed (the live `/welcome` flow has been used to onboard 3 beta users). Failure is in Playwright observation timing during the mount transition, not in product correctness. **Diagnostic journey** (recorded in issue #6 as forward-pointer): five rounds of investigation produced (1) the cleanup-cascade fix where afterEach now always re-logs in as tracked email before `DELETE /api/auth/me` (belt-and-suspenders defense, kept after refactor), (2) explicit status+body capture on admin endpoint failures that surfaced a Zod 100-char `note` cap, (3) two-isolated-`browser.newContext()` structural refactor (didn't resolve the symptom), (4) the 4-datapoint diagnostic matrix that conclusively ruled out cookie collision / server-side state divergence / SWR cache / missing credentials, and finally (5) the (g) quarantine + diagnostic-trail-as-issue path when structural rework didn't land the fix. **§E2.4 verification — all 5 checks green:** full-suite shape 41 passed / 1 skipped / 2 pre-existing Dashboard flakes (Check 1e); ghost-purge fires on >1h, spares <1h (Check 2 — `[global-setup] ghost-purge: deleted 1 stale e2e-welcome-* users`); 0 ghost accumulation after 2 back-to-back welcome runs (Check 3); globalSetup fail-loud with `PrismaClientInitializationError` + named-paths trace on bad `DATABASE_URL_TEST` (Check 4); InviteCode pool intact — codes 1+2 SetNull-restored with `usedAt` timestamps preserved, code 3 unchanged from seed, codes 4+5 untouched, zero admin-generated stragglers because test 5 is skipped (Check 5). The Dashboard flakes are the E1-era pre-existing flake noted in `docs/PLAN.md`; they did not recur in any single-test or welcome-only run during Checks 2-4, only in full-suite Check 1e. Plan reference: `docs/E2E_RESTORATION_PLAN.md` §6 step 5.

**Earlier workstream — E2E suite restoration E1 (E-series, complete 2026-05-11).** Plan in `docs/E2E_RESTORATION_PLAN.md`. Track 2 work, parallel with closed Track 1 admin-polish A-series. Triggered by I-series I4 deferral; the prior E2E suite had been silently broken since I1's seed-andrea pre-step deletion — all 38 `screens.spec.ts` tests timing out, `a11y.spec.ts` reporting 12 false positives because axe-core was scanning the `/login` redirect for every authed route. Strategy locked 2026-05-09 (Option F hybrid B+E) with five Andrea-led refinements (~30/70 mocked/real-DB ratio, data-parity-≠-schema-parity acknowledgment, a11y false-positive fix folded into E1 via global auth fixture, snapshot regen made explicit deliverable, naming convention by isolation level). **9-commit chain:** 5 planned (`f40ca30` infra: test-DB keepalive Action + migrate-status CI gate + `.env.test.example`; `440b76c` seed-e2e.ts + Playwright config wiring `DATABASE_URL_TEST`; `b088771` global auth fixture with per-test `expectedUrl` declaration + spec rename + reclassification verdicts moving 6 tests to vitest; `76512ec` regenerated visual snapshot baselines; `1b61c36` contributor README) + 4 unplanned discoveries (`edc19bb` ESM compat for `__dirname` in playwright.config.ts since `apps/web` is `"type":"module"`; `334a159` `.env.test` parser generalized after adding `E2E_TEST_PASSWORD` to the file; `919a1ab` schema-drift migration for `Game.steamAppId` — prod had the column via undocumented `prisma db push` during the Steam backfill, no migration file recorded it, verified via psql diff between prod and test as single-column drift; `cffdacb` `pool:'threads'` for vitest + `workers:1` for Playwright after parallel-worker contention caused intermittent dashboard-test flakes). Plus two Phase C keepalive-workflow fixes (`6a80842` create `infra:test-db` label inline, `33bb5ac` pass `--repo` explicitly to all gh commands — the workflow doesn't checkout the repo so gh had no git context to infer the target). **Phase C verification:** step 4 (missing-`expectedUrl` guard fires with named-paths error pointing at the spec file by path) ✅; step 5 LOAD-BEARING (corrupting `RequireAuth` to bounce every authed route produces 12/14 a11y failures with explicit `Expected: "/library" Received: "/login?next=%2Flibrary"` errors instead of the pre-E1 false-positive "14/14 PASS" — the property the whole workstream was built for) ✅; step 6 snapshot stability deterministic across two consecutive runs ✅; step 7 (next-day cron confirmation) Andrea's morning verification. End state: `screens.integration.spec.ts` 24/24 + `a11y.integration.spec.ts` 14/14 = 38/38 passing serial.

**Sticky reference — E1 schema-drift gotcha (for the next contributor):** If you provision a fresh hoard-test or any other replica of prod's schema and a test fails with `P2022: column ... does not exist`, the cause is almost certainly the same class of drift — someone ran `prisma db push` (or raw `ALTER TABLE` in Supabase SQL editor) to land a column on prod without writing a migration file. Fix recipe: `prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel packages/db/prisma/schema.prisma --script` between prod and the failing DB shows the exact `ALTER TABLE` SQL needed; wrap each statement in `IF NOT EXISTS` for prod idempotency; apply via the `prisma db execute` + `prisma migrate resolve --applied` recipe in `CLAUDE.md` operational gotchas. The post-E1 CI gate flagged in `docs/PLAN.md` would catch this class going forward.

**Previous workstream — Admin polish (A-series, complete 2026-05-10).** Track 1; plan + lifecycle in `docs/ADMIN_POLISH_PLAN.md`. Single A1 PR with 5 planned commits + 4 follow-up commits surfaced during live-page eyeballing. Decisions A-D1 through A-D12 locked in the plan; architectural ones mirrored in `AGENT.md` decision #39. End state: `/admin` page now has row density redesign (4-col merged identity, 28 px row height), sort + filter + search with URL state (`?filter=&sort=&q=`), `[delete]` user flow with typed-confirmation (`displayIdentity` keyword) + FK cascade + self-protection at both layers + natural JWT-401 invalidation property documented. `ConfirmModal` promoted from inline-in-SettingsDesktop to `apps/web/src/components/modals/ConfirmModal.tsx` with new `'delete-user'` variant. `InviteCode.usedById` FK behavior made explicit (`onDelete: SetNull`) at the schema layer (matches existing SQL — zero migration). Final test count: **284 API + 304 web pass** (+6 API new for the delete endpoint + counts; +35 web new across modal regression + AdminScreen filter/sort/search/delete-flow). Deferred items in plan §4: 500-on-deleted-user-edge-case (3 routes), platform-summary chip-row when 4+ codes appear, OAuth `?next=` round-trip preservation, web full-suite vitest hang, and (added in commit 5) global typography audit across the rest of the codebase.

**Earlier workstream — Closed-beta invite codes (I-series, complete 2026-05-10).** Plan + lifecycle in `docs/INVITE_CODES_PLAN.md` (spec at `docs/Hoard_invite_codes.md`). Six PRs (I1 → I6) plus three smoke-test-driven follow-ups plus three I5 review follow-ups. Decisions I-D1 through I-D15 locked in the plan; architectural ones mirrored in `AGENT.md` decisions #34–#38. End state: closed-beta gate live in production; new signups land in `PENDING_INVITE` and hit the welcome screen; ACTIVE users (Andrea/Luigi/secondary test) unaffected; admin panel at `/admin` desktop-only with sidebar entry conditional on `isAdmin`; redeem + request-access endpoints + race-safe atomic redemption with two-tier rate limit; deep-link preservation via `?next=` URL channel works end-to-end (email/password verified; OAuth path documented as deferred follow-up in plan §4); LoginScreen + RequireAuth + RequireActive + WelcomeScreen + AdminScreen all shipped with regression tests. Final test count at workstream close: 274 API + 265 web pass.

**Sticky enforcement (always applies, no expiry):**

> ⚠️ **D1 — Releases page rename scope.** The page rename is **URL + UI labels ONLY**. `useUpcoming`, `IgdbUpcomingRelease`, `WishlistRelease`, `/api/igdb/upcoming`, `/api/upcoming/:igdbId/wishlist`, table names — **all stay**. Don't search-and-replace "upcoming" across the codebase. The `scripts/check-rename-rule.ts` CI guard catches violations; running `npm run check:rename-rule` locally is faster than waiting on CI. See `docs/RELEASES_PLAN.md` §1 + §7 for the full rename matrix and rationale; D1 is mirrored as decision #23 in `AGENT.md`.

---

## Previous workstream — Releases page rework (complete 2026-05-07)

Source materials: `docs/RELEASES_PLAN.md` (locked decisions D1–D7 + PR sequence), `docs/Hoard_releases_handoff.md` (canonical spec, standalone), `project/Hoard_releases_mocks.html` (visual mocks — packed bundler artifact, open in browser to render; see RELEASES_PLAN.md §0 for source-extraction recipe), `docs/Upcoming/Hoard_design_feedback_rev03.md` … `rev07.md` (design conversation).

**Page concept:** two modes (Wishlist / All), two zoom levels (Months / Quarters), separate `/releases/recent` 14-day-window surface reached via a conditional banner. Mobile uses a different IA than desktop — view-sheet pattern, see handoff §7.

**Six PRs (R1–R6) all done 2026-05-07:**
- **R1** — New `GET /api/releases/recent` returns `{ starred, hyped }` (D7 unified shape). Library-membership filter + igdbId dedupe + 14-day window + `hypes >= 80` muted-banner threshold. Graceful degradation when IGDB throws. New `getRecentlyReleased()` helper. 7 integration tests.
- **R2** — All 5 desktop primitives under `apps/web/src/components/screens/releases/`: `ReleaseCard` (3 variants), `HeroCountdown` (only `[on wishlist]` per D6), `RecentBanner` (2 variants per §4), `TimeNav` (bars + counts + zoom + TBA hatched), `AgendaRail`. Shared `utils.ts` (`toPlatCode`, `hypeToBars`, `releaseDateColumn`, `categoryLabel`). 39 Vitest tests including explicit assertions that `[i got it]` and `[mark all owned]` are absent everywhere (mock-vs-handoff drift guard).
- **R3** — New `ReleasesDesktop` composing R2 primitives with full URL state (`?mode=&scope=&zoom=&bucket=`), bucketing logic per D4 (6 month buckets / 3-quarter+TBA), right-rail conditional, empty states with `[skip ahead]`. `/upcoming` → `/releases` redirect, sidebar/tab-bar relabeled, `UpcomingDesktop` deleted. 31 bucketing tests.
- **R4** — `ReleasesRecentDesktop` in its own file. Two-section layout per handoff §10: `// just out · starred · N` and `// also released · not on your wishlist · N`. Green informational prompt strip; empty state + retry path. 5 smoke tests.
- **R5** — Full mobile shell rewrite. New primitives `MobileViewHeader` (chevron stepping per §8 + §12 punch-list 10), `MobileViewSheet` (single mobile surface for mode/scope/zoom/bucket per §7), `MobileBanner` (compact variants per §12 punch-list 3+4), `MobileReleaseRow` (40/36/1fr/auto grid per §12 punch-list 2). New `ReleasesMobile` + `ReleasesRecentMobile` page components. `UpcomingMobile.tsx` deleted. 20 tests.
- **R6** — Polish + closeouts. New shared `WishlistEmptyRecommendation` component closes the handoff §11 wishlist-empty gap on both desktop + mobile (top-3 hype-sorted picks with `+ wishlist` quick-add). 4 unit tests. axe-core `a11y.spec.ts` updated for the new `/releases` + `/releases/recent` routes (8 routes × 2 viewports). `screens.spec.ts` rewritten with structural assertions that survive IGDB feed drift; new redirect test for `/upcoming` → `/releases`. Stale `upcoming-{desktop,mobile}.png` snapshots removed; `releases-{desktop,mobile}.png` baselines need a one-time `npm run test:e2e:update -w apps/web` run on the user's machine. Drift-guard sweep clean — no `_retired_` artboards, no `[i got it]` / `[mark all owned]` rendered, no `UpcomingDesktop` / `UpcomingMobile` references remain. 136 API + 168 web tests pass.

**Result:** `/upcoming` → `/releases` redirect live; new page concept (modes / zooms / bucket-driven time strip / conditional banner / separate RECENT surface / mobile view-sheet IA) shipped end-to-end.

---

**Previous workstream — Post-8 Interaction Debt complete 2026-05-06.** Production is feature-aligned across desktop and mobile, has full HLTB-or-equivalent coverage where the data exists, honest Upcoming scope semantics, and proper destructive-action confirmation. 129 API + 69 web tests pass. The four PRs:
- **Hot fixes** (commit `6c624a0`) — PSN status logic + Steam connect 404 + Library single-shelf filter+sort.
- **PR D** — HLTB layered fallback (coverage 34.2% → 63.1%).
- **PR A** — 9 interaction-debt items.
- **PR B** — Real wishlist scope + Path-B persistence fix.
- **PR C** — Sync-all + wipe-library.

See `docs/INTERACTION_DEBT_PLAN.md` for details.

---

**Previous workstream — Phase 8 (complete 2026-05-05).**

Drafted from a May 2026 multi-pass UX audit covering parity, Nielsen heuristics, iOS HIG, typography, touch targets, and full accessibility.

**Audit headline findings (the things being fixed):**
- ~290 uses of 9–12 px text; no `--text-*` token scale existed
- Zero `:focus-visible` styles anywhere; ~50 `<div onClick>` without keyboard support
- Zero semantic `<h1>` / `<h2>` / `<h3>` across the app
- Mobile shell rendered a fake hardcoded "9:41 / 100 %" status bar over the real iOS one
- Mobile tab bar grid was `repeat(5, 1fr)` for 4 tabs
- No `viewport-fit=cover` / `env(safe-area-inset-*)` / `100dvh`
- Mobile components don't read `usePreferences()` and don't call `update()` — ~50 % of desktop interactivity missing on mobile equivalents

**Phase 8 progress:**

- **PR 1 — Foundation: done** (commit `9d3ab31`). 11-step typography scale (`--text-3xs` → `--text-display`) + 4-step line-height scale; global `:focus-visible` amber ring + `prefers-reduced-motion` block + skip-link CSS; chip/btn/field heights raised; `--paper-faint` text under 17 px replaced with `--paper-dim` (passes WCAG AA). Inline `fontSize` literal sweep across 24 components — sub-floor sizes (7/8/9 px) bumped, 16/18 collapsed to `--text-md`. Bonus: `GameDetailDesktop` quick-stats + HLTB grids restructured so labels anchor to cell bottoms instead of clipping into the value row.

- **PR 2 — Mobile shell (iOS HIG): done** (commit `b31fa5d`). Fake status bar deleted; mobile tab bar fixed (`repeat(4, 1fr)` grid, `padding-bottom: env(safe-area-inset-bottom)`, 2 px amber top-border active state with `margin-top: -2px` so the row doesn't shift, `:active` press feedback, `navigator.vibrate?.(8)` haptic tick); `viewport-fit=cover` + `100dvh` + `padding-top: env(safe-area-inset-top)`; `MobileHeader` search icon wired to `SearchOverlay`; tab-bar icons replaced with meaningful glyphs (`home` / `rows` / `clock` / `user`). Bonus: `SearchOverlay` state lifted out of `TopBar` into a new `SearchModalProvider` mounted at `AppShell`; `MobileTabBar` rewritten as semantic `<nav><button>` with `aria-current="page"` (head start on PR 3 a11y).

- **PR 3 — Accessibility (WCAG 2.1 AA): done** (commits `3ce97d2` → `52fb4aa`, 6 sub-commits, 2026-05-05). The heaviest PR; landed in 6 parts so each was independently green:
  - **Part 1** (`3ce97d2`): `eslint-plugin-jsx-a11y` (90 errors → 0), ~50 `<div onClick>` converted to `<button>`, form labels, `role="dialog"` + Escape on modals, `useDocumentTitle` hook + per-screen calls, skip-link CSS + `<main id="main-content">` landmarks, Toggle/Radio/Chip rewritten with proper ARIA roles, tab strips with `role="tablist"`, Cover.tsx alt-text strategy, Lighthouse a11y threshold 90 → 95.
  - **Part 2** (`7756ed4`): semantic `<h1>` (TopBar last crumb sr-only, MobileHeader title visible, LoginScreen wordmark, PsnGuidedFlowDesktop hero, DeleteModal `<h2 id>`); `useFocusTrap` hook applied to `AddGameModal` / `SearchOverlay` / `DeleteModal`; `aria-live` regions for save toasts + login error; `@axe-core/playwright` + `tests/e2e/a11y.spec.ts` covering 6 routes × 2 viewports.
  - **Part 3** (`d37b742`): remapped `.t-faint` utility class from `--paper-faint` to `--paper-dim` to fix all 90+ color-contrast violations app-wide in one CSS change; mass-replaced ~15 inline `var(--paper-faint)` text colors with `var(--paper-dim)`.
  - **Part 4** (`244c9ac`): `tabIndex={0}` + `role="region"` on Dashboard mobile scroll container (axe `scrollable-region-focusable`); `aria-label` support added to `Btn` primitive + applied to icon-only "+ add game" button on Library mobile.
  - **Part 5** (`f3193de`): Playwright `webServer` made into an array that auto-starts both `dev:api` and `dev:web` (cwd rewired to monorepo root) — eliminates the silent corrupt-snapshots failure mode where login redirects were captured because the API wasn't running.
  - **Part 6** (`0977cef`): API rate limiter (`globalLimiter` + `authLimiter`) gains `skip: () => NODE_ENV !== 'production'`. Production rate limiting still in force; dev / Playwright runs no longer trip the 100 req/min/IP threshold mid-suite.
  - **End state:** axe-core 12/12 passing across Dashboard / Library / Upcoming / GameDetail / Settings / Login on desktop + mobile. Lint 0 errors. 115 API + 69 web unit tests pass. Visual snapshots regenerated against the working config.

- **PR 4 — Mobile parity: done** (commit `15695fc`, scope option A approved 2026-05-05). 15 ports + 2 documented skips:
  - **GameDetailMobile** is now a real editor: status bottom-sheet picker (with focus trap, role="dialog", safe-area-inset-bottom), tap-to-edit notes textarea inline in the receipt with blur-save + "// saved" toast, action buttons wired (`start` → status='Playing', `+ note` → focuses textarea, `share` → navigator.share with clipboard fallback), back caret uses `navigate(-1)`.
  - **DashboardMobile**: backlog picker + shuffle (closes the AGENT.md decision #4 violation), genre breakdown panel with proportional bars, now-playing card wrapped as a `<button>` that opens the game detail. (Decision: skipped porting the unwired desktop "resume / log session / +note" buttons — they're dead UI on desktop too.)
  - **LibraryMobile**: filter chip strip on the filtered single-shelf view (existed on the unfiltered shelves view), `usePreferences()` consumed for cover density (3 tiers: cozy 96×128 / standard 84×112 / dense 72×96), `MobileShelf` slot count adapts. View-mode toggle SKIPPED (mobile too narrow for useful list/grid distinction).
  - **UpcomingMobile**: scope state lifted; two-chip toggle (wishlist / all releases) above the month strip; DLC + remake category labels inline on agenda cards.
  - **PlatformDetailMobile**: sync-frequency picker (4 radios in a `role="radiogroup"`), full 4-row scope checklist (replaces the one-line summary). Activity log tab SKIPPED.
  - **SettingsMobile**: platform list rows show full name + game count + last sync ("488 games · synced 2h ago") instead of just code+who.

- **PR 5 — IA & polish: done** (commit `b3c4f8d`, 2026-05-05).
  - **Empty / first-run states** on Dashboard, Library, and Upcoming. Each shows an actionable CTA panel ("connect a platform" / "add a game" / "switch to all releases" / "tune hype threshold") instead of a bare blank screen.
  - **Retry buttons** on every data-fetching screen — `useDashboard` and `useGame` now expose `refetch`; `useUpcoming` already did. LibraryDesktop/Mobile already had retry from earlier; the rest now match.
  - **URL state**: Library `?sort=` + `?view=`, Upcoming `?scope=`. Default values omitted from URL to keep it clean. Sharing `/library/Backlog?sort=playtime` reproduces the exact view.
  - **Pull-to-refresh** on Dashboard / Library / Upcoming Mobile via new `usePullToRefresh` hook + `PullableScroll` primitive. Touch-only by design; visual indicator ("// pull down…" → "// release to refresh" → "// refreshing…"). Preserves the WCAG 2.1.1 keyboard-scroll fix.
  - **navigate(-1) audit complete** — remaining hardcoded `navigate('/path')` calls are all intentional fixed destinations (sidebar nav, settings sections, "back to platforms list" semantic). The one ambiguous case was already fixed in PR 4.
  - **Skipped by design**: quick-sync trigger in TopBar (defer to future feature), page-transition animations (terminal aesthetic is intentionally instant).

WCAG 2.1 AA is the bar (Hoard may be released as a product later, so accessibility is a hard prerequisite, not a personal-tool nicety). PR 3 met the bar; PR 5 closed the IA gaps that remained.

---

**Previous workstream — Performance & UX (complete 2026-05-04).**

The full per-PR breakdown (14 items, 6 PRs) is preserved in `docs/PERFORMANCE_PLAN.md`. Headline summary kept here for quick orientation:

- **PR 1 (F1+F2+F3)** — persistent shell: `AppShell` layout route + `UserProvider` context + `RequireAuth` lifted to layout. Sidebar/TopBar/MobileTabBar mount once per session. `api.me` fetched once per session (was 4–5× per navigation).
- **PR 2 (F4+F8+F9)** — stale-while-revalidate cache: `apps/web/src/lib/cache.ts` + `apps/web/src/hooks/useQuery.ts`. All four data hooks + Sidebar use it. Mutations centrally invalidate in `lib/api.ts`. `Cache-Control: private, max-age=10/30` + SW `StaleWhileRevalidate` for shell endpoints.
- **PR 3 (F5+F6)** — slim dashboard + per-shelf endpoint: `/api/dashboard` split into parallel queries with lightweight selects (backlog pool capped at 30); new `GET /api/games/shelves?perStatus=N`; `useShelves` hook; `/api/games` capped at 500.
- **PR 4 (F7+F11)** — covers + preconnect: `loading="lazy"`, `decoding="async"`, intrinsic dims; `igdbCover.ts` substitutes `t_cover_small` for ≤90 px targets (~6× mobile bandwidth saving); preconnect to `api.gamehoardr.com` + `images.igdb.com`. Skeleton rewritten to mirror real Library layout.
- **PR 5 (F10+F12+F13)** — composite indexes (applied to Supabase) + memoization + per-screen `React.lazy`. Initial JS bundle 105.68 → 75.38 KB gzipped (~30%).
- **PR 6 (F14)** — activity heatmap fed by real `lastPlayedAt` data (was synthetic). 24-week column-major grid; mobile slices the rightmost 16. Decision: this is "games last-touched per day," not "playtime per day" — sparse but real.
- **Infra discoveries** — see "Operational gotchas" below for the Railway region / `connection_limit` / Watch Paths fixes that brought the actual production wins.

Final state: **115 API + 69 web** tests passing. Lint clean, typecheck clean. Production endpoints in the 130–500 ms range. Commits `c11fc29` (PR 1–6 perf overhaul), `624a380` (Library error UI), `8e31e46` (ops gotchas docs).

Production URLs:
- Web: **https://gamehoardr.com** (Vercel)
- API: **https://api.gamehoardr.com** (Railway, EU West / Amsterdam)
- Database: Supabase, `aws-0-eu-west-1.pooler.supabase.com:6543` (transaction pooler) — single project shared with dev (Option A from Phase 7)
- Domain: gamehoardr.com via Porkbun (DNS at Porkbun)
- Fallback URLs (still active, for emergencies): `hoard-liard.vercel.app`, `hoardapi-production.up.railway.app`

**Operational gotchas (recorded after a costly debug):**
- **Railway region must match Supabase region.** API was originally deployed in `us-west` (California) and every DB query paid ~150-200 ms transatlantic RTT, then the pgbouncer overhead on top — the dashboard's 7-query `Promise.all` took ~10 s. Moving to `EU West (Amsterdam)` dropped query time to ~50 ms. Settings → Region.
- **`DATABASE_URL` query string must include `?pgbouncer=true&connection_limit=5`.** `pgbouncer=true` disables Prisma prepared statements (required for transaction-mode pgbouncer). `connection_limit=5` lets parallel `Promise.all` queries actually run in parallel — `connection_limit=1` (the original value) serialized them, multiplying any per-query latency by 7 on the dashboard. Same value should be set in `apps/api/.env` for local dev.
- **Railway Watch Paths must use one glob per line, not commas.** Watch Paths set as `apps/api/**,packages/**,railway.toml,package*.json` (single line, comma-separated) is parsed as ONE literal pattern with commas in it and matches nothing — every push gets "Skipped: No changes to watched files." Correct format is one pattern per line. Service Settings → Source.
- **API rate limiter must skip in dev / CI.** With 5 parallel Playwright workers each running multiple tests with multiple data fetches, the default 100 req/min/IP global limiter blew through its budget mid-suite. `/api/auth/me` started returning 429, the frontend read it as auth failure, RequireAuth redirected to `/login`, and visual snapshots silently captured the login screen instead of the actual route. Fix lives in `apps/api/src/index.ts`: both `globalLimiter` and `authLimiter` carry `skip: () => process.env.NODE_ENV !== 'production'`. Production rate limiting still in force.
- **Playwright `webServer` must start both API and web.** A single `npm run dev` from `apps/web/` only starts the Vite frontend on :5173 — not the API on :3001. Without the API, every authenticated route 401s, RequireAuth bounces to `/login`, snapshots are wrong. `apps/web/playwright.config.ts` now uses an array of webServer configs that boots `dev:api` (cwd: monorepo root) + `dev:web` and waits for both before tests run. Symptom of the old config: byte-identical mobile snapshot files (e.g., 3 PNGs all 25,213 bytes) because they all captured the same login redirect.
- **Mobile snapshots can drift silently.** Phase 8 PRs that touch mobile screens (typography, mobile shell, semantic h1, parity ports, IA polish) all change the rendered output. After landing one, run `npm run test:e2e:update -w apps/web` to regenerate the four `*-mobile-darwin.png` baselines, then eyeball each one in Preview before committing — the byte-size signature is a useful sanity check (login-redirect captures all weigh ~25 KB).
- **`content-visibility: auto` is incompatible with `flex-wrap` children in current Chrome (2026-05-31, surfaced during PERF-2 first attempt).** The intent: tell the browser to skip paint + layout for offscreen elements to make long shelves cheaper to render. The reality: when applied to flex children inside a `flex-wrap: wrap` container, Chrome's intrinsic-size placeholder box doesn't coordinate with the parent flex's wrap algorithm — cards collapsed / overflowed / reflowed unpredictably as they entered and left the offscreen state ("all over the place" per Andrea's live eyeball). Reverted same-day. **Rule:** do not reach for `content-visibility: auto` on `flex-wrap` layouts without explicit visual verification on a real shelf first. Single-row test fixtures pass falsely because they never trigger the wrap-vs-intrinsic-size interaction. **If grid virtualization is needed (PERF-3):** use `@tanstack/react-virtual` over a CSS-grid-with-computed-`repeat(N, ${coverW}px)` (drop `flex-wrap` from the equation entirely). React-virtual gives a stable row-height estimate via the `coverDensity` preference (3 tiers).
- **Probe spikes for new platform integrations MUST hit the real target endpoint, not just verify the OAuth chain.** M3 (Nintendo Switch) shipped with a WIP backend whose probe stopped at "session_token_code → session_token → access_token → NA `/users/me` profile fetch works" — and on that basis claimed both the scope set was sufficient AND the access_token was the right bearer. Both were wrong, and Andrea's live smoke test surfaced them as back-to-back 403s on `fetchOwnedDevices`. The fixes (hotfixes `3393fd6` + `2b9967f`) cost ~1h of investigate-fix-deploy each. **Rule:** every new platform-integration probe must end by calling a representative real-target endpoint (Moon `fetchOwnedDevices`, GOG `/account/getFilteredProducts`, Epic `library/api/public/items`, itch.io `/my-owned-keys`, etc.). OAuth correctness ≠ API correctness. Captured as AGENT.md decision #44.
- **Persisted SWR cache must be invalidated when response shapes change (2026-05-31 / 2026-05-31 / 2026-06-01).** The cache (`apps/web/src/lib/cache.ts`) writes through to localStorage under `hoard:cache:vN:` so cold reloads are warm. Entries from BEFORE a response-shape extension carry the old shape; consumers reading the new fields get `undefined` and crash (e.g. `entries.length` on undefined) or render garbage. **Rule:** any PR that adds a NEW REQUIRED field to a response type carried by the persisted cache (DealRow, GameDetailResponse, etc.) MUST also bump `STORAGE_PREFIX` (`vN → v(N+1)`) in `cache.ts`. Three historical bumps: v1→v2 (Library overview heroImageUrl), v2→v3 (DealRow.gameIgdbId), v3→v4 (GameDetailResponse extras — releaseDates/screenshotIds/videoIds). **Belt-and-suspenders:** at consumer call sites, default arrays with `?? []` so a future stale entry degrades to "no data shown" rather than a render crash. Both gotchas surfaced live during DEALS-PR1 ("// invalid game id" from `/game/undefined`) and GD-PR2 ("undefined is not an object (evaluating 'entries.length')").
- **Stop-and-confirm cache flash after every PATCH (2026-06-01, surfaced during GD-PR3 eyeball).** When a mutation route returns the updated row, the api client writes the response THROUGH to the primary cache key (`game:${userGameId}`) AND calls `invalidateLibrary()` to drop cross-cutting list caches. Pre-fix, `invalidateLibrary` ALSO dropped the V2 dispatcher key (`game:igdb:${igdbId}`) — which the GameDetail v2 dispatcher reads from — forcing the dispatcher to refetch from server and flash a loading skeleton on every patch (status flip, sub-status, rating, notes…). UX read as "page refresh on every action." **Fix pattern:** split list-invalidation into two helpers — `invalidateLibraryCounts()` for cross-cutting (shelves / dashboard / counts; safe after any in-page edit), and the full `invalidateLibrary()` only for membership-changing operations (add / remove / wishlist toggle). PATCH-like mutations should write the server response THROUGH to any cache key that holds the same row's representation (the dispatcher key in our case), and only invalidate the cross-cutting list caches. The principle: **don't drop a cache entry you've just refreshed**.
- **Sync-time platform-ID misattachment is a Game-row issue, not a UserGame issue (2026-06-01, Andrea's eyeball during GD-PR3).** When sync title-search picks a similarly-named neighbour (sequel / regional variant / etc.), the platform-side ID (steamAppId / psnConceptId / etc.) gets attached to the WRONG IGDB Game row. The user's UserGame may still point at the CORRECT IGDB Game — the corruption is in the Game row's `platform-id` → IGDB-id mapping, not in the user's library mapping. **The pattern manifested in the Graveyard Keeper case:** the original (igdbId 27384) had `steamAppId=null`; the sequel (igdbId 397817, unreleased) had `steamAppId=599140` (which is actually the original's Steam appid). HLTB lookup via Steam appid populated the sequel row with the original's HLTB data; the original row fell through to the impoverished IGDB time_to_beat fallback. **Detection:** existing `[wrong game?]` remap does NOT fix this — it assumes the UserGame is mismapped, not that the Game row is corrupt. Cross-checking `Game.platformId` columns against IGDB's `external_games` table per igdbId can detect the class globally. **One-off fix recipe used 2026-06-01:** Node script that runs `prisma.game.update` to clear the misassigned platform-side ID on the wrong row + set it on the right row, then triggers HLTB re-fetch via the corrected appid. **Future workstream candidate:** one-shot audit pass over the whole catalogue cross-checking platform-side IDs against IGDB's external_games, flagging mismatches for review.
- **`hltbapi.codepotatoes.de` HLTB mirror can be in maintenance — known signature (2026-06-01).** Returns plain-string body `"It looks like hltb changed their game again. Please wait for an update"` on every request when the mirror is updating its scraper. While down, every Game without an existing HltbData row falls through to the IGDB `time_to_beat` impoverished fallback (single `mainStory` value, no extras / completionist). The Steam-ID-first cascade can't make progress. **Check before any HLTB backfill workstream** — `curl -s 'https://hltbapi.codepotatoes.de/steam/<known-appid>'` and look for the maintenance string. **Rule:** when an HLTB re-fetch fails, distinguish "the mirror is down" (transient — wait it out) from "this game isn't on HLTB" (persistent — log and accept) by checking the response shape before degrading.
- **`prisma migrate deploy` / `migrate dev` hang against the pgbouncer pooler.** Prisma migrate uses advisory locks for coordination, and pgbouncer transaction-mode (`?pgbouncer=true`) doesn't support them — the command silently sits forever. Symptom: 0-byte output file, process alive but doing nothing. The `migrate deploy` command may even exit 0 without actually running the DDL. Workaround that does work: `npx prisma db execute --file <migration.sql> --schema prisma/schema.prisma` to run the DDL, then `npx prisma migrate resolve --applied <migration_name>` to record it in `_prisma_migrations`. Hand-write the migration SQL (matches what the previous `enable_rls_on_public_tables` and `usergame_perf_indexes` migrations already do). Permanent fix would be adding a `directUrl` to the Prisma datasource block — defer until convenient. **Update 2026-05-21 (TL1.1):** `migrate resolve --applied` *also* exhibits the hang now — but **the INSERT into `_prisma_migrations` lands BEFORE the hang** (verified: TL1.1's row had `finished_at: 2026-05-21T18:41:17.512Z` from the first hung attempt). If `migrate resolve` hangs, wait ~10s then verify the row exists rather than retrying — retrying after Ctrl-C can collide on prepared statements left in the pgbouncer pool. **Reliable fallback when the CLI is stuck:** use Prisma Client via a one-off Node script (`const { PrismaClient } = require('@prisma/client'); await prisma.$executeRaw\`INSERT INTO _prisma_migrations ... ON CONFLICT DO NOTHING\``). Honours `pgbouncer=true` for prepared-statement disabling. Also: `prisma db execute` on SELECT queries hits the "prepared statement s1 already exists" collision intermittently against pgbouncer — use Prisma Client `$queryRaw` for any verification SELECTs.

See `docs/PLAN.md` → Phase Status table for full live status.

All phases complete through Phase 8. App is deployed, accessible (WCAG 2.1 AA), and feature-aligned across desktop and mobile. Email/password and Google OAuth login both verified end-to-end. Steam OpenID still works locally and just needs the same redirect URI verification on prod.

**Test commands:**
- `npm run test` — 115 API tests + 69 web tests, all passing
- `npm run test:e2e -w apps/web` — Playwright E2E suite (auto-starts both `dev:api` + `dev:web`). Includes:
  - `screens.spec.ts` — assertions + visual snapshots for every route
  - `a11y.spec.ts` — axe-core (WCAG 2.1 A + AA) on Dashboard / Library / Upcoming / GameDetail / Settings / Login on desktop + mobile (12 tests)
- `npm run test:e2e:update -w apps/web` — same, but regenerate visual snapshots
- `npm run test:e2e:offline -w apps/web` — Playwright offline E2E (3 tests) against production preview build with active service worker

**CI workflows:**
- `.github/workflows/ci.yml` — lint + format + typecheck + tests + builds on every PR. Lint includes `eslint-plugin-jsx-a11y` (added Phase 8 PR 3) — `<div onClick>` without keyboard support, missing form labels, and similar a11y patterns block CI.
- `.github/workflows/lighthouse.yml` — Lighthouse CI on PRs touching `apps/web/**`. Thresholds: Performance ≥ 80, Accessibility ≥ 95 (raised from 90 in PR 3), Best-practices ≥ 90. Local run shows 99 / 100 / 100. PWA category was retired in Lighthouse 12 — installability is verified by the offline E2E test instead.

**Recent fixes landed (most recent first):**
- **DEALS-PR1 — `/deals` foundation with ITAD + market picker + affiliate router + alerts-strip chip (2026-05-31, this commit).** First entry in the DEALS-series per PAGES_PLAN §8. Andrea's pre-deploy ops checklist: `ITAD_API_KEY` env var on Railway (Andrea registering in parallel; without the key `/deals` renders empty — graceful degradation); per-reseller affiliate IDs (`HUMBLE_AFFILIATE_ID` / `INSTANT_GAMING_AFFILIATE_ID` / `GMG_AFFILIATE_ID` / `KINGUIN_AFFILIATE_ID` / `CDKEYS_AFFILIATE_ID`) — optional, set when affiliate accounts are live. **What shipped:** (i) **Schema** — `User.marketCode String?` (ISO 3166-1 alpha-2, drives locale currency + future Amazon storefront selection); `Deal { gameId, shopId, shopName, isReseller, currentPrice, originalPrice, currency, discountPct, dealUrl, voucher?, expiresAt?, storeLow?, isHistoricalLow, isTrendingDown, fetchedAt }`; `PriceSnapshot { gameId, shopId, price, currency, snapshotAt }`. Migration `20260531180000_deals_foundation` applied via documented `prisma db execute` + Node `$executeRaw` migration-record fallback. (ii) **ITAD client** at `apps/api/src/services/itad.ts` — `isItadConfigured()` gates calls on `ITAD_API_KEY` env var (non-Andrea environments without the key return 0 deals); `lookupItadIdBySteamAppId(appId)` via `/lookup/id/shop/61/v1` for stable Steam-keyed lookup; `lookupItadIdsByTitles(titles)` bulk POST for non-Steam-keyed games; `getPricesForGames(ids, country)` POST chunked at 200 per request. Throws `ItadClientError` on misconfiguration/network failure; caller catches + logs + continues. (iii) **Storefront taxonomy** — case-insensitive name match: Tier-1 first-party (Steam/GOG/Epic/Humble Store/Battle.net/itch.io/PSN/Xbox/Nintendo eShop) always in scope; Tier-2 reseller allow-list (Humble Bundle/Instant Gaming/GMG/Kinguin/CDKeys per OQ-DEALS-9) gated. Off-list (G2A/Eneba/Fanatical/GamesPlanet — Fanatical+GamesPlanet are expansion candidates not yet in the list) returns `'excluded'`; orchestrator drops those at sync time. 22 unit tests pin every classification row + the expansion-candidate-not-yet-allow-listed cases. (iv) **Affiliate router** at `apps/api/src/services/deals/affiliate.ts` — per-reseller env-var-keyed affiliate IDs; per-reseller documented param names (`partner` for Humble, `igr` for Instant Gaming, `mw_aref` for GMG/CDKeys, `tap_s` for Kinguin, `as_aid` for GOG, `ref` for itch.io). When env var unset, URL passes through unrewritten — identical UX, just no cost-recovery routing per OQ-DEALS-5. Idempotent (URLSearchParams `set` overwrites). 17 unit tests cover every reseller + idempotency + URL-parse-failure graceful degradation. (v) **Sync orchestrator** at `apps/api/src/services/deals/syncDeals.ts` — resolves ITAD IDs (steamAppId-first, title-lookup fallback; caches result on `Game.metadata.itadId` so future syncs skip the lookup); fetches prices in 200-per-batch chunks at ~3 req/s; classifies + filters via `storefronts.ts`; upserts Deal rows per (gameId, shopId); writes daily PriceSnapshot for every shop returned (regardless of whether there's a discount — captures baseline for trend detection); deletes stale Deal rows for shops no longer on sale. **Trending-down detection** at sync time: after each snapshot insert, queries last 30 days of snapshots for that (game, shop) and sets `Deal.isTrendingDown` true when ≥2 distinct drops (default; tunable via `TRENDING_DOWN_DROPS` / `TRENDING_DOWN_DAYS` env vars per OQ-DEALS-13). (vi) **Routes** — `GET /api/deals` returns `{ topWishlistDeal, wishlistDeals, broaderFeed, marketCode, lastSyncedAt }`. CM12 + already-owned exclusion applied at QUERY time: a game owned via UserGame status≠Wishlist + zero `wishlistedPlatforms` → skipped from broader feed; a game owned on PSN but wishlisted on Switch (CM12 per-platform wishlist) → still in wishlist section. Deal URLs **affiliate-routed BEFORE the response leaves the server** — frontend never sees raw URLs. `POST /api/admin/deals/refresh` triggers `syncAllDeals()` for the admin manual button. `PATCH /api/auth/me` extended with `marketCode: string | null` (ISO 3166-1 alpha-2, regex-validated; null clears). (vii) **Dashboard wiring** — `DashboardResponse.wishlistDealsCount: number`; `prisma.deal.count` over the user's wishlist game IDs in the same Promise.all that powers the rest of dashboard. `AlertsStrip` extended with a second chip kind (amber-bordered `// X wishlist games on sale`) that navigates to `/deals`. Both chips render conditionally; both null returns `null` (no strip — bento grid collapses cleanly). (viii) **Frontend `/deals` page** — new `DealsDesktop.tsx` (~190 lines) + `DealsMobile.tsx` (~110 lines), lazy-loaded. Desktop: hero card (240×135 landscape cover left, metadata right with -X% green, prices, chips, `[buy on Shop →]`); list rows (60×34 cover, title + storefront subline with inline chips, discount %, price, expiry, `buy →`). Mobile: compressed per OQ-DEALS-4 (hero collapses to a regular row; cards drop storefront chip to a subline; pull-to-refresh wired). New `useDeals()` SWR hook + `api.deals()` client method. Sidebar `Deals` nav entry (new `tag` icon path); MobileTabBar widened from 4 to 5 tabs (added Deals between Soon and Me; CSS grid `repeat(5, 1fr)`). (ix) **Settings → Account market picker** — `<select>` with 10 ISO 3166-1 options (AT/DE/IT/FR/ES/GB/US/CA/AU/JP + clear); `saveMarket(next)` calls `api.updateMe({ marketCode })` with green `// saved` toast. **Types — `@hoard/types`:** new `DealRow` + `DealsResponse` interfaces; `AuthUser.marketCode` (required `string | null`); `PatchMeBody.marketCode?: string | null`; `DashboardResponse.wishlistDealsCount: number`. **Tests:** 39 new backend (22 storefronts + 17 affiliate); existing dashboard tests updated to mock `prisma.deal.count` (default 0) + the new `gameId`/`wishlistedPlatforms` columns on the `aggUserGames` select; 9 test fixtures backfilled with `marketCode: null` + `wishlistDealsCount: 0`. **642/642 backend tests pass** (+48 vs the 594 pre-DEALS baseline). 61/61 affected web tests pass. Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** new DealsDesktop chunk 7.68 kB / 2.33 kB gzipped + DealsMobile 3.90 kB / 1.61 kB gzipped (lazy-loaded); DashboardDesktop +0.05 kB gzipped (wishlistDealsCount chip wire); initial JS index 260.74 → 261.34 kB (+0.16 kB gzipped, type-only). **Sticky properties for next DEALS PRs:** (1) `Deal.isReseller` distinguishes Tier-1 from Tier-2 at query level — frontend filter chips by source-type read this directly. (2) `PriceSnapshot` is the source of truth for trend detection; `isTrendingDown` flag on Deal is cached output. Prune snapshots older than 90 days in a future cron. (3) Affiliate router is `routeAffiliateUrl(shopName, url)` — single function, called server-side BEFORE the response leaves. Adding a new reseller = 2 lines (one to `AFFILIATE_ENV_VARS`, one to `AFFILIATE_PARAM_NAMES`). (4) Sale-event grouping (DEALS-PR2) + physical deals (DEALS-PR3) layer on top of this foundation without schema changes to the Deal/PriceSnapshot tables — both add NEW tables (`SaleEvent`, possibly `PhysicalDeal` or extend Deal with a `kind` enum). (5) Cron is currently MANUAL only — no Railway cron job set yet; admin button at `/admin/deals/refresh` is the trigger. Future: add a Railway scheduled job that hits `POST /api/admin/deals/refresh` once per day (admin token in a header or auth-bypass-by-IP).
- **Library overview card restructure — landscape cards with IGDB hero images (2026-05-31, this commit).** Andrea-flagged follow-up after B-IGDB-3b2 finished. Library OVERVIEW shelves now use landscape cards with proper IGDB hero images (artworks[0] preferred, screenshots[0] fallback) instead of the portrait box art that's used everywhere else. Now Playing cards are sized larger than other shelves (280px wide vs 220px) so the same horizontal space fits 4 vs 5 cards — visual hierarchy reflects "what I'm currently playing" as the page's primary signal. Filtered views (`/library/:status`, `/library/by-X/:slug`) keep portrait `ShelfItem` (Andrea explicit: "filtered views keep the current portrait card"). **First pass shipped with cropped portrait covers** (`object-fit: cover` on the existing `coverUrl`); Andrea's eyeball: *"Oh nono its not looking good. We need proper images for this use case. dont we have access to more horizontal images?"* — pivoted to fetch real landscape art from IGDB. **What shipped:** (i) schema `Game.heroImageUrl String?` via migration `20260531120000_game_hero_image_url`; (ii) `deriveHeroImageUrl(artworks, screenshots)` helper in `apps/api/src/services/igdb.ts` returning `https://images.igdb.com/igdb/image/upload/t_screenshot_big/<image_id>.jpg` — prefers artworks[0] (curated key art / banners) over screenshots[0] (in-game shots, always 16:9); returns null when both empty so renderer falls back to coverUrl; (iii) IGDB query strings extended with `artworks.image_id, screenshots.image_id` (7 sites — `getGame`, `mapToSearchResult`'s 3 factories, `getReleaseDetails`, releases recent feed, localization query with `game.` prefix); (iv) types: `Game.heroImageUrl` / `IgdbSearchResult.heroImageUrl` / `IgdbUpcomingRelease.heroImageUrl` / `WishlistRelease.heroImageUrl` all required `string | null`; (v) all `Game.upsert` call sites thread heroImageUrl (`syncRunner`, `wishlistImport`, `routes/upcoming` wishlist toggle, `routes/games` remap); (vi) mapper `apps/api/src/lib/mappers.ts` reads heroImageUrl off Game; (vii) `WishlistRelease`-shaped fallbacks (igdb wishlist scope, releases recent, upcoming, useUpcoming, dashboard wishlistCountdown) default heroImageUrl to null since WishlistRelease doesn't snapshot it — Releases cards use portrait covers anyway. (viii) Frontend: new `apps/web/src/components/library/LibraryOverviewCard.tsx` (~80 lines) — landscape card layout: 16:9 cover with platform glyph top-right, title left + playtime right side-by-side below. `Cover` primitive's existing `object-fit: cover` handles the rare case where heroImageUrl is null + coverUrl is portrait — graceful degradation. `LibraryDesktop` overview `Shelf` component refactored: per-shelf card width (Now Playing 280, others 220) drives the slot count via the existing `ResizeObserver` math; ShelfItem replaced by `LibraryOverviewCard` in the overview branch only. Filtered views unchanged. `GameDisplay` type extended with `heroImageUrl: string | null`; `toGameDisplay` reads it off the mapper. **Backfill:** new `scripts/backfill-game-hero-image.ts` (~85 lines, dry-run flag) re-fetches every existing Game by igdbId through the IGDB service, persists heroImageUrl, throttled at ~3 req/s (350ms delay). Resumable — skips rows that already have heroImageUrl. Ran post-deploy against Andrea's library: 2185 candidate games (includes WishlistRelease-imported + HLTB-driven rows beyond the ~500 owned UserGames). **Migration apply:** documented `prisma db execute --file ... migration.sql --schema ...` recipe; `prisma migrate resolve --applied ...` hit the documented pgbouncer advisory-lock hang, fell back to the Node script (`scripts/check-and-insert-hero-migration.ts`) using `prisma.$executeRaw` to write the `_prisma_migrations` row directly (ON CONFLICT removed because `migration_name` isn't a unique column — pre-existing operational gotcha). **Tests:** 6 new in `LibraryOverviewCard.test.tsx` (title + playtime · heroImageUrl when present · coverUrl fallback when null · click → /game/:id · 16:9 dimensions · platform glyph overlay). 9 existing test fixtures backfilled with `heroImageUrl: null` defaults across `AddGameModal`, `RemapGameModal`, `ReleasesRecentDesktop`, `Dashboard.bento`, `bucketing`, `empty-state`, `mobile`, `pickWishlistedPlatformChips`, `primitives` test files plus `igdbMatch.test.ts` backend fixture. **594 backend tests pass** (unchanged from B-IGDB-3b2 close — no new backend tests since the IGDB service factory is exercised by existing path-tests with `objectContaining` matchers that tolerate the new field). 38/38 affected web tests pass. Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** LibraryDesktop chunk 20.58 → 21.72 kB (+0.10 kB gzipped — LibraryOverviewCard + heroImageUrl threading); initial JS unchanged. LibraryMobile unchanged (mobile keeps portrait — see sticky property below). **Sticky properties for future contributors:** (1) **Mobile shelves keep portrait** in this commit — landscape cards on a narrow viewport fit only 2 per row, defeating the carousel browsing pattern. Revisit if Andrea reports the desktop landscape look should propagate; the swap is small (mirror LibraryDesktop's `Shelf` refactor in `LibraryMobile`'s `MobileShelf`). (2) **Hero image fetched at sync time** for new Games; backfill is one-time. If IGDB updates a game's artworks/screenshots after sync, Hoard's heroImageUrl is stale until that game next syncs (or is explicitly re-fetched via remap). Acceptable — IGDB images rarely change for shipped games. (3) **`t_screenshot_big` (889×500)** is the chosen image size — adequate for ~280px landscape cards on retina (2× = 560 wide); avoid bumping to `t_1080p` which adds bandwidth without visible win at our card sizes. (4) **`heroImageUrl` is null-tolerant everywhere** — every consumer (LibraryOverviewCard + future surfaces) should fall back to `coverUrl` gracefully. Long tail of obscure IGDB games + community-uploaded titles won't have artworks/screenshots.
- **B-IGDB-3b2 — primary-lens routes + Steam-style sidebar browse-by + change-lens + find input (2026-05-31, this commit).** Completes the IGDB-tag-triple workstream. Promotes genre/theme/perspective from secondary filters into PRIMARY lens types (their own route family). Andrea also flagged a follow-up: restructure the Library OVERVIEW cards (landscape orientation, Now Playing larger than other shelves — 4 across vs 5); confirmed B-IGDB-3b2 first, card restructure as a focused follow-up. **Mid-PR Andrea adjustment** after first pass landed: the `BrowseByPanel` was at the BOTTOM of `/library` overview (below all 6 status shelves) per the PAGES_PLAN §4.4.2 mockup — too easy to miss. Andrea: *"I dont see a way to access the different library views ... Take the same approach steam uses when browsing the library."* — rebuilt as a **Steam-style left-rail navigation** in the Sidebar (`// browse by` collapsible groups, one per dimension); removed the now-redundant `BrowseByPanel` from `LibraryDesktop` overview (LibraryMobile still uses the inline panel since there's no sidebar on mobile). **Sidebar browse-by behaviour:** each dimension renders a collapsed header chip (`▸ genre 22`); click → expands inline; auto-opens when the active route is on its dimension (URL `/library/by-genre/action` → genre group auto-opens + "action" highlighted via `aria-current=page`); top-5 visible + "show all N →" toggle to reveal the rest. Click any value → navigates to the lens route. Genres / themes / perspectives don't have icons (sidebar reuses the existing `▸/▾` glyph for collapse state). Group hides entirely when its dimension has zero values (progressive disclosure preserved). **New `BrowseByGroups` + `BrowseByGroup` components** inline in `Sidebar.tsx` (~110 lines). **Tests:** 7 new in new `apps/web/src/components/layout/__tests__/Sidebar.test.tsx` (header renders + only when data exists · empty dimensions hidden · collapsed default · click expands · click value navigates · top-5 + show-all · show-all reveals rest · auto-opens on active route + aria-current highlight). The rest of B-IGDB-3b2 below: **What shipped:** (i) three new routes `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug` registered as explicit routes in `apps/web/src/App.tsx` (vs a generic `by-:dim/:slug` capture — explicit keeps matching unambiguous + per-lens error states easy). (ii) slug round-trip helpers `apps/web/src/lib/tagSlug.ts` (~30 lines) — `slugifyTag("Role-playing (RPG)") → "role-playing-rpg"` (lowercase + strip apostrophes + collapse non-alphanumeric to single hyphen + trim); `findTagBySlug(allTags, slug)` returns canonical name; case-insensitive on input; null when no match. Slugs are NOT stable identifiers (re-derived from name on every read) — acceptable because IGDB tag renames are rare; stale bookmarks 404 to a "tag not found" view rather than serving stale data. (iii) backend `GET /api/games/lens-index` returning `{ genre: TagCount[], theme: TagCount[], perspective: TagCount[] }` — every IGDB-tag value present in the user's library with UserGame counts, sorted by count desc + name asc tiebreak. Single Prisma query iterating all user's UserGames, aggregating client-side per dimension. `Cache-Control: private, max-age=30`. Consumed by both browse-by panel + slug→canonical-name resolution. (iv) new `useLensIndex()` client hook caching via SWR + PERF-1 localStorage persistence; key `lens-index` (no user-scoped variance beyond JWT). (v) new `useLensRoute()` hook (~40 lines) — single source of truth parsing URL pathname + params → `{ type: 'status' | 'genre' | 'theme' | 'perspective' | null, slug: string | null }`. (vi) new `BrowseByPanel` component (~110 lines, 8 tests) — overview `// browse by` section with three rows; each defaults to top-3 + `[show all N →]` link; click "show all" → row expands inline to chip-row of every value with count (OQ-LIB-7 inline-expand lock — IGDB's small enumerations make this scale cleanly at ~22 genres / ~25 themes / ~8 perspectives, no separate index pages needed). Click any value → navigates to the corresponding primary-lens route. Progressive disclosure: dimensions with zero data hidden entirely. (vii) new `ChangeLensPopover` component (~135 lines, 6 tests) — `[change lens ▾]` chip with WAI-ARIA listbox pattern (same shape as `FilterPopover`); pivots primary lens preserving secondary filter intersection where possible. **v1 pivot rules:** FROM Status lens TO tag lens enabled IF `?<dim>=value` is set in URL (status becomes secondary on the new primary). Reverse + tag↔tag pivots ship when status-as-secondary-on-tag-lenses lands. Disabled options grey out with `"—"` hint. (viii) `LibraryDesktop` + `LibraryMobile` refactored to be lens-aware: `useGames()` params switch on `lens.type` (status → `?status=`, tag → `?{dim}=` using `lensCanonical` resolved from `lens-index`); filtered-view branch widened from `if (statusParam)` to `if (filteredEnabled && lens.type)`; secondary FilterPopover dimensions filtered to exclude the PRIMARY one (e.g. on Genre lens, only theme + persp popovers); URL-param reset effect now triggers on `lens.slug` + `lens.type` change instead of just `statusParam`. Page heading shows lens type as a faint prefix + canonical tag name in accent (`// genre  ROLE-PLAYING (RPG) · 87 titles`). (ix) **find input** on filtered views — `?q=` URL param; client-side filter over the already-loaded games (`limit:50000` means the full intersection is in memory); composes with all other filters; cleared on lens change. (x) **slug-not-resolved guard** — when lens-index loads and the URL slug doesn't match any known tag, renders a "tag not found" view with `[back to library]` instead of an empty grid. (xi) **two-row filter bar** preserved from the FilterPopover workstream — Row 1 anchors `[+ add game]` top-right alongside `find` input, `[change lens ▾]`, `sort`; Row 2 holds platform chips + secondary tag popovers. **API contract:** new `LensIndexResponse` + `LensIndexEntry` types in `@hoard/types`; `api.lensIndex()` client method. **Tests:** 25 new (11 tagSlug + 8 BrowseByPanel + 6 ChangeLensPopover) plus 4 backend (`/api/games/lens-index` — aggregation correctness / count-tie name-asc tiebreak / empty-library edge / Cache-Control header). **594 backend tests pass** (+5 vs the 589 baseline). Targeted vitest 47/47 (10 FilterPopover + 11 tagSlug + 11 pickTopTags + 8 BrowseByPanel + 6 ChangeLensPopover + 1 LibraryDesktop). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** LibraryDesktop chunk 17.15 → 20.63 kB (+0.85 kB gzipped — lens detection + change-lens + browse-by + find input + slug resolution); LibraryMobile 15.47 → 18.83 kB (+0.78 kB gzipped — same surfaces on mobile shell); initial JS index 257.88 → 258.18 kB (+0.30 kB raw, type-only). **Sticky properties for future contributors:** (1) **`useLensRoute()` is the single source of truth** for lens detection. Adding any future primary lens type (Collection / Developer / Publisher / Series / Rating per PAGES_PLAN §4.4.1) is a one-line `LensType` union extension + new explicit route in `App.tsx` + new pathname branch in `useLensRoute.ts`; everything downstream (filtered view, change-lens, browse-by) inherits the new lens for free. (2) **`BrowseByPanel` generalizes via `LensIndexResponse` shape** — adding Collection / Developer / Publisher rows means extending the backend lens-index payload with `collection: TagCount[]` etc. + adding a `<Row>` in `BrowseByPanel.tsx`. (3) **Slug round-trip** — `slugifyTag` + `findTagBySlug` are dimension-agnostic; reuse for any future reference-data lens. (4) **Change-lens pivot rules** — currently only `Status → tag` is enabled; when status-as-secondary on tag lenses ships, extend the option-disabled logic in `LibraryDesktop`/`LibraryMobile`'s `changeLensOptions` build + the `handleChangeLens` pivot logic. (5) **Find input** is client-side filtered — works because `limit:50000` loads the full intersection; if libraries ever exceed that, the server-side `?q=` path already exists, just thread it into `useGames()` params instead.
- **Library secondary filter UX — chip strips → single-select FilterPopover (2026-05-31, this commit).** Follow-up after Andrea's live eyeball on B-IGDB-3b1: the three chip rows (genre · theme · persp) spread filter options across the page and felt "all over the place." Replaced with three inline single-select dropdown triggers `[genre: any ▾] [theme: any ▾] [persp: any ▾]`. **Andrea-driven iterations during build:** (a) UX locked via AskUserQuestion before write: inline chip button per dimension + single-select; (b) v1 put add-game + sort + popovers on one row, but on narrower viewports the row wrapped and `[+ add game]` landed on the LEFT of a new line — Andrea: *"the main action should always be top right"*; **split filter bar into two rows** (locked via AskUserQuestion): **Row 1** = shelves crumb · name · count · (spacer) · sort · `[+ add game]` (action anchored top-right, never wraps below), **Row 2** = `plat` label · platform chips · (spacer) · genre · theme · persp dropdowns; (c) on a tight Row 2, long values like "real time strategy" still pushed dropdowns to a third row — Andrea: *"as soon as the size make the filter goes to the next row we add ... to the text"*; **added ellipsis truncation to FilterPopover trigger** so dropdowns SHRINK with `…` before wrapping (wide window: no visible difference; narrow window or long value: trigger label clips). **New component** `apps/web/src/components/library/FilterPopover.tsx` (~200 lines) — chip-styled trigger with `▾` chevron; click opens an absolutely-positioned listbox below with `[any, ...top-N options]`; option click applies + closes; "any" clears. Counts shown in faint `(N)` next to each option. Trigger has `min-width: 0; max-width: 200px`; the label span has `overflow: hidden; text-overflow: ellipsis; white-space: nowrap; min-width: 0`; the chevron has `flex-shrink: 0` so it never disappears. Outer wrapper uses `display: inline-flex; min-width: 0` so the parent flex row can shrink it. The LibraryDesktop per-dimension wrapper `<div>` also gets `min-width: 0` (the well-known flex shrink propagation trick — without it, `min-width: auto` blocks shrink at intrinsic content width and the row wraps instead). **WAI-ARIA listbox pattern:** trigger has `aria-haspopup="listbox"` + `aria-expanded`; listbox has `role="listbox"` + `tabIndex={-1}` and owns key handling (Arrow Up/Down navigate, Home/End jump, Enter/Space select, Escape close-and-refocus-trigger); options have `role="option"` + `tabIndex={-1}` + per-option Enter/Space fallback (satisfies `jsx-a11y/click-events-have-key-events`). Click-outside via `pointerdown` listener; auto-focus on open so keyboard works after mouse open. **Helper extended:** `apps/web/src/lib/pickTopTags.ts` gains `pickTopTagCounts()` returning `{ name, count }[]` — same ordering as `pickTopTags()` (count desc + name asc tiebreak); both LibraryDesktop + LibraryMobile callers swap to the counts variant so option labels can show occurrence counts. **LibraryDesktop:** dropped the B-IGDB-3b1 chip-row block; the existing single filter bar is now two rows (action row + filter row, per Andrea's lock above). **LibraryMobile:** dropped the per-dimension `thin-scroll` chip rows (one per dimension); now a single compact horizontal strip holds the three FilterPopover triggers (still `flexShrink: 0` + `overflow-x: auto` in case all three populate on narrow viewports). Progressive disclosure preserved: dimensions whose loaded shelf has zero tags render nothing. Active-not-in-top-N edge case preserved: if the active filter falls outside top-N, it's prepended with `count: 0`. **URL state unchanged:** `?genre=&theme=&perspective=` query params drive the active values; same `setTagFilter()` callback wired to each FilterPopover. **Tests:** 10 new in `apps/web/src/components/library/__tests__/FilterPopover.test.tsx` (trigger label reflects value · open on click · `[any, ...options]` rendered · click option calls onChange + closes · "any" → null · Escape closes without change · click-outside closes · ArrowDown+Enter selects · counts shown · aria-selected on active). **23/23 across affected scope** (10 FilterPopover + 11 pickTopTags + 1 LibraryDesktop + 1 cache invalidation guard). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** LibraryDesktop chunk 17.29 → 17.15 kB (-0.14 kB raw, -0.04 kB gzipped — inline chip-row JSX replaced by reusable component), LibraryMobile 15.53 → 15.47 kB (-0.06 kB raw, similar gzipped reduction). Net smaller despite adding the ~200-line component. **Sticky properties for future contributors:** (1) **B-IGDB-3b2 (primary-lens routes + browse-by panel)** — FilterPopover takes a generic `TagCount[]` and a label; extending to Collection / Developer / Publisher / Series lenses (post B-Stash-4 + B-Stash-5) is a one-line addition per dimension at the call site. The component itself is dimension-agnostic. (2) **Two-row filter bar is the new convention** when adding more secondary filters or primary actions to Library; do NOT mix actions with filters on the same wrapping row — the primary action anchors top-right of Row 1; secondary controls cluster in Row 2 and wrap independently. (3) **Truncation via flex-shrink + min-width: 0 chain** is the pattern when a flex item's content shouldn't push the parent to wrap — propagate `min-width: 0` through every wrapper level, cap with `max-width` if needed, and apply `overflow: hidden; text-overflow: ellipsis` to the truncatable text element only.
- **PERF-2 partial — Library grid paint perf (React.memo only; content-visibility reverted) (2026-05-31, this commit).** Second perf pass after PERF-1's persisted cache; first attempt at the in-shelf filter-click bottleneck. **Honest scope:** only the `React.memo` half landed. **What shipped:** `React.memo` on `ShelfItem` (`apps/web/src/components/screens/LibraryDesktop.tsx`) — chip toggle / sort changes no longer re-render ~500 prop-identical cards; only cards whose props actually changed reconcile. Shallow-compare cost is negligible vs the saved render work. LibraryMobile's grid card is inline (not extracted as a memoized component) so memo doesn't apply there. **What was reverted same-day:** `content-visibility: auto` + `contain-intrinsic-size` on both LibraryDesktop shelf items + LibraryMobile inline grid cards. Andrea's live eyeball showed visual breakage ("all over the place" — cards collapsed / overflowed) within minutes of deploying. `content-visibility: auto` interacts badly with `flex-wrap` children in current Chrome: the intrinsic-size placeholder box differs from the rendered card box, and cards reflow unpredictably as they enter / leave the offscreen state. Removed both inline-style additions; kept `React.memo` (purely behaviour-preserving wrapper, can't cause visual regression). **Bundle delta:** LibraryDesktop chunk +0.04 kB gzipped from the surviving memo wrap. LibraryMobile reverted to baseline. **Visible UX effect:** moderate — chip clicks re-render less of the DOM, but the browser still paints + lays out all cards (offscreen included). If Andrea reports filter-click lag persists, **PERF-3 is the real virtualization workstream** (back-pocket): `@tanstack/react-virtual` over the shelf grid promoted from `flex-wrap` to a CSS-grid-with-computed-`repeat(N, ${coverW}px)` so row windowing has a stable estimate. Cover dimensions are bounded by the `coverDensity` preference (3 tiers) so row-height estimation is straightforward. **Lesson recorded (operational gotcha for future paint-perf work):** `content-visibility: auto` looks cheap on paper but is incompatible with `flex-wrap` children in current Chrome — the intrinsic-size box is treated as a separate layout context that doesn't coordinate with the parent flex's wrap algorithm. Don't reach for it again on flex-wrap layouts without explicit visual verification on a real shelf first; testing with mock data and a single-row layout will pass falsely.
- **PERF-1 — SWR cache localStorage persistence (2026-05-31, this commit).** Small perf workstream after Andrea's feedback: *"I feel its a bit slow overall. Cant we do something more effective like storing cookies on the user browser so that he do not have to wait a long time every time he changes values or browse the platform everytime?"* (Cookies is the wrong tool — 4KB max + sent on every request; `localStorage` is the right primitive for ~100KB-1MB cached API payloads.) **The pain:** the SWR cache (`apps/web/src/lib/cache.ts`) was in-memory only (`Map<string, Entry>`), so every page reload or new tab started cold — Library, Dashboard, Releases each had to refetch on first paint. **The fix:** extended `cache.ts` with write-through localStorage persistence under the namespaced prefix `hoard:cache:v1:`. Hydration runs synchronously once on module load (before any `useQuery` subscriber subscribes) so cold paint paths return cached data instantly + fire a background refetch via the existing stale-window logic. **API contract unchanged:** `get()` / `set()` / `invalidate()` / `subscribe()` / `_resetForTests()` signatures + behaviour are identical from a consumer's perspective; persistence is purely additive at the implementation layer. `set()` writes through to `localStorage.setItem(STORAGE_PREFIX + key, JSON.stringify({ data, ts }))` then emits to subscribers; `invalidate(prefix)` scans + removes matching `localStorage` entries alongside the in-memory drop; `_resetForTests()` also clears persisted entries to keep test isolation honest. **Edge cases handled:** localStorage unavailable (some private-browsing modes reject reads) → graceful degradation to in-memory-only (no throw); quota exceeded on write → try/catch around `setItem`, `set()` never throws; per-entry size cap at 1.5MB chars (UTF-16 ≈ 3MB bytes) so oversized payloads stay in-memory-only and don't burn the quota (browser limit is typically ~5MB per origin); corrupt persisted entries (JSON parse fails / wrong shape) skipped silently during hydrate rather than crashing the boot path; namespace prefix means we don't collide with other localStorage usage from other libs / dev tools. **Version key:** `v1` in the prefix; bump invalidates every persisted entry app-wide on schema changes (no migration — old entries are orphaned + eventually evicted by browser quota pressure or explicitly cleared in `_resetForTests`). **Tests:** +7 in `cache.test.ts` covering write-through (`localStorage` set after `set()`), namespace isolation (sibling `localStorage` keys untouched), `invalidate(prefix)` removes persisted entries, full-wipe via `invalidate('')`, `_resetForTests` cleanup, size-cap skip-when-too-large (3MB blob → in-memory only, no localStorage write), quota-throw graceful degradation (`setItem` throws → `set()` doesn't throw, in-memory entry still lands), corrupt-entry tolerance (malformed JSON in storage doesn't break unrelated keys). **15/15 cache + 58/58 across targeted scope** (cache + api-invalidation + pickTopTags + Dashboard.bento) pass. Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** initial JS 257.08 → 257.88 kB (+0.8 kB raw, +0.25 kB gzipped) — persistence logic is small. **Visible UX effect:** cold page reload now returns cached Library / Dashboard / Releases data instantly; the SWR background refetch fires under the hood for freshness. The user-visible loading skeleton is now bounded to (a) first-visit-ever-to-the-app, (b) after explicit logout / cache wipe via `invalidate('')`, or (c) genuinely-stale entries past their stale window. **Sticky property for next perf workstream (PERF-2 grid virtualization):** persisted cache only helps cold reload + cross-shelf navigation; it does NOT help the in-shelf DOM re-render on filter chip clicks. 513 game cards re-rendering on every chip toggle / sort change is the next bottleneck — virtualization (only render visible rows) is the structural fix. The persisted cache + virtualization are complementary: the cache makes the data appear instantly, virtualization makes the painted DOM affordable.
- **B-IGDB-3b1 — Library secondary filter chip strips (2026-05-30, this commit).** Sixth PR derived from `docs/PAGES_PLAN.md` v2 spec §4.4. Follows B-IGDB-3 partial (foundation + Dashboard 3-tab) earlier today; wires the IGDB-tag triple foundation into the Library Status-lens secondary filter UI via three new chip rows (`genre · theme · persp`) on `/library/:status` filtered view. **Pure-frontend follow-up** — server filter surface `?genre=&theme=&perspective=` was already live from the B-IGDB-3 foundation. **What shipped:** new `apps/web/src/lib/pickTopTags.ts` (~50 lines) — `pickTopTags(games, dimension, cap)` returns top-N distinct tags ordered by occurrence count desc + name asc tiebreak (deterministic); `filterByTag(games, dimension, value)` is the composable client-side predicate. Both helpers are dimension-agnostic across genre/theme/perspective via a `TagDimension` union — extends cleanly to future B-Stash-4 (Collections) / B-Stash-5 (Developer/Publisher/Series) lenses. **LibraryDesktop** + **LibraryMobile** — three new chip rows below the existing platform row on the `/library/:status` filtered view. Each row only renders when the loaded games carry that tag (progressive disclosure pre-backfill — desktop renders horizontally inline; mobile uses the `.thin-scroll` overflow-x pattern matching the platform strip). Chip values are derived from the FULL shelf pre-filter (NOT the post-filter slice), so the user sees all options even after narrowing on another dimension. If the active filter is a tag NOT in the top-N (edge case after backfill changes the distribution), it's prepended to the chip list so the user can still see it active + un-toggle it. **URL-state-resident** via `useSearchParams`: `?genre=` / `?theme=` / `?perspective=` — composable with each other + the existing `?sort=` param; cleared on `statusParam` change (same React Router 6 reuse-across-route-params reset as the existing `platFilter` — the route component doesn't unmount when the param changes, so we manually clear). `applyFilters` extended to compose three `filterByTag` calls after the platform filter, before sort. Filters intersect: `Backlog × RPG × Horror × First-person` is a natural query that the chip rows + URL state now express directly. **Tests:** 11 unit in new `apps/web/src/lib/__tests__/pickTopTags.test.ts` pinning every contract row (count-desc + name-asc ordering, cap parameter, per-dimension reads, empty edge cases, null/undefined no-op on `filterByTag`, dimension-specific filtering across genre vs theme vs perspective). Targeted vitest **44/44 pass** across `pickTopTags` (11) + `LibraryDesktop.test.tsx` (1 existing shelf-headers regression) + `api-invalidation.test.ts` (14) + `Dashboard.bento.test.tsx` (18). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** LibraryDesktop chunk 16.09 → 17.29 kB (+0.38 kB gzipped), LibraryMobile 14.35 → 15.53 kB (+0.38 kB gzipped). Modest. **Still deferred to B-IGDB-3b2** (would complete the full B-IGDB-3 ambition): (i) primary-lens routes `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug` per PAGES_PLAN §4.4.1 with proper URL design (e.g. `/library/by-genre/RPG?perspective=First-person` — primary vs. secondary precedence); (ii) browse-by panel on Library overview surfacing top-N per dimension as navigable rows. These need Library page IA changes + URL state design rules; structurally distinct from chip strips on the existing Status lens. No `AGENT.md` entry — B-IGDB-3b1's decisions constrain the secondary-filter shape specifically. **Sticky property for B-IGDB-3b2 implementor:** the helper file (`pickTopTags.ts`) is dimension-agnostic by design — extending `TagDimension` to include Collection / Developer / Publisher / Series (post B-Stash-4 + B-Stash-5) is a one-line union addition + adding the dimension's array accessor inside the switch. The chip-row JSX in both Library components is pure repeat-for-each-dimension; new lenses thread in without restructuring. Same goes for the URL state — `setTagFilter(dimension, value)` already generalizes via the dimension arg.
- **B-IGDB-3 partial — IGDB-tag triple foundation + Dashboard breakdown 3-tab (2026-05-30, this commit).** Fifth PR derived from `docs/PAGES_PLAN.md` v2 spec §2 B-IGDB-3 + §7 Dashboard. Ships the **foundation + Dashboard consumer**; Library consumer (filter chip strips + primary-lens routes + browse-by panel) split into a follow-up **B-IGDB-3b** to keep the foundation PR reviewable. Hoard now treats IGDB's `genres + themes + player_perspectives` as **three distinct filterable dimensions** per PAGES_PLAN §4.4.1 (Stash merges them into a single "tags" axis; Hoard keeps them separate because they discriminate differently — "First-person × Horror" is a meaningfully different query than "Action RPG"). **Schema:** new migration `20260530120000_game_themes_perspectives` adds `Game.themes String[] @default([])` + `Game.playerPerspectives String[] @default([])` via hand-written SQL with `ADD COLUMN IF NOT EXISTS` for prod idempotency. Apply via documented `prisma db execute --file ... migration.sql && prisma migrate resolve --applied 20260530120000_game_themes_perspectives` recipe (pgbouncer-safe). **IGDB service** — `mapToSearchResult` + 3 `IgdbUpcomingRelease` factories + `getReleaseDetails` all extended to map `themes` + `player_perspectives` from the IGDB payload. All 7 query strings in `apps/api/src/services/igdb.ts` extended to include `themes.name, player_perspectives.name` in the `fields` list. **All `Game.upsert` call sites threaded the new fields through:** `syncRunner` (main sync path) + `wishlistImport` (Steam wishlist) + `routes/upcoming` (wishlist toggle) + `routes/games` (remap). `WishlistRelease` itself NOT extended — its snapshot doesn't carry themes/perspectives because Releases cards don't display them today; the wishlist-scope branch + `useUpcoming` IGDB-outage fallback default to `[]`. **Types extended:** `Game.themes` + `Game.playerPerspectives` (required `string[]` on the type; defaults to `[]` server-side); `IgdbSearchResult.themes` + `playerPerspectives`; `IgdbUpcomingRelease.themes` + `playerPerspectives`; `DashboardStats.themes` + `playerPerspectives` (same `{name, count}[]` shape as `genres`). **Backfill script** at `scripts/backfill-game-tags.ts` (~95 lines) — re-fetches existing Game rows from IGDB via `getGame(igdbId)`, persists themes + perspectives; throttled at ~3 req/s (under IGDB's 4 req/s budget); resumable (skips rows that already have non-empty values); supports `--dry-run`. Not auto-run; Andrea triggers post-deploy. **Dashboard consumer:** server iterates `aggUserGames` once tallying genres + themes + perspectives in one pass (extends the existing `select` with one extra column per axis; no new round-trip). Top-N capping (`TOP_GENRES = 5`) applied uniformly across all three dimensions. Both `DashboardDesktop` + `DashboardMobile` breakdown cards now render a **3-tab strip** (`genre · theme · perspective`) bound to a single parent-owned `breakdownTab` state. Active tab uses `.chip.solid.amber` (same pattern as DASH-PR2 `PeriodToggle`); disabled state when a dimension is empty in the user's library (gives an empty-data hint pointing at the backfill script). Card marker line dynamically updates: `// top genres` / `// top themes` / `// top perspectives`. **Server `/api/games` extended** with `?genre=` + `?theme=` + `?perspective=` Zod-validated query params (50-char length cap matches IGDB's longest tag like `"Open World"`); Prisma `array.has` filter assembly composes them with the existing `status` + `q` + `platform` filters under a unified `gameFilter` projection — **API surface is ready** for B-IGDB-3b to wire the Library chip strips. Frontend client `GamesParams` extended in lockstep. **Deferred to B-IGDB-3b** (will ship as a follow-up): (i) Library **secondary filter chip strips** for genre/theme/perspective on the existing `/library/:status` Status lens — consumer of the now-ready server filter params; (ii) **primary-lens routes** `/library/by-genre/:slug` + `/library/by-theme/:slug` + `/library/by-perspective/:slug` per PAGES_PLAN §4.4.1; (iii) **browse-by panel** on Library overview surfacing top-N per dimension. The Library rework is structurally bigger than the Dashboard consumer (interacts with existing platform filter + sort + find input; needs URL state design + composability rules); shipping it standalone in a follow-up keeps this PR reviewable. **Tests:** +4 frontend in `Dashboard.bento.test.tsx` covering the breakdown 3-tab strip (renders 3 tabs with correct labels; defaults to `genre` and shows the genres series; clicking theme swaps body to themes; disabled state when `playerPerspectives` series is empty in the fixture). 9 fixture-factory backfills with `themes: []` + `playerPerspectives: []` defaults across `bucketing.test.ts`, `empty-state.test.tsx`, `mobile.test.tsx`, `primitives.test.tsx`, `pickWishlistedPlatformChips.test.ts`, `ReleasesRecentDesktop.test.tsx`, `AddGameModal.test.tsx`, `RemapGameModal.test.tsx`, `igdbMatch.test.ts` — existing backend mocks for `Game.upsert` use loose `objectContaining` matchers so they don't break on the new fields. **18/18 Dashboard.bento + 112/112 releases + 39 backend suites / 589 tests pass.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** DashboardDesktop chunk 16.21 → 17.45 kB (+0.39 kB gzipped — 3-tab UI logic), DashboardMobile 12.24 → 13.32 kB (+0.34 kB gzipped). Library bundles unchanged. No `AGENT.md` entry — B-IGDB-3 partial's decisions constrain the IGDB-tag triple specifically; the "keep the three axes separate vs. Stash's merged tags" framing was already locked at the spec level in PAGES_PLAN §2. **Pre-deploy operational task:** apply migration via `npx prisma db execute --file packages/db/prisma/migrations/20260530120000_game_themes_perspectives/migration.sql --schema packages/db/prisma/schema.prisma` then `npx prisma migrate resolve --applied 20260530120000_game_themes_perspectives --schema packages/db/prisma/schema.prisma`. Then `npx tsx scripts/backfill-game-tags.ts --dry-run` to sanity-check sample IGDB output + rate-limit behaviour before a real backfill run. **Sticky property for B-IGDB-3b:** the chip-strip Library consumer is the smaller half of the deferred work; the primary-lens routes + browse-by panel is structurally bigger and should be its own PR (B-IGDB-3b2). The server filter surface is live so chip strips can be wired without further backend changes.
- **REL-PR1 — Releases per-platform wishlist context chips (2026-05-30, this commit).** Fourth PR derived from `docs/PAGES_PLAN.md` v2 spec §5.4 #2 + §5.6; resolves the CM12 follow-through gap on the Releases surface. When `UserGame.wishlistedPlatforms ⊊ game.platforms`, replaces the generic IGDB platform array on Releases cards with the user's wishlist subset prefixed `// wishlisted:` (desktop) / `wish:` (mobile) / `wishlisted on` (HeroCountdown label). Empty `wishlistedPlatforms` OR full-set match falls back to today's generic rendering — no narrowing to surface. **What shipped:** `packages/types/src/index.ts` — `IgdbUpcomingRelease.wishlistedPlatforms: string[]` added (required field). `apps/api/src/services/igdb.ts` — three factories (`getUpcomingReleases`, `getRecentlyReleased`, `getReleaseDetails`) default `wishlistedPlatforms: []`. `apps/api/src/routes/igdb.ts` — `userGameMap` widened from `Map<igdbId, userGameId>` to `Map<igdbId, { id, wishlistedPlatforms }>`; one extra column in the Prisma `select` on an already-running query (no new round trip). Both wishlist-scope and non-wishlist-scope branches thread the field. `apps/api/src/routes/releases.ts` — `wishlistRowToUpcoming` takes `wishlistedPlatforms` as a 3rd arg; both `starred` and `hyped` branches join against UserGame and thread the field. `apps/web/src/components/screens/releases/utils.ts` — new `pickWishlistedPlatformChips(release)` helper returning `{ mode: 'generic' | 'wishlist', platforms: string[] }`. Callers don't need to inspect `mode` to know which array to map over — `platforms` is always the array to render; `mode` only changes the prefix label + amber styling. **Edge cases pinned in tests:** empty wishlistedPlatforms → generic; full-set match → generic (no narrowing to surface); strict subset → wishlist returning the subset; data-drift (user wishlisted on a platform NOT in IGDB platforms — e.g., a platform they don't have synced) → wishlist mode, render `wishlistedPlatforms` directly (OQ-REL-3 v1 lock — the "you wishlisted on a platform you don't own" UX is a Library / GameDetail concern, not a Releases-card concern); empty IGDB platforms + non-empty wishlist → wishlist; order preserved from `wishlistedPlatforms`, not IGDB. **Components updated:** `ReleaseCard` (all 3 variants — wishlist / all / recent) renders `// wishlisted:` amber-prefixed platform-glyph row when scoped; `MobileReleaseRow` shows inline `wish:` prefix on the meta line; `HeroCountdown` swaps the `// platforms` heading for `wishlisted on` (amber) when scoped. **Skipped per spec:** `AgendaRail` (single-line UI, no space for a prefix without redesigning the row — analogy with OQ-REL-2's same call for the Events back-link chip); `WishlistEmptyRecommendation` (recommendations are pre-wishlist by construction, so `wishlistedPlatforms` is empty → helper naturally returns generic; no change needed). `apps/web/src/hooks/useUpcoming.ts` IGDB-outage fallback defaults `wishlistedPlatforms: []` (WishlistRelease doesn't carry the per-platform context, lives on `UserGame.wishlistedPlatforms` which requires the join; acceptable degradation for an outage fallback). **Tests:** 9 unit tests in new `apps/web/src/components/screens/releases/__tests__/pickWishlistedPlatformChips.test.ts` pinning every truth-table row + edge case. +3 visual regression tests in `primitives.test.tsx` (generic when empty / scoped subset / full-set fallback) + 2 in `mobile.test.tsx` (`wish:` prefix when scoped / hidden when empty). 5 existing test-fixture factories backfilled with `wishlistedPlatforms: []` defaults (`bucketing.test.ts`, `empty-state.test.tsx`, `mobile.test.tsx`, `primitives.test.tsx`, `ReleasesRecentDesktop.test.tsx`). **112/112 releases tests pass** + **39 backend suites / 589 tests pass** (unchanged from DASH-PR2; no regressions on igdb / releases route tests). Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** ReleasesDesktop chunk 18.57 → 18.68 kB (+0.04 kB gzipped); ReleasesMobile unchanged; other releases chunks negligible. The helper + chip-row markup are ~600 bytes total. No `AGENT.md` entry — REL-PR1's decisions constrain the Releases card surface specifically. PAGES_PLAN §5.6 updated with landing notes; spec row marked Done. **Sticky property for OQ-REL-3 v2 follow-up:** the data-drift case (`wishlistedPlatforms` contains entries NOT in IGDB's `platforms`) currently renders identically to a strict-subset — that was the v1 lock. If a future product-strategy session decides that "you wishlisted on a platform you don't own" deserves distinct visual treatment (e.g., a `(not yet connected)` parenthetical or a separate chip color), the helper's return shape is ready to extend: add a `drift: boolean` flag or split `mode` into 3 values.
- **DASH-PR3 — Dashboard sync-error alerts strip (2026-05-30, this commit).** Third PR derived from `docs/PAGES_PLAN.md` v2 spec §7.6; first entry into the bento alerts-strip surface (span-12 at top of grid per §7.4) and resolves OQ-DASH-11. Surfaces any connected Platform with `syncStatus === 'error'` as a slim red-bordered chip. **Two design calls locked via AskUserQuestion before write:** (1) **single aggregated chip** for multi-platform errors — lists codes inline at 1-2 errored platforms (`⚠ steam · psn — sync error`), collapses to a count at 3+ (`⚠ 3 platforms — sync error`); one click → `/settings/platforms` where the user can re-paste tokens / re-auth / view the per-platform log. Avoids chip-overflow when many platforms error simultaneously; (2) **not dismissible** — chip auto-disappears when the underlying error clears on the next successful sync. No localStorage state to manage; no risk of the user dismissing-and-forgetting an unresolved error. **What shipped:** new `apps/web/src/components/screens/dashboard/AlertsStrip.tsx` (~75 lines) — pure component returning `null` when there's nothing to surface. Inside the bento grid context spans `gridColumn: '1 / -1'`; outside the grid (mobile) the parent wraps it in a padding container that's also conditionally rendered. Visual treatment: `Icon name="warn"` (red) + `// sync error` (red) + ` · steam · psn` (paper-dim) + `view in settings →` (faint, right-aligned). `border: 1px solid var(--red-dim)` on `var(--ink)` background. Accessibility: `role="region"` + `aria-label="Dashboard alerts"`; chip is a real `<button>` with descriptive `aria-label` (`"2 platforms failed to sync — open Settings"`). Wired into both `DashboardDesktop` (mounted as the first child of the bento grid; grid reflows when component returns null) and `DashboardMobile` (parent-level conditional render avoids carrying blank-space padding when no alerts active). **Tests:** 8 unit tests in new `apps/web/src/components/screens/dashboard/__tests__/AlertsStrip.test.tsx` — null when no platforms / no errored platform; correct copy at 1 / 2 / 3+ errored platforms; click navigates to `/settings/platforms`; **NOT dismissible regression guard** — exactly one button in the strip + no dismiss affordance + no aria-label mentioning dismiss/close/hide; edge case of `syncable=false` with `syncStatus='error'` still surfaces (user might want to re-enable + retry). Plus 2 integration tests in `Dashboard.bento.test.tsx`: alerts strip hidden by default; renders at top of bento as first child when an errored platform is present. **22/22 across both files.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta:** DashboardDesktop chunk 16.10 → 16.21 kB (+0.01 kB gzipped), DashboardMobile 12.01 → 12.24 kB (+0.05 kB gzipped). Negligible. No `AGENT.md` entry — DASH-PR3's decisions constrain the alerts-strip surface specifically. PAGES_PLAN §7.6 updated with landing notes; spec row marked Done. **DASH-series complete** (PR1 + PR2 + PR3 all shipped 2026-05-30) — Dashboard doesn't need further dedicated workstreams per PAGES_PLAN §7.6 sequencing notes; everything else threads in via Q-series / EV-PR3 / Deals / GD-PR3 / B-Hoard-1 etc. **Sticky property for next alerts-strip workstream** (Q-series pending-review / EV-PR3 events-missed / Deals callout): add new chip kinds INSIDE `AlertsStrip.tsx` rather than mounting parallel strip components. The `null`-when-empty contract is what keeps the bento layout collapsing cleanly when no alerts exist; multiple parallel components would each render their own wrapper and break bento spacing. Aggregation pattern (list at 1-2, count at 3+) + chip styling (warn icon · semantic label · `view in settings →` affordance) generalize to other chip kinds with different icons + nav targets.
- **DASH-PR2 — Dashboard time-axis toggle (2026-05-30, this commit).** Second PR derived from `docs/PAGES_PLAN.md` v2 spec §7.6; follows DASH-PR1 (the bento-box layout) the same day. Adds the `// this year / this month / all time` toggle in a **combined `card-progress` (span-8)** that holds completion + achievements as two halves divided by a hairline, with one toggle in the card header. Resolves PAGES_PLAN OQ-DASH-8. **Semantics locked via AskUserQuestion before write:** engagement-scoped — `this year` / `this month` = stats AMONG UserGames whose `lastPlayedAt` falls in the period. Tractable from current schema (no `completedAt` or per-achievement timestamps); intuitive *"your year in games"* framing; matches the activity heatmap's lastPlayedAt-based temporal slice. Achievements card uses the same scope, summing all-time achievement counts on games last-played in the period (not "achievements earned in the period" — we can't know that). **Schema-additive on `DashboardStats`**, not in-place: top-level `completedCount` / `totalGames` / `completionPct` / `achievementsRollup` stay all-time (greeting header reads them); new `period: 'all' | 'year' | 'month'` + `periodStats: { completedCount, totalGames, completionPct, achievementsRollup }` carry the scoped variants. When `period === 'all'` server collapses `periodStats` to mirror all-time, so the frontend has no branching. **What shipped:** `packages/types/src/index.ts` — new `DashboardPeriod` + `DashboardPeriodStats` types; `DashboardStats` extended. `apps/api/src/routes/dashboard.ts` — `parsePeriod(req.query.period)` silently collapses unknown values to `'all'` (toggle UI only emits known values; URL typos shouldn't 400 the page); `periodStart(period, now?)` returns UTC start-of-current-calendar-month / year / null. Refactor: dropped the separate `achievementsRows` query — its `achievementsByPlatform` select rides along on `aggUserGames` since the new period semantics already need `status` + `achievementsByPlatform` + `lastPlayedAt` on the same row. Single iteration over `aggUserGames` now tallies the all-time rollup AND the engagement-scoped variants in one pass. `apps/web/src/components/screens/dashboard/PeriodToggle.tsx` (new) — three-state chip group with `role="radiogroup"` + `role="radio"` semantics; `compact` variant for inline bento card placement. `apps/web/src/hooks/useDashboard.ts` — accepts `DashboardPeriod` arg (default `'all'`). Cache key: legacy `'dashboard'` for default + `'dashboard:year'` / `'dashboard:month'` for scoped variants. `cache.invalidate('dashboard')` prefix-matches all three, so existing mutation invalidation continues to cover the new keys without per-mutation changes. `apps/web/src/lib/api.ts` — `api.dashboard(period?)` omits the query param for default `'all'` (keeps URLs clean + matches server default). **DashboardDesktop + DashboardMobile** both lift `const [period, setPeriod] = useState<DashboardPeriod>('all')` AND `const lastGoodRef = useRef<DashboardResponse | null>(null)` at the parent. Combined `card-progress` mounts the sole `PeriodToggle` bound to that state. Halves read from `stats.periodStats.*`; greeting header still reads top-level all-time. **Andrea-driven iteration after first-pass eyeball** (*"if this is the intended logic then the two widgets don't have to be separated, but unified so the control is shown once"* + *"it reload the whole page"*): (a) merged the v1 split (`card-completion` + `card-achievements`, each span-4, each with its own toggle) into one combined `card-progress` (span-8) with completion + achievements as two halves; **DASH-PR1's row 2 layout updated** to `backlog-picker (4) | card-progress (8)` (was `(4) | (4) | (4)`); when achievements data is absent for the active period, the hairline + right half are hidden and completion fills the card; (b) **fixed the loading-skeleton flash on chip click** — the cache-key change (`dashboard` → `dashboard:year`) momentarily set `data=undefined` and the bento bailed to the loading skeleton; `lastGoodRef` keeps the previous payload on screen until the new one resolves. Toggle chip flips immediately (state is synchronous); numbers update when fetch resolves. **Tests:** +7 backend in `dashboard.test.ts` (default period = all + unknown period silently collapses + period=year denominator + period=month boundary + null-lastPlayedAt excluded + period-scoped achievements rollup + top-level all-time stats unaffected by period) → **39 backend suites / 589 tests pass** (+7 vs DASH-PR1's 582). +4 frontend in `Dashboard.bento.test.tsx` (post-iteration): one shared toggle in `card-progress` (not duplicated per half) + total of exactly one `[role="radiogroup"]` on the page; defaults to `period='all'` and reads from `periodStats` not top-level (sample fixture intentionally diverges the two so the binding source is unambiguous); year-chip click refetches with `?period=year` and both halves of the progress card update together; **no-skeleton-flash regression guard** — bento stays mounted while a new period is in flight, previous values remain visible. Plus 4 in new `api-dashboard.test.ts` (default / explicit-all / year / month URL encoding) + 1 in `api-invalidation.test.ts` (`cache.invalidate('dashboard')` covers all period suffixes regression guard). **48/48 across targeted vitest scope.** Typecheck + lint + rename-rule + production build all clean. **Bundle delta (post-iteration):** DashboardDesktop chunk 15.18 → 16.10 kB (+0.19 kB gzipped), DashboardMobile 11.41 → 12.01 kB (+0.17 kB gzipped), initial JS index 257.05 → 257.08 kB (+30 bytes gzipped, type-only). No `AGENT.md` entry — DASH-PR2's decisions constrain Dashboard specifically. PAGES_PLAN §7.6 updated with landing notes; spec row marked Done. **Sticky property for future DASH workstreams that add period-aware cards:** the toggle owner is the parent (single source of truth). New period-scoped cards either consume the same parent state OR call `useDashboard(period)` themselves — don't introduce a second period-state owner. **AND if a card's data shares the same period scope as another card, merge them rather than duplicating the toggle** — Andrea's lock (the v1 split was the visual mistake caught on first eyeball).
- **DASH-PR1 — Dashboard bento-box layout rework (2026-05-30, this commit).** First implementation PR derived from the PAGES_PLAN v2 spec (§7.4) and lowest-friction unblocker per the spec's sequencing notes — no new data, just structural rework that every later Dashboard workstream snaps cleanly into. **Two decisions locked via AskUserQuestion** before write: the `// the hoard · in numbers` 8-tile stat grid removed entirely (redundant with sidebar shelf counts + new dedicated cards); the multi-card `// wishlist · dropping soon` panel replaced by a single dominant `NextReleaseCountdown` (span-3) + `see all →` link to `/releases` per spec — Dashboard answers "what's next?" with the next single release; the multi-item view lives on `/releases`. **New file `apps/web/src/components/screens/dashboard/NextReleaseCountdown.tsx`** — compact span-3 card with live 1Hz d/h/m/s tick via `useNow(1000)` + `countdownParts` (mirrors `/releases` HeroCountdown behaviour, narrower footprint). **Inline `BentoCard` wrapper** in `DashboardDesktop.tsx` applies `gridColumn: span N` + `data-bento-span={N}` introspection attr + the existing `.panel` styling. **Desktop bento (12-col):** Row 1 — `now-playing+rotation (span-6)` · `next-release (span-3)` · empty 3-col tail reserved for EV-PR1's next-event countdown; Row 2 — `backlog-picker (span-4)` · `completion-gauge (span-4)` · `achievements-gauge (span-4)`; Row 3 — `genre-breakdown (span-6)` · `hours-by-platform (span-6)` (hours-by-platform slots into the year-in-review reserved half until OQ-DASH-9 ships); Row 4 — `activity-heatmap (span-12)`. Slim alerts strip (span-12) deliberately absent — threads in via Q-series / EV-PR3 / DASH-PR3 / Deals as callouts arrive (progressive disclosure per spec). **Now-playing card gains an active rotation sub-section** when `nowPlaying.length > 1` (compact rows below the hero — API returns up to 3 sorted by `lastPlayedAt desc`; only [0] was being rendered before). **Mobile collapses to 1-col in OQ-DASH-1 #1 fixed-by-importance order** (`now-playing → next-release → backlog → completion → achievements → breakdown → platforms → heatmap`); the 4-tile mobile stat grid removed alongside the desktop 8-tile grid for consistency; the mobile multi-row "dropping soon" list replaced by the same `NextReleaseCountdown` component. **Greeting + bignum + system status hero row stays above the bento grid** (terminal-aesthetic-y personal touch; doesn't fit the span pattern). **GD-PR3 inline edit affordances** (`[+min]` / `[done]` / `[+note]`) explicitly out-of-scope per §7.4 matrix. **Bonus:** fixed a pre-existing React key warning in the system status `.map` — `<>` Fragments don't accept keys on children; moved to `<Fragment key={p.code}>`. **Tests:** 8 new in `apps/web/src/components/screens/__tests__/Dashboard.bento.test.tsx` — bento-grid card presence (data-testid for each card per §7.4 matrix entry); correct `data-bento-span` per §7.4 (size = importance — a swap would mean a real regression); active rotation conditional (renders when N>1, hidden at N=1); `NextReleaseCountdown` surfaces the next wishlist release title + `see all` link; the two removed sections stay removed as anti-regression pins (the 8-tile stat grid + the multi-card wishlist panel are easy to accidentally re-add); mobile asserts the OQ-DASH-1 fixed-by-importance card order via `[data-testid^="card-"]` ordered scan. Targeted vitest scope covering my changes: **46/46 pass** (8 Dashboard.bento + 10 layout + 5 shell-persistence + 7 cache + 13 api-invalidation + 3 useNow). Full vitest run: **37 test files / 460 tests pass cleanly** (including the new Dashboard.bento at 8/8); the 38th file (`RequireActive.test.tsx`) hits the documented pre-existing infra flake — fails with `ReferenceError: document is not defined` in isolation on clean `main` too (jsdom env not being applied to single-file runs in vitest 3.2.4 + threads pool) and hangs the full-suite worker. Predates this workstream; unchanged by my work. **Production build clean** (`✓ built in 2.06s`): DashboardDesktop chunk 15.18 kB (gzip 4.23 kB), DashboardMobile 11.41 kB (gzip 3.48 kB), initial JS `index-CBdT25FT.js` 257.05 kB / 81.73 kB gzipped — modest gzip increase consistent with adding `NextReleaseCountdown` + bento wrapper. Typecheck + lint + rename-rule guard all clean. **No `AGENT.md` entry** — DASH-PR1's decisions constrain Dashboard specifically, not future architectural shape. PAGES_PLAN §7.4 + §7.6 updated with landing notes; spec row marked Done. **Sticky property for the next Dashboard contributor:** add new cards via `<BentoCard span={N} testId="card-foo">`; mobile auto-folds in `[data-testid^="card-"]` order — the test pins both desktop layout + mobile order, so adding cards to the desktop grid in a different position will fail both axes (caught early in CI). Reserve the alerts strip as `span-12` at the top of the grid (no slot today, but render at `gridColumn: '1 / -1'` when callouts arrive in DASH-PR3 / Q-series / EV-PR3 / Deals).
- **PAGES_PLAN functional analysis — forward-looking v2 spec across 8 pages (2026-05-29..30, 14 commits).** Doc-only workstream. New canonical planning doc at `docs/PAGES_PLAN.md`. Drilled all 8 surface pages with Andrea's input + pushback throughout: §1 surface inventory + §2 benchmark catalog (5 sources Stash O14 + IMDB + IGDB + HLTB + Hoard-native — 22 stable capability IDs) + §3 GameDetail v2 four-state architecture (S1 released-not-owned / S2 upcoming-not-owned / S3 owned-in-progress / S4 owned-completed with archivist-relic identity moment) + §4 Library composition model (primary lens × secondary filters × find-within-lens × sort; status remains primary; Collections / IGDB-tag-triple genre+theme+perspective / dev/pub/series as orthogonal lenses) + §5 Releases (mostly done after R-series; small enhancements thread into other workstreams) + §6 Events (new top-level surface — IGDB events API; hero countdown reuses Releases pattern; nightly sync + manual refresh + .ics calendar export per OQ-EV-10) + §7 Dashboard (bento-box layout replacing the original 4-zone vertical proposal per Andrea's pushback — 12-column grid with variable card spans; mobile collapses by span-order) + §8 Deals (new top-level surface — ITAD-sourced; locked trusted-reseller allow-list at Humble · Instant Gaming · GMG · Kinguin · CDKeys with official first-party storefronts always shown; physical deals in scope via Amazon Product API; market preference via new `User.marketCode` column; historical-low + trending-down chips; sale-event grouping with `/deals/event/:slug` sub-route). **4 identity rejections captured** (don't reopen): reactive scoring emojis; community-aggregate stats; user-vs-community HLTB pace AGGREGATE on Dashboard (per-game pace on GameDetail stays — single-game contextual, not comparative-across-collection); cross-region price-comparison. **1 identity REVERSAL captured** (with rationale): affiliate-link routing for cost-recovery — Hoard runs at a cost (Railway + IGDB + ITAD + Vercel); affiliate revenue from storefronts where the user was already going to buy is cost-recovery, not monetisation-driven product behaviour. Identity boundary explicitly preserved: no buy-promoting UX, no rank boost for higher-commission storefronts, no hiding of non-affiliated storefronts; trusted reseller allow-list is curated for "what Andrea actually buys from," not "where Hoard earns most." **Mirrored as AGENT.md decisions #45 (affiliate cost-recovery boundary) + #46 (storefront taxonomy — Tier 1 first-party always-show, Tier 2 reseller allow-list, grey-market never).** Workstream inventory the spec implies (in rough sequencing-friendly order): DASH-PR1 bento layout (lowest-dep unblocker) · DASH-PR2 time-axis toggle · DASH-PR3 sync-error banner · REL-PR1 per-platform wishlist context chips · B-IGDB-3 genre+theme+perspective filter (lightest "real feature") · DEALS-PR1 foundation + Dashboard callout + GameDetail S1 price-offers card (shared component) · EV-PR1 Events foundation · GD-PR1 GameDetail v2 route migration + S1 · GD-PR2 S2 + countdown + media · GD-PR3 S3 enrichments (sub-status / rating / pace / manual playtime) · GD-PR4 S4 enrichments + archivist relic (needs OQ-GD-13 visual design first) · EV-PR2..4 / DEALS-PR2..4 / GD-PR5 placeholders fill · B-Stash-3 rating column · B-Stash-4 personal Collections · B-Stash-5 Developer/Publisher/Franchise reference entities · B-Hoard-1 archivist-relic visual design · Q-series `UserGame.needsReview` flag · R-series remap durability. PAGES_PLAN is **the source of truth for the v2 ambition**; per-workstream `*_PLAN.md` docs spin up when each PR opens.
- **Admin-IA redesign — sub-routes + sidebar + delete-feedback (2026-05-29).** `/admin` split from a 1300-line `AdminScreen.tsx` monolith into 5 sub-routes under a `200px` sidebar nav: `/admin/pending` / `/users` / `/codes` / `/feedback` / `/events`. `/admin` (no sub-section) redirects to `/admin/users` via React Router's index route. Each section is a lazy-loaded chunk; `AdminScreen.tsx` is now ~40 lines of route gate (auth + breakpoint + admin-flag) that renders `<AdminLayout />` which provides the sidebar + `<Outlet />`. **New `apps/web/src/components/screens/admin/` directory:** `AdminLayout.tsx` (sidebar w/ count badges per section + global `[refresh]`), `AdminPending.tsx` (pending access requests + per-row `[generate code for X]`), `AdminUsers.tsx` (filter chips + search + sort cycle + delete-user modal, URL state preserved as `?filter=&sort=&q=`), `AdminCodes.tsx` (invite codes list + `[+ generate code]` CTA moved from the global top-bar to the codes-section header), `AdminFeedback.tsx` (feedback w/ mark-read toggle + NEW `[delete]` affordance), `AdminEvents.tsx` (telemetry feed). Shared primitives extracted to `admin/shared.tsx` (`SectionHeader`, `EmptyLine`, `ErrorBlock`, `LoadingLine`, `MobileFallback`, `NotFoundView`, `noteLabel`, `relativeTime`, `USER_ROW_GRID`, filter/sort types). Mobile fallback preserved per I-D3 — admin stays desktop-only. **Sidebar count badges:** users / pending (amber when >0) / codes / feedback (green when unread >0) / events (∞ — telemetry has no terminal). **Delete-feedback flow:** new `DELETE /api/admin/feedback/:id` route (admin-only via the existing requireAdmin gate; 204 on success, 404 if gone) + `api.admin.deleteFeedback(id)` client method with NARROWER cache invalidation than `deleteUser` (drops only `admin:feedback`, not the full `admin:` prefix — deleting a feedback row can't orphan an InviteCode or change the users list). `ConfirmModal` gains a `'delete-feedback'` variant (4th variant alongside `delete-account` / `wipe-library` / `delete-user`; keyword `DELETE`, feedback-specific copy). `[delete]` button on FeedbackRow uses `stopPropagation` so it doesn't toggle row-expand. **App.tsx routing:** the parent `<AdminScreen>` route wraps 5 children + an `index` redirect; sub-routes are lazy-loaded so each section ships as its own chunk. **Sidebar.tsx unchanged** — still points at `/admin` which redirects to `/admin/users` via the index route. **Tests:** AdminScreen.test.tsx (40 tests, +1) — `renderScreen()` helper now takes a path arg defaulting to `/admin/users`; rewrote 2 cross-section tests as sidebar-nav tests (count-badge presence, empty-state ∞ for events); moved CTA-trigger tests to `/admin/codes`; moved pending-row tests to `/admin/pending`. AdminScreen.feedback.test.tsx (9 tests, +3) — new delete-affordance coverage (modal opens with DELETE keyword copy / typing DELETE unlocks confirm + calls api.admin.deleteFeedback / clicking [delete] doesn't toggle row-expand). AdminScreen.events.test.tsx (6 tests, unchanged shape). ConfirmModal.test.tsx (+3) — delete-feedback variant copy + uppercase input + confirmed-gating. api-invalidation.test.ts (+1) — deleteFeedback narrow invalidation. admin.feedback.test.ts backend (+2) — DELETE happy-path 204 + 404-when-gone + non-admin-gating in the existing requireAdmin describe block. **Final test count: 39 backend suites / 582 tests passing** (+4 from M3's 578); **83 web tests passing** across the touched files. Typecheck + lint + production build all clean (5 fast-refresh warnings on `admin/shared.tsx` matching the existing accepted pattern in `useSearchModal.tsx` — non-blocking, mix-of-helpers-and-components is a known convention). Production bundle: AdminLayout (~6 KB), each section page (~2-5 KB gzipped) — splits cleanly across lazy chunks rather than the prior monolithic ~25 KB. **Known-gap entries closed in this commit:** "Admin page IA structurally fatigued" (2026-05-21) + "No delete capability on admin feedback rows" (2026-05-21) — both struck through below.
- **M3 post-deploy hotfixes — Moon API scope list + id_token bearer (2026-05-29, commits `3393fd6` + `2b9967f`).** Andrea's live smoke test against his real Switch surfaced two 403 forbidden failures on `fetchOwnedDevices` that the WIP commit's probe spike never caught — the probe verified the OAuth chain end-to-end (session_token → access_token → NA `/users/me`) but never actually called any Moon endpoint, so scope correctness AND bearer-token-type correctness were both unverified. **Hotfix 1 (`3393fd6`):** the 5-scope subset (`openid`, `user`, `moonDailySummary`, `moonMonthlySummary`, `moonUser:administration`) was too narrow. Replaced with the canonical 13-scope set from pynintendoauth's `KNOWN_NINTENDO_SERVICES["54789befb391a838"].scopes`. Critical missing scope: `moonOwnedDevice:administration` — exactly what `fetchOwnedDevices` authorizes against. The WIP comment claiming "Nintendo no longer grants the full 13-scope list" was a misdiagnosis. **Hotfix 2 (`2b9967f`):** even after re-authing under the full scope set, Moon still 403'd. Real bug — **Nintendo's Moon API validates the `id_token`, NOT the `access_token`.** Confirmed against pynintendoauth's `access_token` property which returns `f"Bearer {self._id_token}"` (confusingly named getter — but the wire behavior is unambiguous). The NA-account API at `api.accounts.nintendo.com` IS access_token-bearer (which is why `getNintendoAccountUser` worked and the nickname came back); the Moon API at `app.lp1.znma.srv.nintendo.net` is id_token-bearer. Two endpoints, two different tokens. **Code changes:** added `idToken: string` to `NintendoCredentials`; `moonHeaders` / `getNintendoDevices` / `getNintendoLatestMonthlySummary` / `syncNintendoLibrary` all take/use idToken; `ensureFreshNintendoCredentials` force-refreshes when `idToken` is missing (migration path for the legacy Platform row — Andrea's existing creds had only `accessToken`; first sync after deploy pulled a fresh pair, no disconnect/reconnect needed). +1 backend test (force-refresh when idToken missing); existing tests updated to assert id-token-bearer behavior. **Live end-state (2026-05-29 15:22):** `sync ok in 2.3s`, Moon API authoritative. Library count pending — newly-paired console takes ~24h for Nintendo to generate the first daily summary; recheck 2026-05-30. **Sticky property recorded as AGENT.md decision #44:** probe spikes for new platform integrations MUST end by hitting a representative real-target endpoint, not just verify the OAuth chain. OAuth correctness ≠ API correctness; both bugs were directly attributable to the probe stopping at the OAuth chain.
- **M3 — Nintendo Switch auto-sync (complete 2026-05-29).** Fourth and final PR of the sync-expansion workstream (M-series, plan at `docs/SYNC_EXPANSION_PLAN.md`). Backend WIP in commit `2bb75f0` (2026-05-28); this commit ships the frontend, tests, migration apply, and doc closeouts. Nintendo doesn't publish a public games API — instead Hoard uses the Parental Controls "Moon" API: the user pairs their Switch with their own Nintendo Account as the parent (Nintendo allows a single account to be both parent and child), and the Moon endpoints surface per-title playtime + last-played dates. **Schema:** `Game.nintendoTitleId String? @unique` (16-character hex application IDs — opaque, same shape as Epic's catalogItemId; NOT numeric like Steam/PSN/Xbox/itch). Migration `20260528170000_nintendo_title_id` applied via `prisma db execute` + a Node `$executeRaw` migration-record insert (the documented pgbouncer fallback recipe — `migrate resolve` continues to have the advisory-lock hang risk and we bypass it by writing the `_prisma_migrations` row directly). **Backend (~462 lines in `apps/api/src/services/platforms/nintendo.ts`):** full Nintendo Account OAuth + PKCE chain. `generateNintendoPkce` produces an RFC 7636 verifier + S256 challenge; `getNintendoAuthUrl` builds the authorize URL with the Parental Controls public client ID (`54789befb391a838` — same across nxapi, pynintendoparental, MoonControl); `extractSessionTokenCode` parses both `npf<client_id>://auth#session_token_code=...` redirect URLs AND query-string variants AND bare-code pastes (the JWT shape requires the regex to include `.` characters — initial probe bug came from a too-strict character class); `exchangeNintendoSessionTokenCode` exchanges the one-shot code for a long-lived (months) session_token via `accounts.nintendo.com/connect/1.0.0/api/session_token`; `exchangeNintendoAccessToken` swaps session_token → 15-min access_token at the JSON `/api/token` endpoint using `grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer-session-token` — Nintendo gates on a Dalvik User-Agent at this endpoint (any other UA gets 403); `ensureFreshNintendoCredentials` is the identity-equality refresh pattern from GOG/Epic — caller checks `fresh === creds` to decide whether to persist; `getNintendoAccountUser` / `getNintendoUsername` fetch the NA profile via `api.accounts.nintendo.com/2.0.0/users/me` with the NASDKAPI UA (different host, different UA from the Moon API); `getNintendoDevices` calls Moon `/v2/actions/user/fetchOwnedDevices` for the user's paired consoles; `getNintendoLatestMonthlySummary` returns `null` on 404 (newly-paired consoles take ~24h to produce the first daily summary — silent return, no error); `syncNintendoLibrary` orchestrates the lot: list devices → per-device monthly summary → aggregate per-applicationId playtime across players (parent + child playing the same game on the same console roll up to one row) and across devices (multi-Switch user combines into one row), takes `max(lastPlayedAt)`. Per-device failures don't fail the whole sync — they're caught at the loop and logged. **Moon API version constants** (`ZNMA_VERSION='2.4.0'`, `ZNMA_BUILD='660'`, `ANDROID_OS_VERSION='34'`, `DEVICE_MODEL='Pixel 4 XL'`) mirror pynintendoparental's `const.py` (the actively-maintained Python lib that powers Home Assistant's Switch integration). Nintendo gates the Moon API on an `(X-Moon-Os, version, build)` server-side allowlist that shifts every ~2-3 months when Nintendo ships a new Parental Controls app update. Quarterly maintenance tax — when sync starts returning `errorCode=update_required` or clustered 401s around a date, refresh from `https://github.com/pantherale0/pynintendoparental/blob/main/pynintendoparental/const.py` and redeploy. **IGDB resolution:** `getGameByNintendoTitleId` wraps the shared `getGameByExternalUid('nintendo.com', uid)` helper from N-FIX. syncRunner cascade extended: `steamAppId → psnConceptId → xboxTitleId → gogAppId → itchGameId → epicCatalogItemId → nintendoTitleId → title-search → localization`. P2002 collision recovery extended to handle nintendoTitleId. **Routes:** `GET /api/platforms/nintendo/auth-url` (generates fresh PKCE pair per call + state token, returns `{ url, verifier, state }` — the frontend stores the verifier in component state and posts it back alongside the pasted redirect URL); `POST /api/platforms/nintendo/connect` (accepts `{ redirectUrl OR code, verifier }`, extracts the session_token_code, exchanges through the full auth chain, fetches the NA profile, persists `{ sessionToken, accessToken, naId, expiresAt, username? }` on Platform.credentials, returns 400 BEFORE any DB write if Nintendo rejects the code — same validate-then-persist pattern as Epic/Itch); NT branch in `POST /platforms/:code/sync` uses `ensureFreshNintendoCredentials` with the GOG-style merge-on-refresh persistence; NT added to all `validCodes` lists; NT in username backfill block; NT in `/credentials` reveal endpoint exposes `naId` only (session_token is too sensitive). **Frontend:** new 7-step `NintendoGuidedFlowDesktop` + `NintendoGuidedFlowMobile` lazy-routed at `/settings/platforms/nt/connect`. Steps: (1) explainer with parent-as-own-child pairing diagram, (2) install Parental Controls app — App Store + Play Store deep links, (3) pair Switch with own Nintendo Account as parent, (4) open auth URL — desktop renders a 220×220 QR code (white-on-transparent, via the `qrcode` npm package, mocked to a fake data URL in tests since canvas doesn't render in jsdom) for the desktop-to-mobile sign-in handoff; mobile just shows the tap-to-open link, (5) copy the npf:// redirect URL from the "this site can't be reached" error page — explicit copy framing per the WIP commit's UX lesson ("first round of UX copy hand-waved this and confused testers; ship the explicit version"), (6) paste into Hoard, (7) success. Skip-ahead link on steps 2 and 3 jumps straight to step 4 for returning users who've already paired. Auth URL + verifier fetched lazily on first reach of step 4 — the PKCE pair doesn't sit around if the user gets stuck on the explainer. `api.nintendoAuthUrl()` + `api.connectNintendo({ redirectUrl, verifier })` added with cache invalidation. PlatformDetailDesktop + PlatformDetailMobile + SettingsDesktop + SettingsMobile all flipped NT from `syncable: false → true` / dropped the `unsupported` branch — the M1 sticky property (generic `info.connectPath` fallback in `ConnectButton`) means NT works through the existing route-based ConnectButton without a per-platform branch. `qrcode` + `@types/qrcode` added to apps/web devDeps (~17 transitive packages); production bundle adds ~41 KB / ~14.6 KB gzipped as the lazy-loaded `NintendoGuidedFlowDesktop` chunk (not in initial JS). **Tests:** +32 backend `nintendo.ts` (PKCE generation + auth URL + extract × 4 + session_token exchange × 3 + access_token exchange × 2 + computeExpiresAt + ensureFresh × 2 + NA profile × 2 + getUsername × 4 + getDevices × 3 + getMonthlySummary × 2 + syncLibrary × 7) + 11 platforms route (auth-url × 2 + connect × 7 + credentials reveal × 2) = +43 backend = **39 backend suites / 578 tests passing** (+44 from M2's 534). +6 frontend `NintendoGuidedFlow` (lazy fetch / link + QR / error / skip-ahead / save happy / save error) + 1 api-invalidation for `connectNintendo`. Typecheck + lint + rename-rule guard + production build all clean. **Hard Rule #6 amended** per M-D14: rewritten from "Nintendo and Epic are manual-only" to "Manual-add is the fallback, not the rule" — all 7 platform codes now have auto-sync paths. AGENT.md decision added for the Moon API quarterly version-constant maintenance tax (architectural-shaped: constrains the maintenance shape of any M-class platform integration). **Q1 resolved** during the WIP probe: the Moon API monthly_summaries response includes a `shopUri` per title pointing at `nintendo.com/store/...` URLs, but IGDB's external_games coverage for Switch titles is sparse; M3 stores `nintendoTitleId` (opaque 16-char applicationId) as the stable join key and falls through cleanly to title-search + localization for the long tail. **Q4 resolved** during the probe: a single Nintendo Account can pair as both parent and child of its own console — captured in the guided-flow copy as "you'll act as your own guardian." **Two sticky properties for future reference:** (1) When a future platform integration uses a custom URL scheme redirect that browsers can't follow (like npf://, ms-windows-store://, etc.), be explicit in the UX about the "site can't be reached" framing — testers got confused otherwise. (2) Nintendo's NA API (`api.accounts.nintendo.com`) and the Moon API (`app.lp1.znma.srv.nintendo.net`) use different User-Agent strings — `NASDKAPI; Android` for NA, `moon_ANDROID/2.4.0 (com.nintendo.znma; build:660; ANDROID 34)` for Moon. Mixing them gets 401/403. **Pre-deploy operational gotcha:** Moon API version constants need a refresh every ~2-3 months — `errorCode=update_required` is the canary; source of truth is pynintendoparental's const.py.
- **M2 — Epic Games Store auto-sync (complete 2026-05-28).** Third PR of the sync-expansion workstream (M-series, plan at `docs/SYNC_EXPANSION_PLAN.md`). Epic doesn't issue OAuth credentials for third-party library aggregators, so we use the publicly-known "Fortnite Android" client credentials — same pattern as GOG's Galaxy desktop client (env-vars-only: `EPIC_CLIENT_ID` + `EPIC_CLIENT_SECRET`). User flow: open Epic login URL → sign in → Epic redirects to a page rendering JSON with `authorizationCode` → user pastes URL/JSON/bare-code → server exchanges for access + refresh tokens → persists. **Schema:** `Game.epicCatalogItemId String? @unique` (opaque hex string, NOT Int — Epic's catalog uses hex IDs unlike steamAppId/psnConceptId/xboxTitleId/itchGameId). Migration `20260528160000_epic_catalog_item_id` applied via `prisma db execute` + Node `$executeRaw` migration-record insert (same fallback recipe as M1). **Backend:** new `apps/api/src/services/platforms/epic.ts` (~330 lines) — `exchangeEpicAuthCode` (POST to account-public-service-prod03 with Basic Auth + grant_type=authorization_code), `refreshEpicToken` (same endpoint, grant_type=refresh_token — Epic rotates refresh tokens on every refresh, must persist new one), `ensureFreshEpicCredentials` (identity-equality refresh pattern from GOG — caller checks `fresh === creds` to skip DB write), `getEpicUsername` (fail-silent via `/account/api/public/account/{accountId}` → `displayName`), `syncEpicLibrary` (cursor-paginated `library-service.live.use1a.on.epicgames.com/library/api/public/items`). Title resolution via secondary `catalog/.../bulk/items` batch call — Epic's library endpoint returns opaque `appName`/`catalogItemId` but no titles; bulk catalog endpoint takes up to 50 IDs per call, namespace-grouped. New `getGameByEpicCatalogItemId` in `igdb.ts` wraps the shared `getGameByExternalUid('store.epicgames.com', uid)`. syncRunner cascade extended: `steamAppId → psnConceptId → xboxTitleId → gogAppId → itchGameId → epicCatalogItemId → title-search → localization`. P2002 collision recovery extended for epicCatalogItemId. **Routes:** `GET /api/platforms/epic/auth-url` + `POST /api/platforms/epic/connect` (validate-then-persist — exchange code BEFORE DB write so a bad code → no half-written Platform row); EP added to `validCodes` for sync endpoint; sync branch uses `ensureFreshEpicCredentials` with GOG-style merge-on-refresh persistence; username backfill block covers EP. **Frontend:** new 5-step `EpicGuidedFlowDesktop` + `EpicGuidedFlowMobile` lazy-routed at `/settings/platforms/ep/connect`. `extractCode()` handles three paste shapes (bare code, full URL, JSON blob — Epic's redirect renders JSON so the user often pastes the whole thing). `api.epicAuthUrl()` + `api.connectEpic(code)` added with cache invalidation. PlatformDetail* + Settings* + Mobile platforms list all flipped EP from `syncable: false → true` across 5 UI surfaces. `PlatformDetailDesktop.ConnectButton` already had a generic `info.connectPath` fallback from the M1 fix — EP works through it without a per-platform branch (the sticky property paying off as M-series scales). **Tests:** +23 backend (4 exchange + 2 refresh + 3 ensureFresh + 4 username + 5 sync + 5 platforms-route: auth-url / connect-happy / missing-code / Epic-rejects / null-username) + 6 frontend (5 EpicGuidedFlow + 1 api-invalidation). Final: **38 backend suites / 534 tests passing** (+23 from M1's 511); typecheck + lint clean. **Pre-deploy env-var setup required:** Production needs `EPIC_CLIENT_ID` + `EPIC_CLIENT_SECRET` on Railway → API service → Variables before connect works (same pattern as GOG). Q2 from plan §5 (Epic public-client ID stability) resolved by env-vars-only — if Epic rotates credentials, Andrea updates env vars + redeploys, no code change. **Sticky property for M3:** Epic's redirect-renders-JSON pattern is more user-friendly than GOG's empty-page-with-code-in-URL — the JSON shows the user exactly what to copy. When designing M3's guided flow, prefer surfaces where the user can see what they're copying.
- **M1 — itch.io auto-sync (complete 2026-05-28).** Second PR of the sync-expansion workstream (M-series, plan at `docs/SYNC_EXPANSION_PLAN.md`). itch.io is the only platform in M-series with a fully sanctioned public API — no OAuth, no reverse-engineering. The user generates a per-account API key from `https://itch.io/user/settings/api-keys` and pastes it into Hoard; the key goes in the URL path (e.g. `https://itch.io/api/1/<key>/me`) rather than a Bearer header. **Schema:** `IT` added to `PlatformCode` enum (DB + `@hoard/types`); `Game.itchGameId Int? @unique` column added. Migration `20260528150000_itch_platform_code` applied via `prisma db execute` + Node `$executeRaw` fallback (the documented `migrate resolve` advisory-lock hang recurred; also surfaced that `_prisma_migrations.migration_name` isn't a unique constraint so `ON CONFLICT (migration_name)` errors with P2010 — use SELECT-then-INSERT). **Backend:** new `apps/api/src/services/platforms/itch.ts` with `validateItchApiKey` / `getItchUsername` / `syncItchLibrary`. `syncItchLibrary` paginates `/my-owned-keys` (50/page, 200ms polite delay, 50-page hard cap = 2500 owned keys ceiling), throws on 401/403 (revoked key); filter-out keys without a game subdoc / missing id / missing title. `getItchUsername` prefers `display_name` over `username` (more recognisable identity); fail-silent per M-D13 — decorative metadata must never block connect/sync. `validateItchApiKey` returns false on any failure mode so the connect endpoint can reject malformed/expired keys before DB write. **IGDB resolution:** new `getGameByItchGameId` in `apps/api/src/services/igdb.ts` wraps `getGameByExternalUid('itch.io', uid)` with 24h cache. syncRunner cascade extended to include itchGameId between gogAppId and title-search; `Game.itchGameId` persisted on upsert with P2002 collision recovery extended. **Routes:** `POST /api/platforms/itch/connect` validates-then-persists (no half-written Platform row on a bad key); IT branch added to `POST /platforms/:code/sync`; IT added to all `validCodes` lists across the platforms.ts route file (status, log, patch, sync, credentials-reveal endpoints all extended); `PLATFORM_NAMES` gains `IT: 'itch.io'`; username backfill block covers IT via `getItchUsername`. **Frontend:** new 4-step `ItchGuidedFlowDesktop` + `ItchGuidedFlowMobile` lazy-routed at `/settings/platforms/it/connect`; `api.connectItch(apiKey)` client method with cache invalidation. `PlatformDetailDesktop` + `PlatformDetailMobile` + `SettingsDesktop` + `SettingsMobile` all extended to surface itch.io in platform maps/lists. `dashboard.ts` `PLATFORM_LABELS` adds `IT: 'ITCH.IO'`; `buildAchievementRows` sort order extends to IT (no achievement support — entry never appears, but type system stays exhaustive). **Tests:** +14 backend (5 `validateItchApiKey` × happy/error/short-key + 3 `getItchUsername` × display_name/username/null + 5 `syncItchLibrary` × pagination/filter/401/missing-key/errors + 4 connect-route × missing-key/rejection/happy/null-username) + 6 frontend (5 `ItchGuidedFlow` happy-path + error + navigate + disabled-state + step-1-link + 1 `api-invalidation` for connectItch). Final: **37 backend suites / 511 tests passing** (+17 from M0's 494); typecheck + lint clean (1 pre-existing warning). **Match-rate caveat documented in M-D9:** most itch.io games are jam entries / hobby releases not in IGDB; the sync's skipped-titles list will be longer than for Steam/PSN/Xbox/GOG. Manual-add still works for those. Q3 from plan §5 (itch.io rate limits) resolved by defensive choice: 200ms inter-page delay + 50-page hard cap suffices for single-user libraries; no rate-limit handling needed.
- **M0 — Per-platform achievements (complete 2026-05-28).** First PR of the sync-expansion workstream (M-series, plan at `docs/SYNC_EXPANSION_PLAN.md`). Foundational data-model fix shipped before the new platforms (itch.io / Epic / Nintendo) get added: cross-platform games like Cyberpunk (44 Steam achievements vs 45 PSN trophies — entirely different sets per game) were having their flat `achievementsEarned/Total/Percent/UpdatedAt` columns overwritten on every sync cycle by whichever platform synced last. **Schema:** new `UserGame.achievementsByPlatform Json @default('{}')` (shape `Partial<Record<PlatformCode, { earned, total, percent, updatedAt }>>`, mirrors `playtimeByPlatform`); 4 flat columns dropped. **Migration** `20260528120000_achievements_by_platform` applied via documented `prisma db execute` + `migrate resolve --applied` recipe (resolve did NOT hang this time, ran cleanly). **Backfill heuristic** uses Postgres `?` operator for JSONB key-presence — more accurate than value > 0 because P-FIX-2 backfills `ST:0` / `PS:0` as a side-effect of trophy/achievement writes. Attributes flat-column data to user's most-recently-synced platform among {ST, PS} that has a key present in `playtimeByPlatform`; falls back to PS when neither key is present (matches legacy `achievementLabel` default). **Q0 ambiguity probe** ran twice (value-based first, then refined to key-presence) — 33/1222 ambiguous (2.7%) where both ST and PS keys exist; self-healing within one sync cycle. **Post-migration verification** via `scripts/check-trophies.ts`: 1222 rows have `achievementsByPlatform` entries (887 ST + 335 PS = 1222 — exact match with probe predictions). **Writers** (`apps/api/src/services/trophies.ts` PSN + `apps/api/src/services/platforms/steamAchievements.ts` Steam) now merge into the new column: `{ ...existing, [PLATFORM]: { earned, total, percent, updatedAt } }`. Steam re-sync preserves `.PS` entry (and vice versa). **CM13 helper** `promoteWishlistOnEngagement(status, achievementsByPlatform)` reads ANY entry — Wishlist + any-platform 100% → Completed; Wishlist + any-platform >0 → OnHold (M-D8 semantics). No platform precedence; engagement signal is engagement signal regardless of source. **GameDetail Desktop + Mobile** render one row per entry via new `buildAchievementRows(abp)` helper in `apps/web/src/lib/utils.ts` (PS first then ST/XB/GG/NT/EP for stable order). Single-platform games look identical to today's rendering; cross-platform games show multiple rows. **Dashboard T6 rollup** changed from Prisma `_sum` aggregate to per-row iteration (single Postgres aggregate query can't sum over JSON keys); cheap because dashboard already loads UserGame rows for other aggregations. **New helpers added:** `apps/api/src/lib/achievementsShape.ts` (defensive `unknown → AchievementsByPlatform` narrowing at the mapper boundary), `scripts/probe-m0-ambiguity.ts` (Q0 probe kept for future M-series migrations). **Test counts:** backend 36 suites / 494 tests passing (was 477+); modified web files (targeted): 12 tests passing. Full web vitest suite hangs at end per documented infra flake (predates M0). Typecheck + lint clean. **No `AGENT.md` entry** — M0's decisions constrain M-series specifically; the per-platform shape is symmetric with `playtimeByPlatform` which doesn't have an AGENT.md entry either.
- **Platform sync resolution overhaul — G-series + username capture + P-series + L-series + N-series + N-FIX (complete 2026-05-27, this commit is doc closeout).** Single sustained session across ~20 commits. Full workstream summary lives in the Current Phase "Previous workstream" block above; abbreviated here for the recent-fixes index. **GOG sync** shipped end-to-end (`b1c1aad` OAuth + `36d9cfc` syncGogLibrary + `e43f22d` orchestration + `c370ec3` GogGuidedFlow paste-code UI), with Galaxy desktop-client credentials kept env-var-only per Andrea 2026-05-27. **Username capture** (`c6f5ce9`) lands "signed in as X" across ST/PS/XB/GG via four fail-silent fetchers wired into both connect endpoints + sync-time backfill paths. **P-series** (`6de5d4c`) extends CM13 auto-promote (Wishlist → OnHold / Completed) to fire on trophy/achievement evidence — closes the gap where PSN's trophy API surfaces engagement near-instantly while `getUserPlayedGames` lags 24-72h on new releases. **P-FIX** (`e496a74`) catches P2002 collisions on `Game.psnNpCommunicationId @unique` (cross-region trophy duplicates) + backfills `playtimeByPlatform.PS = 0` / `.ST = 0` on trophy/achievement match so Library platform filter + cover tag surface games whose only signal is trophies. **Diagnostic instrumentation** progressively extended (`d67fe29` skipped titles → `9aeb3a9` per-title error messages → `a7db864` platform-side IDs per skipped) — activity log now self-diagnoses Sony-side vs IGDB-side gaps without Railway access. **L-series + L-FIX** (`dfe4d0a` + N-FIX commit) — IGDB `game_localizations` tertiary fallback with corrected query syntax (`where name ~` since `search` is rejected on this endpoint per IGDB error message). **N-series** (`7886b11` + migration `20260527140000_game_psn_concept_id` applied via `8fda618` script) introduces stable Sony concept-id → IGDB external_games resolution (mirror of Steam's existing pattern), new `Game.psnConceptId Int? @unique` schema column, refactored `getGameByExternalUid(urlPattern, uid)` helper backing four platform-id lookups. **N-FIX** (`b161b91`) — original N-series filtered IGDB external_games by `category = N` but Andrea's probe diagnosis revealed IGDB is migrating from `category` enum to `external_game_source` schema; LEGO Batman's PSN row (uid=10008537) has `category` NULL, so the filter returned `[]` despite the row existing. Switched to URL-pattern matching (`store.playstation.com` / `store.steampowered.com` / `gog.com` / `microsoft.com`) which survives the IGDB schema migration. **End state on Andrea's live PSN sync (2026-05-27 15:36):** 147/147 imported, 0 skipped, 0 errored (was 138/9 pre-overhaul); Lego Batman shows real 17h playtime + 16/52 trophies + status=OnHold; all 8 previously-skipped Italian-localized titles now resolve to canonical English IGDB titles via concept-id path. Trophy match rate also improved 118 → 124 because newly-resolved Games get their `psnNpCommunicationId` persisted via first-sync title fallback. **Two architectural learnings recorded as Sticky Properties** in the Current Phase block above: (1) IGDB's `category` enum is being deprecated; always use URL-pattern matching for `external_games` filters going forward. (2) Trophy/achievement signals are faster than playtime signals on PSN + Steam — for engagement detection, prefer the trophy API. **Operational gotcha added:** IGDB rate-limiting on cold cache during first N-series sync after deploy — 138 external_games calls hit 4 req/s wall, ~30% 429'd. Subsequent syncs are cache-warm (24h TTL) and run clean. Cache warming is the de-facto throttle. **Two diagnostic scripts kept in repo for future use:** `scripts/probe-igdb-localizations.ts` (probes localizations + alternative_names + games endpoints for a given title) and `scripts/probe-igdb-external-games.ts` (dumps all external_games rows + replicates production query for a given IGDB game id). **Memory entry persisted:** `project_psn_token_reaccess_gap.md` — PSN guided flow only shows the Sony token URL on first connect; no re-access path when NPSSO expires (~60 days). Captured for the post-overhaul platform-polish workstream.
- **F1 manual-add workstream closeout (2026-05-24, prior commit).** Doc closeouts only — no code in this commit. PLAN.md F1 row extended to cover the full PR1 → PR6 sequence (was "PR1 + PR2 Done; PR3–PR6 not yet started" before, now collapses to a single "F1 (PR1 → PR6) Done" row). CLAUDE.md Current Phase rewritten: F1 workstream block collapsed into one "Previous workstream" entry covering all 6 PRs + the PR4 deferral; F1-PR1+PR2-only Previous block removed (subsumed by the new combined block); 6 natural next-step candidates enumerated for the next workstream pick. Old F1 commit chain since the last F1 closeout: **F1-PR3** (3 commits `00bdcf7` + `cb504cf` + `c31d262`, 2026-05-23) — backend `manualPlaytimeMinutes` field (whole-minute int [0, 600000], first-write-only on update) + frontend `[+ more details ▼]` collapsible panel with hours+minutes inputs (closes S7 "playtime: —" downgrade) + times-beaten architectural placeholder + P2 scroll fix capping at `calc(80vh - 160px)` with `thin-scroll` so the panel toggle is reachable on shorter viewports. **F1-PR4 DEFERRED** (2026-05-23) — freeform-fallback (G7) marked "extremely marginal" after a 400 surfaced on the live smoke test. 4 commits hard-reset to `ba58b4b`; schema columns (`Game.igdbId` nullable + `Game.userOriginated`) stay on prod as benign dead weight, providing a half-done foundation if freeform is ever revisited. **F1-PR5** (3 commits `30b4725` + `5902230` + `2fe250f`, 2026-05-24) — extracted `promoteWishlistOnOwnership` helper into `apps/api/src/lib/promoteWishlist.ts` + refactored syncRunner (pure refactor, locks CM13 policy in one place); replaced naive overwrite-on-update upsert with the full 6-row CM12 + CM13 conflict matrix on `POST /api/games/manual` (find → branch-on-state → create or update, with auto-promote on Wishlist→owned transitions per CM13); returns full `UserGameDetail` shape (200 on merge, 201 on create) so the modal can read `userGameId` from `response.id`; closes G5 (silent merge instead of 409). OQ-F1-5: wishlist + create path wraps `userGame.create` + `wishlistRelease.create` in `$transaction` after best-effort `getReleaseDetails` (graceful degradation on null/throw — UserGame still created). **F1-PR6** (`e49b389`, 2026-05-24) — P5 deep-links: `[view game]` → `/game/{userGameId}` and `[+ rate / note]` → `/game/{userGameId}?focus=notes` (reuses post-8 PR A focus-on-notes pattern per OQ-F1-7); both cancel the auto-close timer + close the modal before `navigate()`. Test harness wraps `render()` in `MemoryRouter` + `LocationProbe` so deep-link assertions read the post-navigate URL via `getByTestId('location')`. **Two operational gotchas surfaced during F1-PR5** (already documented in `CLAUDE.md` operational gotchas, cross-referenced here): (1) when rewriting a route handler to use new prisma methods (`findUnique` / `create` / `update`) that the test mock didn't declare, calling the undefined method throws `TypeError` in an async handler, which Express 4 swallows without `express-async-errors` → supertest hangs indefinitely → jest never exits. The fix: kill the hung process, add the missing methods to the `jest.mock('@hoard/db', ...)` block. Lesson: when widening a route's prisma surface, audit the test mock surface in lockstep. (2) The `$transaction` mock pattern from `routes/games.ts` (passing a `tx` proxy with the methods the handler will call) was applied here for the OQ-F1-5 atomic test — `(prisma.$transaction as jest.Mock).mockImplementation(async (cb) => cb({ userGame: { create: txCreate }, wishlistRelease: { create: txWlCreate } }))` lets us assert against both writes inside the transaction independently. **Cumulative test counts at workstream close:** 32 backend suites / 363 tests pass; 58/58 across the directly-affected web surface (AddGameModal + buildPlatformRows + api-invalidation + RemapGameModal); typecheck + lint clean. **No AGENT.md entries** — F1's decisions constrain F1-series specifically; the sticky property worth knowing for the next GameDetail-edit workstream is that `addManualGame` now returns `UserGameDetail` on both 200 and 201 (no refetch needed if the frontend wants the full game shape).
- **F1-PR2 frontend — collector chip strips + GameDetail PLATFORMS + per-platform un-wishlist (2026-05-23, this commit).** Closes the F1-PR2 workstream's frontend half over 3 commits + doc closeouts in this commit. **`5baca70` modal collector chip strips** — mediaType (DIGITAL | PHYSICAL) always visible once a game is selected; condition (LOOSE / CIB / SEALED / REPLICA / GRADED) and region (NTSC-U / NTSC-J / PAL / OTHER) only render when mediaType=PHYSICAL; switching back to DIGITAL or toggling PHYSICAL off hides AND clears stale values so they can't ride along under hidden UI; optional-field passthrough drops condition/region from the addManualGame body entirely unless mediaType=PHYSICAL; `[+ add another]` resets per-entry while platform pin persists. +8 modal tests. **`155b461` GameDetail OWNED ON → PLATFORMS rename + wishlistedPlatforms rendering** — 4 spots renamed (Desktop main panel marker `// platforms` + share-receipt section header `PLATFORMS`; Mobile main panel marker + receipt header). New shared helper `buildPlatformRows(playtimeByPlatform, wishlistedPlatforms)` in `apps/web/src/lib/utils.ts` returns a typed `PlatformRow[]` with owned rows first (playtime desc — matches legacy ordering) and wishlist-only rows after (alphabetical); ownership wins on dedupe (a code in both lists renders as owned). Wishlist-only rows render with an amber `wishlisted` marker; share-receipt PLATFORMS section stays owned-only (SUBTOTAL is a playtime metric; wishlist intent isn't shareable). +6 helper tests. **`35e8199` per-platform un-wishlist endpoint + GameDetail affordance** — `DELETE /api/games/:id/wishlist-platforms/:code`, user-scoped via `findFirst({ id, userId })`, code length-capped 1–32, idempotent (no-op returns 200 with unchanged record and skips the `userGame.update` call so `updatedAt` stays honest on double-taps). +6 backend tests. New `api.removeWishlistedPlatform(userGameId, code)` percent-encodes the code segment and invalidates `game:{id}` + library family caches (`games:` + `shelves:` + `gameCounts` + `dashboard`); +2 cache-invalidation tests covering both invalidation set and URL encoding guard. GameDetail Desktop + Mobile both gain `unwishlistPlatform(code)` — optimistic local update (strip the row immediately) + idempotent server DELETE + refetch reconcile on failure. Desktop renders `[× un-wishlist]`; Mobile renders compact `[×]` for row density. **OQ-S-13 reversal locked in this PR pair:** the Releases-page wishlist toggle does NOT populate `wishlistedPlatforms`; the array is collector opt-in only (manual-add + future GameDetail collector flow). **Test counts:** 22 new tests total across the 3 commits; full backend suite 31/31 / 335/335 pass at PR2 close; typecheck + lint clean. No AGENT.md entry — PR1+PR2's decisions constrain F1-series specifically (CM12 + CM13 + simplified-MediaType are CONCEPTUAL_MODEL-layer, not architecture-layer). Doc closeouts in this commit cover `docs/PLAN.md` Phase Status (new F1 row spanning PR1 + PR2 with the remaining PR3–PR6 sequence enumerated), `CLAUDE.md` Current Phase (rewritten to enumerate the remaining F1 sequence; TL-series demoted from Previous to Earlier; F1-PR1+PR2 takes the Previous slot with full Previous-workstream block), this Recent Fixes entry. Remaining F1 sequence (PR3 → PR6) not yet started per the stop-and-confirm cadence rule.
- **Post-deploy polish (2026-05-21, this commit).** Three small fixes after Andrea eyeballed the deployed F-series / TL-series / R2 cascade: (1) **Settings section renamed "About" → "Feedback"** — the section was always about sending feedback, "About" was vestigial copy from when it was a placeholder; URL path updated to `/settings/feedback`, label updated in `SettingsNav` + `SettingsDesktop` + `SettingsMobile`, function renamed `AboutSection` → `FeedbackSettingsSection` to disambiguate from the admin-side `FeedbackSection` in `AdminScreen.tsx`. (2) **FeedbackForm textarea padding** — `.field` class is sized for single-line inputs; textareas need explicit padding so the placeholder doesn't touch the top edge. Added `padding: '10px 12px'` + `lineHeight: 'var(--lh-normal)'`. (3) **Admin EVENTS section spacing** — INVITE CODES wrapper was missing `marginBottom`; EVENTS section header was sitting flush against the last invite-code row. Added `marginBottom: 32` to match the other sections' wrappers. Two deferred items captured in Known gaps (delete-feedback capability + admin IA redesign) — both belong to the same admin-IA workstream so doing them together once.
- **R2 — PSN mobile guided-flow fix shipped (2026-05-21, this commit).** Lifted `SearchModalProvider` from `apps/web/src/components/layout/AppShell.tsx` to `apps/web/src/App.tsx`, alongside `UserProvider` / `PreferencesProvider`, so it wraps the entire `<Routes>` tree regardless of layout topology. Closes the O12 mobile PSN onboarding block (Gaetano, 2026-05-11) where `PsnGuidedFlowMobile` mounted outside AppShell but used `MobileHeader → useSearchModal()`, throwing the React render error "useSearchModal must be used inside <SearchModalProvider>". The provider is pure context with no rendering output — zero cost when no consumer mounts. Existing tests already mounted the provider themselves (layout, library, releases-recent test files), so no test changes were required; 73/73 web tests across layout + admin + library + releases-recent + feedback regression-green post-lift. Known gap entry struck through; `docs/USER_RESEARCH.md` §6.5 R2 marked Done.
- **TL-series — L1 event-log telemetry complete (2026-05-21, this commit).** Doc-only closeouts on the engineering side; doc churn across `docs/TELEMETRY_PLAN.md` §6 (TL1.5 marked Done with workstream-close notes) + `docs/USER_RESEARCH.md` §6.5 (R3 → Done; R2/R4/`/layers-user-needs` re-listed as next-step pool with the 2026-06-04 earliest-re-trigger date for `/layers-user-needs`) + `docs/PLAN.md` Phase Status (new TL-series row) + this file (Current Phase rewritten; this Recent Fixes entry; two new Known gaps added — auth-chain smoke test + dotenv test-infra workstream candidate — alongside now-explicit dotenv + async-rejection operational-gotcha entries). **TL-series total:** 5-commit single PR (TL1.1 → TL1.5), 27 new tests, every scope-edge guard respected, two operational issues surfaced and documented (entries directly below). No AGENT.md entry — TL1's decisions constrain TL-series specifically. Channel operational end-to-end on production: `UserEvent` table populated by every authed request (session.opened daily-throttled) and by every touchpoint going forward; `/admin` EVENTS section reads via cursor pagination. Natural next-step is R4 (reactive async DMs once telemetry surfaces a question) or `/layers-user-needs` (~2 weeks of telemetry baseline = 2026-06-04 earliest re-trigger).
- **dotenv-via-transitive-import trap (2026-05-21, surfaced during TL1.2).** Adding `logEvent` to `requireUser` pulled in `@hoard/db` transitively, which loaded `apps/api/.env` via dotenv, which overrode `JWT_SECRET` from `'dev-secret'` (the test fallback) to the prod value, which broke `middleware/user.test.ts` (tests sign JWTs against `'dev-secret'`). Local fix: `jest.mock('dotenv/config', () => ({}))` at the top of the offending test. Long-term fix tracked in Known gaps as a test-infra workstream candidate (`apps/api/.env.test` + Jest `setupFiles`). Pattern: **any middleware/route test that transitively imports `@hoard/db` loads the prod `.env`.** Full operational guidance lives in the operational gotchas list below.
- **Express 4 async-handler-rejection plumbing doesn't reach `app.use((err, ...))` without `express-async-errors` (2026-05-21, surfaced during TL1.2).** First attempt at HTTP-level testing of TL1.2's `error.surfaced` touchpoint timed out at 5s because the async route handler's rejection had nowhere to go. Fix: extracted `globalErrorHandler` to a named export in `apps/api/src/index.ts` and invoked it directly in tests. **Caveat:** direct invocation covers middleware behaviour *given* an error object, NOT the plumbing that delivers errors to it. If a future workstream changes error delivery (e.g. adds `express-async-errors`, or migrates to Express 5), don't lean on the direct-invocation pattern as proof of end-to-end error flow. Full guidance in the operational gotchas list below.
- **`prisma migrate resolve` hangs after writing the migration row against pgbouncer (2026-05-21, surfaced during TL1.1 apply step).** Extension of the existing pgbouncer migrate-hang gotcha: `migrate resolve --applied` writes the `_prisma_migrations` INSERT BEFORE it hangs (verified — TL1.1's row landed with `finished_at` from the first hung attempt). **Rule: verify rather than retry** — retrying after Ctrl-C collides on stale prepared statements in the pgbouncer pool ("prepared statement s1 already exists"). Reliable fallback when the CLI is stuck: one-off Node script with `prisma.$executeRaw\`INSERT INTO _prisma_migrations ... ON CONFLICT DO NOTHING\``. Full recipe + threshold for promoting to `packages/db/scripts/` documented in the operational gotchas list below.
- **E2 — welcome integration spec complete with 5/6 coverage + test 5 quarantined under #6 (2026-05-21, this commit).** Closes out the E-series workstream. 4-commit E2 chain (`cb1876a` docs / `1ce96ec` seed / `6ef2e0f` spec+globalSetup+config / `dbaced4` quarantine) plus Flag-A fix-up `1c4d2d9` (project-level `testIgnore` over runtime `testInfo.skip`). 5/6 welcome tests pass against the real API — register, deep-link `?next` preservation, valid-code redemption, open-redirect defense (`%2F%2Fevil.com` → `/`), API error-mapping smoke (INVALID_FORMAT / CODE_NOT_FOUND / CODE_ALREADY_REDEEMED). Test 5 (friction-free flow: request-access → admin generates → redeem) skipped pending #6 — UserProvider `loading → authed` mount race where the redeem-button click is lost before reaching the server. **Product-correctness verified independently** via direct `/api/auth/me` calls returning `hasRequestedAccess: true` with byte-identical session cookies at both step 1 and step 3 of the failing test; failure is in Playwright observation timing during the React mount transition. **§E2.4 verification — all 5 checks green:** suite shape 41 passed / 1 skipped / 2 pre-existing Dashboard flakes; ghost-purge fires on >1h (deletes 1 of 2 fakes), spares <1h; 0 ghost accumulation after 2 back-to-back welcome runs; globalSetup fail-loud on bad `DATABASE_URL_TEST` with `PrismaClientInitializationError` + named-paths trace; InviteCode pool intact (codes 1+2 FK-SetNull-restored with `usedAt` preserved, code 3 unchanged, codes 4+5 untouched, zero admin-generated stragglers). **Investigation artifacts kept in code:** the fixture's always-relogin-as-tracked-email cleanup defense (belt-and-suspenders even though test 5's cookie-swap path no longer exists), Fix 2's explicit status+body capture on admin endpoint failures (cheap insurance — was load-bearing for diagnosing the round-1 Zod 100-char `note` cap that capped `E2 test 5 — generated for {long-fresh-email}` notes), and an issue #6 with full diagnostic trail (4 verbatim `[diag]` log lines + DB forensic + ruled-out matrix + working hypothesis + repro recipe). **Doc churn:** `docs/E2E_RESTORATION_PLAN.md` §6 step 5 → Done with full E2 outcome notes; `docs/PLAN.md` E-series row updated to "E1 + E2 (Done 2026-05-21)"; this file (CLAUDE.md) gets the Previous-workstream block rewritten to span E1 + E2 + this Recent Fixes entry. Current Phase block unchanged — Andrea owns the framing of "no active engineering workstream" + the R-priorities reshuffle (D10) for the next workstream pick.
- **F-series — user-research observation channel + in-app Feedback complete (2026-05-13, this commit).** Doc-only closeouts on the engineering side; doc churn across `docs/FEEDBACK_PLAN.md` §4 (added cursor-aware optimistic reconciliation as item 10 deferred) + §6 (F1.5 marked Done with workstream-close notes) + `docs/USER_RESEARCH.md` §6.5 (R1 → Done; fallback rule marked resolved since R1 didn't slip; R3 active with the 2-week chat window half-spent reminder) + `docs/PLAN.md` Phase Status (new F-series row) + this file (Current Phase rewritten to "no active engineering workstream" with R2/R3/R4/user-needs as natural next-steps; this Recent Fixes entry). Channel is operational end-to-end on production: `Feedback` table live, `POST /api/feedback` accepts user submissions, `/admin` renders the FEEDBACK section between PENDING REQUESTS and ALL USERS with unread chip + mark-read flow + [load more] pagination. **F-series total:** 5-commit single PR (F1.1 → F1.5), 22 new tests, every scope-edge guard respected, one bonus latent-bug fix shipped in-scope (the router-prefix entry directly below). No AGENT.md entry — F1's decisions constrain F-series specifically. Natural next-step question per Andrea 2026-05-13: R3 (L3 chats, active per `docs/USER_RESEARCH.md` §6.5) vs R2 (PSN mobile fix, ordinary backlog) vs `/layers-user-needs` (refining the 7 candidate job stories from `docs/USER_RESEARCH.md` §4).
- **Router-level middleware without a path prefix gates everything that enters the router, including fall-through (2026-05-13, surfaced during F1.2 of `docs/FEEDBACK_PLAN.md`).** `apps/api/src/routes/admin.ts` originally had `router.use(requireUser, requireActive, requireAdmin)` at the top — meaning the admin gating ran on every request that entered `adminRouter` whether or not it matched an admin route, then 404'd anyone non-admin before letting Express fall through to the next sibling router mounted at the same `/api` prefix. Latent shipped-code, not scaffolding — it had been there since I3 (commits `4f4ba3f` + `6acdf69`). Surfaced now because F1.2 added the first non-admin route (`POST /api/feedback`) that lived at a sibling prefix; without a path-scoped gate it got intercepted by `adminRouter` and 404'd. Fix: scope to `/admin` paths only — `router.use('/admin', requireUser, requireActive, requireAdmin)`. Pattern to remember: **router-level middleware without a path argument applies to fall-through traffic, not just to routes that router owns.**
- **E1 — E2E suite restoration workstream complete (2026-05-11, commits `f40ca30` → `967a2dd`).** Doc closeouts only in `967a2dd` (this commit) — no code. Full E1 chain across 9 commits + 2 Phase C keepalive-workflow fixes summarized in the new Previous workstream block above. PLAN.md row marked Done with the same summary. E2E_RESTORATION_PLAN.md §6 step 4 marked Done (2026-05-11). README at `apps/web/tests/e2e/README.md` gained an `error-context.md` forensics note in commit `967a2dd` (the per-test page DOM artifact Playwright writes on failure — distinguishes empty-state vs misroute vs real selector bug without re-running). New post-E1 follow-up flagged in Known gaps: **`useAutoSync` E2E ambient noise** — seed's `lastSyncAt: minsAgo(8/12/...)` set at seed-run time drifts past the HOURLY threshold as time passes, causing every dashboard mount in E2E to fire `/api/platforms/:code/sync` against fake credentials; mitigation candidate is bumping the seed values to `minsAgo(<much-larger>)`. **No active workstream.** Step 7 (next-day cron at 04:00 UTC) is Andrea's morning verification — independent of doc closure.
- **A1 commit 5 — Admin polish (A-series) workstream complete (2026-05-10, commit `<this commit>`).** Doc closeouts only — no code in this commit. AGENT.md decision #39 added (mirrors A-D1 + A-D2 + A-D3 + A-D12 — destructive-admin-actions on User rows: hard-delete + FK cascade + self-protection at both layers + natural JWT-401 invalidation). CLAUDE.md Current Phase rearranged: E-series stays Active (Track 2); A-series moved to Previous; I-series demoted one slot to Earlier. PLAN.md row added for A-series. ADMIN_POLISH_PLAN.md §5 status table marked Done with all 9 landed commit hashes (`141b781` → `47b3dfe` → `0df6bf3` → `2f61787` → `36c5222` → `ed689b6` → `01cf7c8` → `2fba14c` → `620e2f7`). New plan §4 deferred-work entry for a global typography audit — A1's audit caught 4 pre-existing violations on the admin/modal surface only; similar drift likely exists across other screens, scoped as a standalone workstream when prioritized. **No active workstream on track 1; E-series remains the only active track.**
- **A1 typography hygiene — pre-existing token-scale + paper-faint violations (2026-05-10, commit `620e2f7`).** Hygiene cleanup of 4 violations of decision #12 surfaced during the A1 commit-5 typography audit. **None A1-introduced** — provenance: V1 (`fontSize: 26` → `var(--text-xl)` in ConfirmModal headline) from `de28577` May baseline; V2 (drop inline `color: var(--paper-faint)` on EmptyLine), V3 (statusColor PENDING `--paper-faint` → `--paper-dim`), V4 (SectionHeader label `--text-2xs` → `--text-xs`) all from I5's `6d7fc73`. A1 commit 4 copied the existing logic verbatim without changing it; the new code A1 added is clean (32/32 fontSize references token-anchored, zero `--paper-faint` as text color, all interactive text ≥ 12px). 4-line diff total. CLAUDE.md / AGENT.md unchanged — decision #12 was correct as written, only the implementations had drifted.
- **A1 commit-2-eyeball polish — codes grid proportional flex (2026-05-10, commit `2fba14c`).** Second-eyeball follow-up after the prior fix widened the codes grid's used-by column 180→240 px fixed (which solved truncation at medium widths but reintroduced it on wide windows: 1fr absorbed all slack). Fix: `'180px 1fr 240px 110px'` → `'180px minmax(120px, 1fr) minmax(240px, 2fr) 110px'`. Two flexible columns share slack 1:2 (used-by gets twice as much extra space as note — reflects content bias). Min-width 686 px sits comfortably under the 724 px content area at the 1024 px desktop floor; at 1500 px wide windows used-by grows to ~783 px. User-row PLATFORMS column intentionally not changed (bounded content range, not truncating). 39 AdminScreen tests stay green. Side note: Andrea onboarded Giuseppe Spizzico via the new admin flow — first real beta user beyond Luigi's earlier test; end-to-end flow worked. A-D6 stays locked.
- **A1 first-eyeball polish — 4 fixes (2026-05-10, commit `01cf7c8`).** Live-page eyeball after commit 4 deployed surfaced four issues, all fixed in a single commit: (1) ConfirmModal keyword display rendered uppercase due to `t-up` wrapper while validation required literal-case keyword (mismatch between displayed instruction and required input); fix: drop `t-up`, hard-code static `// TYPE … TO CONFIRM`, render keyword span in actual case naturally. Validation stays strict per A-D6. (2) PLATFORMS column 130 → 180 px — was wrapping "N platforms · M games" to two lines on 3-digit game counts. Added `whiteSpace: nowrap` + ellipsis safety net. (3) INVITE CODES used-by column 180 → 240 px with same nowrap/ellipsis treatment. (4) ACTIONS column breathing room — USER_ROW_GRID 56 → 80 px, CodeRow 90 → 110 px so [delete]/[revoke] don't visually butt against the prior cell. Net grids: `1fr 70px 80px 180px 80px` (user) and `180px 1fr 240px 110px` (codes — superseded by `2fba14c`'s flex fix above).
- **A1 commit 4 — admin UI rewrite: row density + sort/filter/search + delete UI (2026-05-10, commit `36c5222`).** The substantive UI commit. ALL USERS section rewritten end-to-end (PENDING REQUESTS + INVITE CODES sections deliberately untouched per plan). Toolbar adds 4-chip filter strip (`[ all ] [ active ] [ pending ] [ admin ]` with strict A-D9 semantics — admins are their own bucket; counts always reflect full dataset), search input (no debounce per A-D8 — closed-beta scale doesn't need it; case-insensitive substring match against email + name + displayIdentity; filter applies before search), and a single sort cycle button (`[ sort: joined ↓ ]` → `[ sort: status ]` (no arrow — bucket-based ordering) → `[ sort: platforms ↓ ]`). URL state via `?filter=&sort=&q=` per A-D5; defaults omitted. UserRow rebuilt as 5-col grid `1fr 70px 80px 180px 80px` (after polish commits) per A-D10 — identity merges name + email when both set ("Bedkarma · andreacama92@gmail.com"); PLATFORMS column shows "N platforms · M games" inline (more useful scan signal than storefront codes per A-D11(3)). ACTIONS column has `[delete]` button hidden on admin's own row per A-D2 (server-side `CANNOT_DELETE_SELF` 400 is the belt; UI hide is the suspenders). Click → `ConfirmModal` (`delete-user` variant, A-D6 keyword = `displayIdentity`, A-D11 details `{ games, platforms }` only — wishlists deliberately omitted from modal copy). On confirm → `api.admin.deleteUser` → `cache.invalidate('admin:')` → green inline `// deleted: <displayIdentity>` toast for 1.5s (viewport-stable position, NOT row-anchored — cache invalidation makes the row vanish, anchoring would render against a doomed DOM node). +23 new tests covering filter/search/sort/delete. **Per-route `req.user` audit confirmed clean** for the 4 `requireActive`-exempt routes (`GET/DELETE /api/auth/me`, `redeem-invite`, `request-access` — each does its own internal Prisma lookup against `req.userId`); A-D12's natural-401 property holds.
- **A1 commit 3 — promote ConfirmModal to shared modals/ + delete-user variant (2026-05-10, commit `2f61787`).** Net delta is ~120 lines moved out of `SettingsDesktop.tsx` into new `apps/web/src/components/modals/ConfirmModal.tsx`. Variant union widened from `'delete-account' | 'wipe-library'` to add `'delete-user'`. New optional `details: { games: number; platforms: number }` prop renders an `// X games · Y platforms` line under the description (per A-D11). Wishlists count NOT in the prop type — locks A-D11's "modal copy ≠ payload shape" decision at the type system layer with a defensive test. SettingsMobile uses inline typed-confirm fields (different UI pattern), deliberately untouched. 12 new modal tests including legacy regression guards (delete-account / wipe-library copy preserved verbatim post-move).
- **A1 commit 2 — DELETE /api/admin/users/:id endpoint + cascade-aware list payload (2026-05-10, commit `0df6bf3`).** New route stacked behind `requireUser → requireActive → requireAdmin`. Self-protection: 400 `CANNOT_DELETE_SELF` if `req.user.id === req.params.id`. Existence check via `findUnique({ select: { id: true } })` → 404 with canonical body. Delete via single `prisma.user.delete()` — FK cascades handle Platform/UserGame/WishlistRelease/PlatformLog; `InviteCode.usedById` flips to NULL via the SQL FK behavior. Returns 204. `GET /api/admin/users` payload extended with `_count: { userGames, wishlists }` aggregates; `AdminUser` interface gains `gamesCount` + `wishlistCount`. Frontend `api.admin.deleteUser(id)` invalidates `admin:` prefix (a deletion can orphan an InviteCode). +6 API tests including self-delete guard, non-existent-target 404, cascade verification. Pre-coding sanity check confirmed `userGames` + `wishlists` are the right relation names (schema.prisma:87-88) and that `req.user` audit was clean before relying on A-D12.
- **A1 plan-doc commit (2026-05-10, commit `47b3dfe`).** Source-of-truth `docs/ADMIN_POLISH_PLAN.md` lands; mirrors the `c0ffcb0` E-series pattern of opening a workstream with a dedicated plan-doc commit before the work. 12 locked decisions A-D1 → A-D12, single A1 PR with 5 commits planned. Plan §4 carries the test-infra forward-pointer for the web full-suite vitest hang surfaced during commit 1's verification (pre-existing, reproduced on baseline; targeted single-file runs work). +1 commit to the workstream sequence post-launch (the §4 deferred-work entry at `ed689b6` for the 500-on-deleted-user-edge-case finding — clean-for-security but minor UX wart on 3 routes; ~15-line P2025 catch-and-404 fix in a future auth-correctness pass workstream).
- **A1 commit 1 — explicit onDelete: SetNull on InviteCode.usedById (2026-05-10, commit `141b781`).** Pure declarative schema annotation — zero SQL diff verified via `prisma migrate diff --script` (output: empty migration). The I1 migration `20260508140000_invite_codes` already shipped `ON DELETE SET NULL` at the SQL layer; this just makes the schema explicit, futureproofing against a Prisma version that ever changes the implicit-default-for-optional-relations behavior. Side effect: `prisma format` re-aligned column whitespace in HltbData + WishlistRelease (formatter idempotence; zero SQL impact). Web full-suite vitest hung on this environment during verification — reproduced on baseline (with the schema change stashed), confirming pre-existing test-infra flake. Captured as forward-pointer in `docs/ADMIN_POLISH_PLAN.md` §4.
- **I6 — Closed-beta invite codes workstream complete (2026-05-10, commit `f18180f`).** Doc closeouts only — no code in this commit. CLAUDE.md Current Phase flipped from "Active: I-series" to "Active: E-series" with I-series moved to Previous; INVITE_CODES_PLAN.md §6 Phase Status table marked I1–I5 + 3 smoke-test followups + 3 I5-review followups + I6 all Done; PLAN.md row updated to Done with final test count. Operational verification (generate first real codes, verify with one friend per spec §8, 24h log-monitoring window for stray `403 PENDING_INVITE` on active users) runs asynchronously with Andrea as the operator; any incidents will be recorded inline.
- **I5 follow-up C — global SWR cache reset between tests (2026-05-10, commit `858cb00`).** Test-infra cleanup. Caught while writing AdminScreen.test.tsx — 8/14 tests failed initially because the cache module is a singleton + `vi.clearAllMocks()` doesn't touch real modules. Per-file `cache.invalidate('')` worked but was a latent footgun for future test files. Moved into `apps/web/src/test-setup.ts` via `cache._resetForTests()` (clears entries AND subscribers; strictly more thorough than `invalidate('')`). Per-file flush removed from AdminScreen.test.tsx. **265 web tests still pass.**
- **I5 follow-ups A + B — note pre-fill compact label + platform-summary deferral (2026-05-10, commit `2662f7c`).** Reversal of I5 judgment call #5: `for marco@gmail.com` was over-verbose for a quick recognizer in the codes list. New `noteLabel(user)` helper mirrors `displayIdentity` precedence (User.name → real-email local-part → "Steam user — {steamId}") but optimized for compact-label rather than ground-truth. Two-concerns split kept deliberate: button label = `displayIdentity` (what you're acting on), pre-fill note = `noteLabel` (recognizer for codes list later). Two new precedence tests. **B:** documented platform-summary truncation past ~3 codes as a deferred entry in INVITE_CODES_PLAN.md §4 — closed-beta users have ≤2 platforms, revisit when 4+ shows up. **C** lands separately. Plan §6 doc-hygiene catch-up (Andrea's reminder: phase tables are source of truth, don't drift).
- **I5 — Closed-beta admin panel (2026-05-10, commit `6d7fc73`).** `/admin` route with three sections (PENDING ACCESS REQUESTS / ALL USERS / INVITE CODES) backed by I3's endpoints. Top-bar `[refresh]` invalidates both `admin:` cache keys. `[+ generate code]` opens a focus-trapped two-state modal (prompt → green-tinted callout with copyable code). Per-pending-row `[generate code for X]` button pre-fills the modal note (compact label per follow-up A). User row = 4-col grid (identity / status badge / joined / platform summary); code row = 4-col grid (code / note / used-by-or-unused / `[revoke]`). Three guards: mobile breakpoint → desktop-only fallback per I-D3; non-admin → 404 view (defense-in-depth alongside Sidebar's conditional entry and the server's `requireAdmin` 404 per I-D15). New `useAdminUsers` + `useAdminInviteCodes` SWR hooks. Conditional `// admin` Sidebar group. 14 new tests covering gating + sections + empty states + generate flow + refresh + revoke (incl. confirm-cancel + no-revoke-on-used). **265 web tests pass.**
- **Doc-currency pass (2026-05-09, commit `484c6bc`).** All four tracked workstream docs brought current after the I-series + E-series state diverged from what the docs showed. CLAUDE.md `Current Phase` rewritten (was carrying a stale "Active: Trophies" header alongside a "complete" body); 7 new entries prepended to Recent Fixes (I1–I4 + auth follow-ups + E1 plan). AGENT.md Data Model + Key Decisions extended with closed-beta gating fields and decisions #34–#38 (column-not-env-var, sidebar-as-affordance, `$transaction` predicate-guarded redemption, two-tier rate limit, URL-not-state ?next= channel). PLAN.md gained 5 missing rows (Steam wishlist + Settings audit + Closed-beta + E2E). INVITE_CODES_PLAN.md §6 marked Pre-step + I1–I4 Done with commit hashes.
- **E2E suite restoration plan revised + locked (2026-05-09, commit `5791ba8`).** Andrea confirmed Option F (hybrid B+E) with five substantive refinements that change both the framing and the scope. §3 fully rewritten around "what does each test uniquely prove?" (~30/70 mocked/real-DB ratio; data-parity acknowledgment); §4 PR sequence rewritten around E1 alone (E3 dissolved into E1 via global auth fixture); §5 expanded with three new "don't reopen" entries; §6 status table marks step 2 Done, step 3 (E1 PR plan drafts) is the new open gate. Held for Andrea's review before E1 PR-shaped deliverables get written up. Two specific items flagged for resolution at review time: global auth fixture mechanism (per-test expected-URL declaration vs convention-based inference), keepalive Action specifics (cron, failure-mode, recovery).
- **Auth deep-link real fix — `RequireAuth` forwards `?next=` via URL (2026-05-09, commit `9051b36`).** Andrea's smoke test #3 against the deployed `5024234` build said deep-link preservation didn't work despite that commit's claim to close the §4 deferral. Working through her four-possibility checklist, the root cause was hypothesis #2: `RequireAuth` redirected unauthenticated users to bare `/login` and passed the original path via React Router's `state={{ from: location.pathname }}` — a channel `LoginScreen` (which reads `useSearchParams()`) never consumed. `params.get('next')` always returned `null` for cold deep-link visits → `safeNext(null) === '/'` → user landed on `/`. The earlier change was correct in isolation, just had nothing to read; unit tests passed because they injected `?next=` directly into `MemoryRouter` URLs. Real fix: `RequireAuth` now redirects to `/login?next=<encoded path + search>`. Single channel — URL query string — used by both `/login` and `/welcome`. New integration test [auth-deeplink.test.tsx](apps/web/src/__tests__/auth-deeplink.test.tsx) mounts `<App>` with no `?next=` seeded, asserts the param is on the URL after the unauth redirect, then asserts the user lands on the deep-link after auth. **The lesson** (recorded in `docs/INVITE_CODES_PLAN.md` §5.2): closing a deferral requires manual end-to-end verification in the live env — passing unit tests do NOT prove integration. **251 web tests pass.**
- **LoginScreen post-auth navigation: setUser-before-navigate + status-aware target + safeNext (2026-05-09, commits `386059d` + `5024234`).** Smoke-test-discovered pre-existing bug surfaced by closed-beta gating. Andrea reported: register a fresh account → redirect to /login (not /welcome) → retry register → 409 because the User row was already created. Root cause: `UserProvider` fetches `/api/auth/me` once at App mount (pre-cookie → `status='unauthed'`); after register/login, the cookie is set but UserContext was never told. SPA-internal `navigate('/')` then hits `RequireAuth` which still sees `'unauthed'` and bounces. OAuth callbacks dodge it via full-page browser redirect (UserProvider remounts with cookie); local form path skips that. Fix: capture the AuthResponse, call `setUser(response.user)` BEFORE `navigate`, branch the navigation target on `response.user.status` (ACTIVE → `safeNext(next)`, PENDING → `/welcome` with `next` preserved), `replace: true` so back-button doesn't return to login. New `LoginScreen.test.tsx` with 10 cases including invocation-order assertion (`mockSetUser.mock.invocationCallOrder[0] < mockNavigate.mock.invocationCallOrder[0]`) so the bug class — "API call resolves, response discarded, context never updated" — is pinned for both register and login paths. Plus `RedeemInviteError` typed-error class so the welcome screen can render distinct copy per failure mode (`INVALID_FORMAT` / `CODE_NOT_FOUND` / `CODE_ALREADY_REDEEMED` / `RATE_LIMITED` / `UNKNOWN`). 249 web tests pass.
- **I4 — Welcome screen + `RequireActive` wrapper (2026-05-08, commit `683b05c`).** Closes the I2→I4 deploy gap. New `/welcome` route inside RequireAuth-only (NOT RequireActive — pending users have to reach it without bouncing in a loop); responsive single-component screen matching the `/login` aesthetic with two states (default / request-sent driven by `user.hasRequestedAccess`). Form-level non-empty constraint on `[send request]` (refusal to refresh `accessRequestedAt` on rows with no new content) added as I4 follow-up `e8ca975`. Four distinct error messages per Andrea's reminder #2: format-invalid (regex caught client-side, no API call) / code-not-found (server 409) / already-redeemed (server 409) / rate-limited (429); plus an UNKNOWN fallback. New `safeNext` helper at [apps/web/src/lib/safeNext.ts](apps/web/src/lib/safeNext.ts) with three-rule open-redirect defense (must start with `/`, NOT with `//`, no `:` before any `/`); 15 dedicated tests covering attack vectors (`//evil.com`, `https://evil.com`, `javascript:alert(1)`, `data:`, `vbscript:`, `\\\\evil.com`, nullish/empty). New `RequireActive` wrapper redirects pending users to `/welcome?next=<encoded original path + search>`. E2E + a11y for `/welcome` deferred to the broader E2E-suite restoration workstream (existing E2E broken since I1's seed-andrea deletion); deferral wording explicitly forbids prod-polluting tests when the suite is restored. **236 web tests pass at I4 land; 249 after the LoginScreen fix above.**
- **I3 — Closed-beta admin endpoints (2026-05-08, commits `4f4ba3f` + `6acdf69`).** Four routes mounted at `/api/admin/*`, every route stacks `requireUser → requireActive → requireAdmin`. Non-admins get 404 with the canonical `{ error: 'Not found' }` body so the admin surface stays invisible at the URL level (mirrors how routes/games.ts handles "row not found"). `GET /api/admin/users` returns full user listing with `displayIdentity`, redeemed-code summary, platform summary, sorted pending-with-request first then `createdAt` desc. `GET /api/admin/invite-codes` returns all codes sorted unused-first, then `usedAt` desc. `POST /api/admin/invite-codes` generates `HOARD-XXXX-XXXX` codes from the 32-char no-0/O/1/I alphabet via `crypto.randomInt`, retries up to 5× on P2002 collisions (statistically ~10⁻⁴⁶ per attempt; would indicate a real bug). `DELETE /api/admin/invite-codes/:id` allows deletion only for unused codes (used codes return 409 — there's nothing to roll back since the user is already ACTIVE). Shared helpers: `apps/api/src/lib/displayIdentity.ts` (single source for "Steam user — {steamId}" vs email rendering) and `apps/api/src/lib/inviteCodes.ts` (the alphabet + generator). Follow-up commit `6acdf69` reversed an I3 judgment call: `displayIdentity` now prefers `User.name` over the Steam-id fallback (handle is what Andrea recognizes; Steam64 ID is unreadable noise; falls back only when `name` is null — private profile / `GetPlayerSummaries` failure). 274 API + 196 web tests pass.
- **I2 — Closed-beta gate middleware + redeem-invite + request-access (2026-05-08, commit `702f493`).** New `requireActive` middleware applied to every authenticated route except the explicit exempt set: `GET /api/auth/me` (frontend hydrates welcome screen), `DELETE /api/auth/me` (escape hatch for pending users), `POST /api/auth/logout`, the new redeem and request-access endpoints. Pre-impl audit in `docs/INVITE_CODES_PLAN.md` §3 maps every `requireUser` consumer in `apps/api/src/routes/*` against every `'/api/...'` reference in `apps/web/src` — gating decisions are now bottom-up complete. New signups (register, Google OAuth, Steam OpenID) land in `PENDING_INVITE` via the I1 schema default; OAuth callbacks status-redirect new pending users to `${WEB_URL}/welcome` instead of dashboard. `POST /api/auth/redeem-invite`: Zod regex validation, atomic redemption via `prisma.$transaction` with `WHERE usedById IS NULL` predicate on `prisma.inviteCode.updateMany` (race-safe — parallel-Promise.all test asserts exactly one wins), two-tier rate limit (per-IP 10/h + per-user 5/h, both production-only, keyed on `req.ip` and `req.user.id` respectively per I-D6). `POST /api/auth/request-access`: idempotent, 500-char Zod cap; subsequent calls overwrite `accessRequestMessage` and refresh `accessRequestedAt`. `GET /api/auth/me` response gains `status` / `isAdmin` / `hasRequestedAccess` so the frontend can drive welcome-screen state and admin-link visibility. 245 API tests pass.
- **I1 — Closed-beta schema + migration (2026-05-08, commit `6856f10`).** Pure additive change with one-time backfill. New `UserStatus { PENDING_INVITE, ACTIVE }` enum; new `User` columns (`status`, `isAdmin`, `hasRequestedAccess`, `accessRequestMessage`, `accessRequestedAt`); new `InviteCode` model (single-use codes; `usedById` is `@unique` for the 1:1 redemption invariant; FK `ON DELETE SET NULL` so a deleted-user keeps the audit row). Hand-written migration `20260508140000_invite_codes` applied to Supabase via the documented `db execute` + `migrate resolve` pgbouncer recipe. Pre-step (commit `38098e2`): deleted `seed-andrea` (phantom seed user, 32 games / 0 platforms — the non-cuid id was the giveaway) and `daniel.guernieri@gmail.com` (unintended signup from same day as Luigi, 0 games / 0 platforms). Backfill SQL: `UPDATE "User" SET status = 'ACTIVE' WHERE "createdAt" < NOW();` + admin promotion `UPDATE "User" SET "isAdmin" = true WHERE id = 'cmooks9ey0000ho06z65remze'`. RLS enabled on the new `InviteCode` table to match the existing public-table policy. New helper `scripts/snapshot-users.ts` writes a rollback dump of all User columns + refuses to write to non-gitignored paths via `git check-ignore`. New `docs/runbooks/` directory (gitignored) holds the local-only snapshot file; pre-deletion state of all five users captured there. **Schema is ahead of code; safe in this direction** — the existing API code didn't read any of the new columns, so the migration was invisible to the running prod build until I2 caught the code up. Behavior change zero. 226 API + 196 web tests pass.
- **E2E suite restoration plan opened (2026-05-09, commit `c0ffcb0`, `docs/E2E_RESTORATION_PLAN.md`).** Parallel workstream triggered by I4's deferral note. **Diagnostic finding** worth knowing: the existing E2E suite has been broken since I1's pre-step deletion of seed-andrea, in a deceptive way. `screens.spec.ts` (38 tests) all fail with "element(s) not found" timeouts because the auth chain redirects to `/login` and content assertions don't match. `a11y.spec.ts` (12 tests) "passes" because axe-core scans whatever DOM is rendered — it reports `/login` as accessible (true) for every test that expected to scan an authed route. False-positive coverage. **Strategy decision held at step 2** awaiting Andrea's input — five options laid out with tradeoffs across cost / CI complexity / schema parity / secrets management / dev ergonomics. Andrea's standing rule (from I4 deferral): **don't pollute prod `User` table with register-fresh-user tests** when E2E is restored. The right answer is dedicated test DB or `globalSetup`/`globalTeardown` against an isolated namespace. No test code drafted yet.
- **Settings audit PR B — platform sync log (2026-05-08).** Brings the Log tab back on PlatformDetail with real backing data. New `PlatformLog` table (migration `20260508130000_platform_log`, applied) with a `LogLevel` enum (`info` / `warn` / `error`) + indexes for cursor pagination. New `apps/api/src/services/platformLog.ts` `logPlatform()` helper that swallows its own errors so a logging issue can never fail a sync. Write hooks in `routes/platforms.ts` at every sync touchpoint — `sync.started`, `library.imported` / `library.unsupported`, `trophies.applied` / `trophies.failed`, `wishlist.imported` / `wishlist.failed`, `achievements.applied` / `achievements.failed`, `sync.ok` (with duration), `sync.error`. New cursor-paginated `GET /api/platforms/:code/log?cursor=` endpoint (50/page, `[{createdAt: desc}, {id: desc}]` for stable order across same-millisecond writes). Shared `PlatformLogTab` component renders entries in terminal aesthetic: `[YYYY-MM-DD HH:MM:SS] level · message`, level color-coded (info=green / warn=amber / error=red), `[load more]` button, "no activity yet" empty state. Wired into both `PlatformDetailDesktop` and `PlatformDetailMobile` (re-adding the tab dropped in PR A). 8 new tests covering helper write/error-swallow paths + read endpoint shape/cursor/ordering. **226 API + 196 web tests pass.**
- **Settings audit PR A — wired the unwired controls + dropped the lying ones (2026-05-08).** Audit (`docs/SETTINGS_AUDIT_PLAN.md` §1) found four genuinely decorative controls on PlatformDetail. Fixed in one PR: **(S1)** the `[reveal]` NPSSO button now actually toggles the masked / unmasked display via a new `GET /api/platforms/:code/credentials` endpoint with `Cache-Control: no-store` so the credential never persists in cache. Server returns the platform-specific field shape (`{ npsso }` for PSN, `{ steamId }` for ST, `{ apiKey }` for XB). **(S2)** the lying "auto-refresh 7d before expiry" toggle is gone — Sony has no NPSSO refresh API, the toggle was promising a feature we'll never build. Replaced with a token-health status row reading `Platform.syncStatus` (`ok` → green dot · "// connection healthy · last verified Xh ago"; `error` → red dot · "// last sync failed — token may be expired" + inline `[paste new token]` button that opens an inline 64-char input mirroring the initial-connect flow). **(S3)** the four scope checkboxes (library / playtime / trophies / friends) are now a read-only `// what hoard reads` info display with green check / red x icons, no checkbox affordance — none of them are meaningfully toggleable in v1, and the previous look implied otherwise. **(S4)** the empty `// no log entries yet` Log tab is dropped entirely from `TabKey` / `MobileTab` types, tabstrip arrays, section renders, and the `LogTab` component itself; PR B brings it back with a real `PlatformLog` model. Same changes shipped on both `PlatformDetailDesktop` and `PlatformDetailMobile`. 5 new backend tests on the credentials endpoint. **219 API + 196 web tests pass.**
- **Steam wishlist import (2026-05-08).** Smaller follow-up workstream after the trophies push (no formal `*_PLAN.md` doc — scope was small enough that decisions live inline in code comments). New `getSteamWishlist(steamId)` in `apps/api/src/services/platforms/steam.ts` calls the public `IWishlistService/GetWishlist/v1/` endpoint; same public-profile caveat as Steam achievements (T-D7), silent-skip + return `[]` on private / non-2xx / network error. New orchestrator `apps/api/src/services/wishlistImport.ts` exports `applySteamWishlistImport(userId, items)` — for each Steam wishlist item: resolves `appid` via `getGameBySteamId`, looks up Game by igdbId in our catalog, and **skips** if the user already has any UserGame for it (preserves library decision; mirrors the manual-star flow's `update: {}` no-op pattern from decision #29). Otherwise pulls the rich `getReleaseDetails` shape and atomically upserts `Game` (with `steamAppId` populated so future Steam syncs hit the exact-match path), creates `UserGame(status='Wishlist')` with the user's actual Steam-side `addedAt`, and creates `WishlistRelease` so the Releases page hero/agenda renders correctly. Wired into `routes/platforms.ts` Steam branch as fire-and-forget alongside the achievement background pass — runs AFTER `Platform.syncStatus` flips to `ok`. **Removal is NOT supported in v1** — if a user removes a game from Steam wishlist (or buys it), the Hoard wishlist row stays until they un-star manually. Per-UserGame source tracking would be required to mirror the un-star branch safely; documented in Known Gaps below. PSN / Xbox / GOG wishlists not feasible — none have public APIs that expose wishlist data; `psn-api` doesn't include it. 12 new tests (6 fetcher × happy / private / non-2xx / network-fail / no-key / missing-fields, 6 orchestrator × import / skip-existing / skip-already-wishlist / unresolved IGDB / unresolved release-details / per-item error continuation). **214 API + 196 web tests pass.**
- **T6 — Dashboard achievements rollup (2026-05-08).** Final (optional) PR of the trophies workstream. `DashboardStats.achievementsRollup: { earned, total, percent } \| null` returned from `GET /api/dashboard`. Server-side: single Prisma `aggregate` query with `_sum` on `achievementsEarned` + `achievementsTotal` scoped to userId AND filtered to `achievementsTotal: { not: null }` so unsupported games don't pollute the denominator. `null` rolls up when no row in the user's library has any achievement data — UI hides the line entirely instead of rendering `0/0 · NaN%`. `DashboardDesktop` surfaces it as a `Gauge` block right under the existing completion-ratio gauge for visual symmetry; `DashboardMobile` surfaces it as a single bordered row in the receipt aesthetic. Percent renders green at ≥ 80, paper-dim otherwise (matches T5). 4 new backend tests on the rollup path. **202 API + 196 web tests pass.** **Trophies workstream now complete: T1 schema → T6 dashboard, all six PRs shipped same day. No active workstream.**
- **T5 — GameDetail receipt-block UI + Steam scope-tab note (2026-05-08).** Final visible PR of the trophies workstream — the four `UserGame.achievements*` columns populated by T2/T3 now show on GameDetail. New `achievementLabel(playtimeByPlatform)` helper in `apps/web/src/lib/utils.ts` (returns `'trophies'` when the user has any PSN playtime, `'achievements'` otherwise — Andrea is PSN-heavy so dual-platform games default to "trophies"). `GameDetailDesktop` inserts a single-line Marker `// trophies · 12/35 · 34%` after the HLTB block, hidden when `achievementsTotal === null`, percent colored `var(--green)` when ≥ 80, `var(--paper-dim)` otherwise. `GameDetailMobile` weaves the same data into the existing PROGRESS pre-block as a dotted-row in the HLTB style. T-D7 amendment: `PlatformDetailDesktop` and `PlatformDetailMobile` scope tabs now render a Steam-only subline directly under the existing `trophies` checkbox: "// note · steam profile must be public for achievement sync. settings → privacy → game details on steam." `ScopeTab` desktop component gained a `code` prop to gate the subline. 3 new label-inference tests. **198 API + 196 web tests pass; lint + typecheck + rename-rule clean.** **T6 (Dashboard rollup) is optional / deferrable — awaits Andrea's call.**
- **T3 — Steam achievement fetcher + background pass (2026-05-08).** Third PR of the trophies workstream. New `apps/api/src/services/platforms/steamAchievements.ts`. `getSteamAchievementsForGame(steamId, appid)` makes one `ISteamUserStats/GetPlayerAchievements/v1` call per game; returns `{ earned, total } \| null`. `null` covers all five "no data" cases silently per T-D7: private profile, no-achievement-support, non-2xx HTTP, fetch network error, empty-array response. `triggerSteamAchievementsBackground(userId, steamId)` filters candidates to UserGames whose Game has a `steamAppId` AND who have Steam playtime in their `playtimeByPlatform.ST` (so PSN-only games matched on a shared `steamAppId` don't get the wrong user's achievements written). Throttled at 330ms (~3 req/s) — 1000-game library is ~5 min, well under Steam's 100k/day per-key cap. Writes `(earned, total, percent, updatedAt)`, applies T-D2 auto-complete via the shared helper. Per-game errors don't kill the loop. Wired into `routes/platforms.ts` Steam branch as fire-and-forget AFTER `Platform.syncStatus` flips to `ok` — UI doesn't wait, data trickles in. 14 new tests (7 fetcher × no-data branches + 7 orchestrator on candidate filter + write + auto-complete + skip + error). **198 API + 193 web tests pass.**
- **T2 — PSN trophy fetcher + auto-complete (2026-05-08).** Second PR of the trophies workstream. New `getPsnTrophyTitles(npssoToken)` in `apps/api/src/services/platforms/psn.ts` makes one paginated `getUserTitles` call returning the cleaned shape with `npCommunicationId` + per-rank earned/defined trophy counts + `lastUpdatedAt`. New orchestrator `apps/api/src/services/trophies.ts` matches each trophy title to the user's library via two lookup Maps — primary by `Game.psnNpCommunicationId`, fallback by normalized title (only on Games where `psnNpCommunicationId` is null, so a stable match never gets overwritten by a worse title-based one). Persists `Game.psnNpCommunicationId` on first title-fallback match, then writes `(achievementsEarned, achievementsTotal, achievementsPercent, achievementsUpdatedAt)` to the matched UserGame and applies the auto-complete rule. T4's auto-complete helper extracted early to `apps/api/src/lib/achievements.ts` — T-D2 in one place: `Backlog/OnHold/Playing → Completed at percent === 100`, preserve `Dropped/Wishlist/Completed`. Wired into the PSN branch of `routes/platforms.ts` after `runSync` returns; trophy-fetch failures don't fail the whole sync (library import already succeeded). 22 new tests (9 auto-complete matrix + 13 orchestrator covering matching strategy, auto-complete branches, aggregate math, defensive zero-defined-trophies skip). **184 API + 193 web tests pass.**
- **T1 — Trophies & achievements schema (2026-05-08).** First PR of the trophies workstream (`docs/TROPHIES_PLAN.md`). Migration `20260508120000_trophy_aggregates` adds `Game.psnNpCommunicationId String? @unique` + four nullable columns on `UserGame` (`achievementsEarned`, `achievementsTotal`, `achievementsPercent`, `achievementsUpdatedAt`). `@hoard/types` extended in lockstep; `apps/api/src/lib/mappers.ts` `UserGameRow` + `mapUserGame()` updated to surface the new fields; one frontend test fixture (`RemapGameModal.test.tsx`) backfilled with the four new nullable fields to keep the type contract whole. Migration applied to Supabase out-of-band via the documented `db execute` + `migrate resolve` recipe. **No behavior change** — every column stays null until T2 (PSN trophy fetcher) and T3 (Steam achievement fetcher) populate them. 162 API + 193 web tests pass; lint + typecheck + rename-rule clean. **Next phase: T2 (PSN trophy fetcher).**
- **Remap collision merge flow (2026-05-08, hot-fix on the remap UI).** Original behaviour returned 409 + `{ error }` and stopped — but the new `Game` row had already been upserted, so users ended up with both the (correct) target game in their library AND the wrong-matched UserGame still pointing at the wrong Game. Three changes: **(1)** server's 409 response now includes `conflictUserGameId` + `conflictTitle` so the client can name the existing entry; **(2)** the endpoint accepts `merge: true` to combine the source UserGame INTO the target one inside a `$transaction` — max-per-platform playtime, max(lastPlayedAt), min(addedAt), source's status/notes/rating win when non-default, source UserGame deleted; **(3)** `api.remapGame()` parses the 409 body and throws a typed `RemapConflictError`, which `RemapGameModal` catches to swap the footer for a `role="alertdialog"` merge prompt — confirm calls `remapGame(..., merge=true)`. The "wrong UserGame still hangs around" mid-state is gone. 3 new backend tests + 1 new frontend test cover collision shape, merge transaction (max-per-platform playtime + status/notes precedence), and the 409 → merge-confirm flow. **162 API + 193 web tests pass.**
- **In-app `[wrong game?]` remap UI (2026-05-08, sync-quality batch part 2).** Closes the loop opened by the smart matcher: the audit script catches platform-collision-class mismatches (Ragnarok-MMO), but the wrong-sequel class (Slay-the-Spire-2) and any future IGDB drift need user-eyeballed correction. New `POST /api/games/:userGameId/remap` endpoint + `RemapGameModal` component + `[wrong game?]` chip on GameDetailDesktop and a matching link on GameDetailMobile. Server preserves notes / status / playtime / rating / addedAt / lastPlayedAt — only `gameId` is rewritten. The endpoint upserts the new `Game` row by `igdbId` (reuses if another UserGame already references it; creates from IGDB metadata otherwise), guards against the `(userId, gameId)` unique-constraint collision with a 409, and triggers HLTB background fetch if the new Game has no HLTB row. Cache invalidation: `game:{userGameId}` + library prefixes (`games:`, `shelves:`, `gameCounts`, `dashboard`). Decision mirrored as #33 in `AGENT.md` — full automation rejected because we don't store platform-side IDs (PSN titleId / Steam appid) per UserGame; storing those is a v2 schema-change effort. The remap modal pre-fills the search with the current title so the right game is often one rank below; selected result preview shows current → new with IGDB IDs to make the swap explicit. 6 new tests (2 backend remap path including 409 collision + no-op same-id, 4 frontend modal interactions). **160 API + 192 web tests pass; lint + typecheck + rename-rule clean.**
- **Smart IGDB title-matching for sync (2026-05-08).** Two real-world bugs from Luigi's first PSN test + Andrea's Steam sync today: PSN's "God of War Ragnarök" matched IGDB's mobile-only Korean MMO "Ragnarok: War of Gods"; Steam's "Slay the Spire" matched the early-access "Slay the Spire 2". Both came from the same root cause in [syncRunner.ts:75-76](apps/api/src/services/syncRunner.ts#L75-L76): we ran `searchGames(title)` and blindly took `results[0]` — i.e. trusted IGDB's relevance ranking. New `apps/api/src/services/igdbMatch.ts` implements a soft-scoring `pickBestMatch(query, results, platformCode)` that ranks the top-N IGDB candidates by (a) normalized title similarity (exact normalized match gets +1000, prefix +200, all-words-contained +50), (b) platform agreement with the syncing platform (+500 match / +50 unknown / -200 wrong), and (c) popularity tiebreak via `total_rating_count` (`log10(count)*10`). Soft scoring (not hard filtering) is deliberate — IGDB's platform data is sometimes incomplete; we don't want to exclude a perfect-title match just because IGDB hasn't tagged its platforms. Decision mirrored as #32 in `AGENT.md`. **Schema/type changes:** `IgdbSearchResult` gains `platforms: string[]` + `totalRatingCount: number | null`; the IGDB `searchGames` query now also fetches `platforms.name, total_rating_count`. Existing IGDB callers (`getGame`, `getGameBySteamId`) still produce empty defaults for these fields — fine because their callers don't read them. **Diagnostic:** new `scripts/audit-mismatches.ts` (read-only, ~3 IGDB req/s) walks every UserGame with sync-derived playtime and prints those whose IGDB-cataloged platforms don't include the platform it was synced from. Catches the Ragnarok-MMO class. Does NOT catch the Slay-the-Spire-2 class (that game IS on PS5 — the title is just wrong); the new matcher prevents these going forward, existing rows need user-eyeballed remap (Prisma Studio for v1). 12 new tests in `igdbMatch.test.ts` covering both real-world bugs as named cases plus normalize() suffix/diacritic handling. **154 API + 188 web tests pass; lint + typecheck + rename-rule clean.**
- **GameDetail back-button bottom padding + mobile tab-bar anchoring (2026-05-08, commit `976a692`).** Two visual fixes. **(1)** [GameDetailDesktop.tsx:132](apps/web/src/components/screens/GameDetailDesktop.tsx#L132) had `padding: '12px 36px 0'` so the `[back]` chip sat flush against the bottom border — restored symmetric vertical padding. **(2)** Mobile shell felt "floating" because `.m-tabbar` carried `padding-bottom: env(safe-area-inset-bottom)` — that painted tab-bar bg color through the iOS home-indicator gesture zone, which read as ~34 px of empty tab bar below the items. Moved the safe-area padding to the outer `.app-mobile` container instead, so the gesture zone shows page bg and the tab bar items sit flush at the bottom of the content area.
- **"Feel alive" batch: live HeroCountdown ticker + real platform sync frequency (2026-05-07).** Two related "frozen on render" gaps closed in one PR. **(1) HeroCountdown** on the Wishlist Hero now ticks the d/h/m/s grid live at 1 Hz instead of only updating on page refresh. New `useNow(intervalMs)` hook in `apps/web/src/hooks/useNow.ts` returns a re-rendering `Date.now()` value, paused while `document.hidden` and resumed on `visibilitychange` so a backgrounded tab doesn't burn battery. `countdownParts` and `daysUntil` in `lib/utils.ts` gained an optional `now` parameter (default `Date.now()`) so the live value is threaded through deterministically per render. **(2) Platform sync frequency** is no longer decorative. The radio picker on PlatformDetail desktop + mobile sync tabs was hard-coded `15m` selected with no state, no onChange, no persistence — and there was no scheduler anywhere on the API. Now: schema gains `Platform.syncFrequency` (`enum SyncFrequency { FIVE_MIN, FIFTEEN_MIN, HOURLY, MANUAL }` default `HOURLY`), hand-written migration `20260507120000_platform_sync_frequency` (SQL ready to apply via the documented `db execute` + `migrate resolve` recipe — not auto-applied; deploy step below). New `PATCH /api/platforms/:code` endpoint persists the choice and returns the updated `PlatformDetail`. New `api.updatePlatform()` client method invalidates `platformStatus` cache. Both pickers wire `onClick → handleSyncFrequencyChange` with optimistic local state + rollback on failure. New `useAutoSync()` hook mounted once at `AppShell` reads the existing cached `platformStatus` (no extra fetch — dedupes with Sidebar's `useQuery`), and triggers `POST /api/platforms/:code/sync` on mount + on `visibilitychange` + on a once-per-minute interval for any platform whose `lastSyncAt` is older than its `syncFrequency` window. Skips non-syncable platforms (NT/EP), `MANUAL` cadence, in-flight syncs (`syncStatus === 'syncing'`), and locally-recently-kicked-off ones (per-code `Map<code, ts>` ref guards the cache-refresh race window). Decision: **client-side `setInterval` on the open tab, not server-side cron** — for a one-user personal tool, the cost of running a Railway cron service was disproportionate, and the picker now means what it actually says ("how often hoard polls your library while the app is open"). Default `HOURLY` chosen because Steam/PSN library data doesn't change often enough to warrant sub-hour polling. 12 new tests (3 useNow + 7 useAutoSync `isOverdue` + 1 HeroCountdown live-tick + 4 PATCH endpoint). 143 API + 188 web tests pass; lint + typecheck + rename-rule clean. **Migration deploy step (manual, follows the documented pgbouncer workaround):**
  ```
  npx prisma db execute \
    --file packages/db/prisma/migrations/20260507120000_platform_sync_frequency/migration.sql \
    --schema packages/db/prisma/schema.prisma
  npx prisma migrate resolve \
    --applied 20260507120000_platform_sync_frequency \
    --schema packages/db/prisma/schema.prisma
  ```
  Until applied, the API will fail to map `GET /api/platforms/status` (column missing on Prisma read) and PATCH 500s. The auto-sync hook fails closed — no platforms read → no syncs triggered.
- **Releases — IGDB query: bumped `limit` 50→500, dropped 365-day cap (2026-05-07, commit `50093c3`).** Andrea's intent: "I don't want to filter out anything announced, but filter out everything not relevant thanks to the hype filter." The hype threshold is the qualitative gate; the date window and 50-row truncation were artificial caps that hid late-year and far-future announcements even when they cleared hype. Changes in `apps/api/src/services/igdb.ts`: `getUpcomingReleases` default `limit = 50` → `limit = 500` (IGDB's hard ceiling per query); dropped the `first_release_date <= now + 365d` clause (and its `toTs` calculation); lower bound `>= now` stays — this is the upcoming feed, not "all releases ever". Effect: games announced for >12 months out are now fetched and naturally land in the Quarters zoom's TBA bucket on the client (which already sorts by hype desc). Spec doc `docs/igdb_filtering.md` updated to match. Server-side cache key (`upcoming_${day}_h${threshold}_${platforms}`) is unchanged; existing cache will TTL-expire over the next 24h before the new payload is served, OR users can bump+restore their hype threshold to invalidate immediately (see Bug 3 in the audit-fix entry below).
- **Releases — five post-rework bugs from a focused audit (2026-05-07, commit `b95ce35`).** After Andrea spotted the wishlist-mode hero showing a non-wishlisted game, I audited the same surfaces and found four more issues. **Bug 1** (the headline): `api.igdbUpcoming('wishlist')` only forwarded the scope param when scope was `'all'`. Calling `useUpcoming('wishlist')` silently routed to the `my-platforms` feed; the hero picked the first future-dated item from THAT feed and rendered with a hollow star (the my-platforms feed correctly tagged it as not wishlisted). Regression dating to Post-8 PR B (commit `47a46de`) — never tested at the api-client URL level. Fix: forward both `'all'` AND `'wishlist'` as `?scope=…`. **Bug 2**: Mobile All Releases mode hid the star toggle on rows because `ReleasesMobile` passed `mode === 'wishlist' ? onToggleWishlist : undefined` — exactly inverted. Mobile users could browse All but couldn't wishlist anything from it; every wishlist add had to come from the game-detail page. **Bug 3**: Settings hype-threshold change had no immediate effect because `api.updateMe` didn't touch any cache. Bumping `hypeThreshold` re-filtered the server feed (different cache key) but the client kept serving stale data for up to 30s until SWR's stale window expired. Fix: invalidate `upcoming:` when `body.hypeThreshold !== undefined`. **Bug 4**: Desktop `ReleaseCard` wasn't clickable for game-detail nav — the main grid cards had a clickable star inside but the rest of the card was dead. Inconsistent with `AgendaRail` rows, RECENT page cards, and `MobileReleaseRow`. Fix: `ReleaseCard` now accepts `onClick` (later renamed `onOpen` in the wishlist-as-library work below); implemented with `role="button"` + `tabIndex` + keyboard handler (NOT a real `<button>`) so the inner star toggle can stay a real `<button>` without invalid nesting. The star's `onClick` stops propagation so a star tap doesn't double-fire the card click. **Bug 5**: `toggleWishlist` didn't invalidate `releases:recent` — the RECENT page joins wishlist with the IGDB recent feed server-side, so un-starring a recent drop should remove it from `// just out · starred` and starring a high-hype recent drop should move it from `hyped` → `starred`. Fix: add `cache.invalidate('releases:recent')` to `toggleWishlist`. **Bug 6** (deferred to v2): mobile sheet drag-down dismiss is still a TODO; the other 4 dismissal paths (Done / X / scrim / Escape) all work. New tests: `api-igdbUpcoming.test.ts` (3 tests) locks the URL encoding regression for each scope at the api-client level; `api-invalidation.test.ts` (+2) on `toggleWishlist` releases:recent + `updateMe({hypeThreshold})` upcoming-cache invalidation; `primitives.test.tsx` (+3) on card body click + non-interactive states + star stop-propagation. **136 API + 176 web tests pass.**
- **Wishlist games are first-class library citizens (2026-05-07).** Andrea reported three connected gaps: (1) starred releases were invisible to the top-bar search, (2) didn't appear in the Library Wishlist shelf, and (3) 404'd when clicked because the cards navigated with `igdbId` but `/game/:id` expects a `UserGame.id` (cuid). Architectural fix: keep the two-table model (`WishlistRelease` for upcoming-tracking metadata, `UserGame` for library citizenship) but sync them at the toggle boundary. The `POST /api/upcoming/:igdbId/wishlist` endpoint now wraps `Game.upsert` + `UserGame.upsert(status=Wishlist)` + `WishlistRelease.create` in a `$transaction`. Empty `update: {}` on the `UserGame` upsert is intentional — if a UserGame already exists with a non-Wishlist status (the user owns the game and starred it for DLC tracking, etc.), we don't override the user's library decision. Un-star branch deletes the `UserGame` ONLY if its status is still `'Wishlist'` (so manually-moved entries survive). RECENT page library-membership filter refined: drops a starred row only when its `UserGame.status !== 'Wishlist'` — without this, the now-always-present auto-created Wishlist UserGames would have caused the filter to drop every starred drop. New `IgdbUpcomingRelease.userGameId: string | null` (REQUIRED field) is populated server-side via a new `userGameMap(userId, igdbIds)` helper joined per-response. Frontend cards (`ReleaseCard`, `AgendaRail`, `MobileReleaseRow`, `StarredPanelCard`) navigate via `userGameId` (string, never null at the callback site); releases without one render non-interactive (no `role="button"`, no cursor pointer) instead of 404'ing. `api.toggleWishlist` invalidation contract extended with `games:` / `shelves:` / `gameCounts` so the Library Wishlist shelf, search overlay, and per-shelf counts reflect immediately. `SearchOverlay` placeholder updated: "search your library + wishlist…". One-shot backfill script `scripts/backfill-wishlist-usergames.ts` walked every existing `WishlistRelease` and created the missing `Game` + `UserGame(Wishlist)` rows. **Production backfill: 15 scanned, 11 new Game rows, 11 new UserGame(Wishlist) rows, 4 already-ok (pre-seeded), 0 errors.** Andrea's wishlist now has 7 UserGames(Wishlist) — 2 fresh stars + 5 from earlier seed data. 138 API + 177 web tests pass; lint + typecheck + rename-rule clean.
- **Releases page rework R6 — polish + closeouts (2026-05-07).** New `apps/web/src/components/screens/releases/WishlistEmptyRecommendation.tsx` shipped on both `ReleasesDesktop` and `ReleasesMobile` — closes the handoff §11 "// hot this month · on your platforms" gap with top-3 hype-sorted picks (filtered to future-dated, drops past per D4) plus a `+ wishlist` quick-add button per row. The bare wishlist-empty state is no longer a dead-end. Pulls from `useUpcoming('my-platforms')` via SWR — when the parent already has it cached (typical case), there's no extra round-trip; when not, one cheap fetch. 4 new Vitest tests (top-3 hype sort + quick-add wiring + past-drop filter + null-feed gate). E2E updated: `a11y.spec.ts` swaps `Upcoming /upcoming` for `Releases /releases` + `Releases recent /releases/recent` (8 routes × 2 viewports). `screens.spec.ts` rewritten — old test pinned to live IGDB titles ("Pragmata", "Death Stranding 2") replaced with structural assertions that survive feed drift (per-breakpoint chrome detection + "either content or empty-state CTA" checks). New legacy-redirect test for `/upcoming` → `/releases`. Stale `upcoming-{desktop,mobile}.png` snapshots removed from `apps/web/tests/snapshots/`; `releases-{desktop,mobile}.png` baselines need a one-time `npm run test:e2e:update -w apps/web` run on the user's machine (eyeball the .png files in Preview before committing — the byte-size signature is a useful sanity check, ~25 KB = login-redirect capture). Drift-guard sweep clean: no `_retired_` artboards in production code, no `[i got it]` / `[mark all owned]` rendered anywhere (matches are all comments + drift-guard test assertions), no `UpcomingDesktop` / `UpcomingMobile` references remain. Doc closeouts: `RELEASES_PLAN.md` row marked Done, `docs/PLAN.md` Phase Status row updated, `CLAUDE.md` Current Phase flipped to "no active workstream", D1 mirrored as decision #23 in `AGENT.md`. **136 API + 168 web tests pass; lint + typecheck + rename-rule + production build all green.**
- **Releases page rework R5 — mobile shell (2026-05-07).** Full mobile shell rewrite. New primitives `MobileViewHeader` (chevron stepping + tappable label per handoff §3 + §8; `disabled` attribute + opacity 0.35 at extremes per §12 punch-list 10), `MobileViewSheet` (single mobile surface for mode/scope/zoom/bucket — applies on every dismissal path per §7 — Done, X, scrim tap, Escape; scope conditional on mode=all per punch-list 5; zoom change re-maps bucket to its containing bucket per §7), `MobileBanner` (compact 1-line variants — green-prominent drops the subline at mobile width per punch-list 3; muted ellipsizes title list while preserving `+N · last 14d` per punch-list 4), `MobileReleaseRow` (40/36/1fr/auto grid per punch-list 2 — date column / 36×48 cover / title-meta / T-N + star). New `ReleasesMobile.tsx` page composes the above with full URL-state parity to desktop — defaults are omitted from the URL so MobileTabBar SOON nav (`/releases` with no params) hydrates to defaults per §3. Within-session navigation preserves state via URL params. Bucket↔zoom mapping via local `mapBucketBetweenZooms` helper (months → containing quarter, quarters → first visible month of that quarter, TBA passthrough). Hero countdown reuses the desktop's "next starred globally" rule (D5). New `ReleasesRecentMobile.tsx` — uses existing `MobileHeader` (back arrow + title only per §10 mobile), green informational prompt strip, asymmetric two-section layout (`// just out · starred` panel-style cards vs. `// also out · not starred` denser list rows per §10 — intentional weight difference). `UpcomingMobile.tsx` deleted; `App.tsx` route mapping + `screens/index.ts` exports updated; `releases/utils.ts` `toPlatCode` comment updated to reflect that the Releases-page copies are gone (3 left: 2 in Dashboard + the canonical one). 20 new Vitest tests covering chevron disabled state, sheet apply/scope-preserve/bucket-select, banner conditional + drift-guard, row state + drift-guard. 136 API + 164 web tests pass.
- **Releases page rework R4 — RECENT page proper layout (2026-05-07).** `ReleasesRecentDesktop` extracted from the R3 stub into its own file with the proper two-section layout per handoff §10: `// just out · starred · N` (2-col grid from `data.starred`) and `// also released · not on your wishlist · N` (2-col grid from `data.hyped`). Green prompt strip at top mirrors the green banner styling but is informational only (`role="note"`, no buttons) — copy "they'll move to your library automatically once your platforms sync." Section markers omitted entirely when their list is empty. Empty state ("// nothing in the last 14 days" + back-to-releases CTA) and dedicated error/retry path. Each card wrapped in a `<button>` for keyboard nav to `/game/:id` (no `[i got it]`, no `[mark all owned]` per §10 — drift-guard test asserts both are absent). Stub deleted from `ReleasesDesktop.tsx`; `screens/index.ts` and `App.tsx` imports updated. 5 new Vitest smoke tests. 136 API + 144 web tests pass.
- **Releases page rework R3 — desktop page composition (2026-05-07).** New `ReleasesDesktop` component + stub `ReleasesRecentDesktop` (R4 fills it in fully). New `releases/bucketing.ts` with all the time-window math: `visibleMonths` (current + next 5), `visibleQuarters` (current + next 2 + TBA catch-all), `bucketKeyFor` (per-release bucket assignment, drops past-dated everywhere except `/recent` per D4), `buildBuckets`, `nextNonEmptyBucket` (powers `[skip ahead →]` CTA per §11), `quarterCaption` (per §12 punch-list 8: "jul → sep" / "sorted by hype"), `nextStarredGlobally` (D5 hero rule). URL state via `?mode=&scope=&zoom=&bucket=` — defaults are omitted from the URL so shareable links stay clean. Right-rail conditional renders only on `mode=all & zoom=months & bucket≠TBA` (handoff §6 truth table). Empty-state CTAs for both "wishlist global empty" and "bucket empty" cases. `/upcoming` redirects to `/releases` via `<Navigate>`; `Sidebar` label flipped from "Upcoming" to "Releases"; `MobileTabBar` SOON path repointed to `/releases` (label stays — abstract per D1). `UpcomingDesktop.tsx` deleted; mobile still shows `UpcomingMobile` until R5 ships the new mobile shell. `shell-persistence.test.tsx` updated to click "Releases" instead of "Upcoming". 31 new bucketing tests. 136 API + 139 web tests pass; lint + rename-rule guard clean.
- **Releases page rework R2 — desktop primitives (2026-05-07).** All 5 components shipped under `apps/web/src/components/screens/releases/`. `ReleaseCard` is the 3-variant card (wishlist / all / recent) — HypeBars only on `'all'` per handoff §5; explicitly omits `[i got it]` per §5/§10 (mock has it, handoff removes it). `HeroCountdown` ships with only the `[on wishlist]` button per D6 (`[trailer]` and `[remind me]` are deferred to v2 — no backing data). `RecentBanner` has the 2 variants per §4 with handoff-spec copy ("they'll move to your library automatically once your platforms sync"); explicitly omits `[mark all owned]`. `TimeNav` renders bars + counts with TBA as a hatched diagonal pattern (handoff §12 punch-list 7) and a `MONTHS · QUARTERS` zoom toggle. `AgendaRail` is the chronological right-rail flat list. Shared `utils.ts` adds `toPlatCode` (DRY-ed but kept local until existing screens get reworked), `hypeToBars` (raw IGDB hypes 0–500+ → 1–5 perceptual buckets), `releaseDateColumn`, `categoryLabel`. 39 new Vitest smoke tests including explicit assertions that the removed mock buttons (`[i got it]`, `[mark all owned]`, `[trailer]`, `[remind me]`) never appear. 108 web + 136 API tests pass; lint + rename-rule guard green.
- **Releases page rework R1 — server endpoint (2026-05-07).** New `GET /api/releases/recent` powering the RECENT page and the banner-qualification logic. Returns `{ starred, hyped }` both shaped as `IgdbUpcomingRelease[]` per D7 (unified response). Server-side maps `WishlistRelease` → `IgdbUpcomingRelease` shape, dropping the unused DB pk + userId. `starred` excludes any release whose `igdbId` already exists as a `UserGame` for this user (library-membership filter); `hyped` is `getRecentlyReleased()` IGDB output deduped against `starred` with `hypes >= 80` (the muted-banner constant from handoff §4 — NOT the user's hypeThreshold). Graceful degradation when IGDB throws — `starred` still served. 7 new integration tests covering D7 shape, library filter, dedupe, both date windows, IGDB-throw graceful path, empty case. New `RecentReleasesResponse` type in `@hoard/types`. New `api.releasesRecent()` client method (frontend not consumed yet). 136 API + 69 web tests pass; rename-rule CI guard green.
- **Post-8 PR C — Sync-all + wipe-library with feedback (2026-05-06).** Sync-all on Settings → Platforms (desktop): fires `POST /api/platforms/:code/sync` for every connected, syncable platform in parallel, then polls `platformStatus` every 2s until none are `syncing` (capped 60s/platform). Button enters `syncing…` state and disables; aria-live status panel transitions `// syncing N platforms…` → `// done — all platforms synced` (or `// done — N failed (codes…)`). Mobile already had per-platform sync via PlatformDetailMobile. Wipe-library: new `POST /api/auth/me/wipe-library` route deletes the user's UserGames + Platforms in a transaction; preserves Game / HltbData / WishlistRelease / account / preferences (per D10). The existing DeleteModal generalised to `ConfirmModal` accepting a `variant` + `confirmKeyword` prop (`HOARD` / `WIPE`); both desktop + mobile show a green aria-live success toast `// N games removed · M platforms disconnected` after wipe completes. 129 API + 69 web tests pass.
- **Post-8 PR B — Real wishlist scope + Path-B persistence fix (2026-05-06).** Three real scopes on Upcoming: `my-platforms` (IGDB feed, hype-filtered, your platforms), `all` (IGDB feed, hype-filtered, all platforms), `wishlist` (only games you've starred — DB read, no hype filter). The chip labelled "wishlist" finally means what it says. Wishlist toggle endpoint (`POST /api/upcoming/:igdbId/wishlist`) now uses new `getReleaseDetails(igdbId)` helper to fetch the rich upcoming-release shape and persists `releaseDate` / `platforms` / `synopsis` / `hype` / `category` / `releaseDateCategory` — was previously dropping all of these. Migration `20260506160000_wishlist_release_category` adds the missing `category` column to `WishlistRelease`. New `scripts/backfill-wishlist-fields.ts` re-fetches existing impoverished rows from IGDB; 7/7 updated cleanly on production. `UpcomingScope` type widened to `'my-platforms' \| 'all' \| 'wishlist'`; wishlist count is queried separately so it stays honest regardless of active scope; empty-state copy updated. 127 API + 69 web tests pass.
- **Post-8 PR A — Interaction debt cleanup (2026-05-06).** All 9 items shipped in one batch. **A4** `MobileHeader` now defaults `onBack` to `navigate(-1)` (was undefined → silent dead UI on every Settings + PlatformDetail sub-page); new `.m-icon-btn` utility delivers 44×44pt Apple HIG hit area + `:active` press flash + `navigator.vibrate(8)` haptic across mobile icon buttons; DashboardMobile's unwired `<Icon name="menu">` override removed (default search button returns). **A5** `html, body` get `overflow: hidden; overscroll-behavior: none` and inner scroll surfaces (`.thin-scroll`, `.app-content`, `.app-mobile-content`, `PullableScroll`) get `overscroll-behavior: contain` — kills body-level rubber-band so the mobile shell stops wiggling on drag. **A3** Library shelves view simplified — sort + platform-filter chips + view-mode chips + the `prefs.libraryView` setting all gone (column kept in schema as no-op default per D5); skeletons match. **A1** `LibraryDesktop` "find" field is now a real `<input>`, hitting the existing `?q=` server filter (case-insensitive title match scoped to UserGames); typing swaps the shelves view for a flat result grid; `/` shortcut focuses the input on `/library*` (skips when already in another input). LibraryMobile gets the same input minus the keyboard shortcut. **A2** Cmd-K binding was already in `useSearchModal` — confirmed working; comment clarifies the dual-shortcut pattern. **A6/A7** four Settings stubs (Library/Notifications/Privacy/Data export) now show a real `ComingSoonPanel` with v2 description; Account profile-visibility radios + sessions list get a `// v2` chip + `opacity: 0.5; pointer-events: none`. **A8** `GameDetailMobile` receipt block extended with all three HLTB rows (main / extras / 100%). **A9** Dashboard "see full upcoming feed →" → real `<Link>`; now-playing buttons wired (`resume` → game detail, `+ note` → game detail with `?focus=notes` which auto-opens the editor on land; `log session` deleted as no v1 model); GameDetail HLTB chip → real `<a target="_blank">` to `howlongtobeat.com/game/{hltbId}` (search-URL fallback when no id); GameDetailDesktop gets a `← back` chip; Upcoming month-strip tabs (desktop + mobile) functional with an "all" pseudo-tab — clicking a month filters featured + agenda. 125 API + 69 web tests pass; lint 0 errors.
- **Post-8 PR D — HLTB layered fallback chain (2026-05-06).** Migration `20260506140000_game_hltb_id_gog_id_hltb_source` adds `Game.hltbId` + `Game.gogAppId` (captured from the codepotatoes.de payload — unblocks real HLTB deep-links and future GOG sync) and `HltbData.source` (`'hltb'` / `'igdb'`). New `getTimeToBeat(igdbId)` in `apps/api/src/services/igdb.ts` hits IGDB's dedicated `/game_time_to_beats` endpoint (mid-PR correction — `time_to_beat` is NOT a sub-field on `/games`, it's a separate endpoint keyed by `game_id`; the games-endpoint approach silently returned nothing). New `fetchHltbWithFallback` orchestrator runs Steam-ID lookup → IGDB time_to_beat in order. New `scripts/audit-hltb.ts` (read-only diagnostic) and `scripts/backfill-missing-hltb.ts` (one-time backfill). Backfill ran end-to-end: 264 new HLTB rows (46 HLTB-sourced + 218 IGDB-sourced). Coverage moved 318/929 → 586/929 (34.2% → 63.1%). 125 API tests pass. See `docs/INTERACTION_DEBT_PLAN.md` PR D section.
- **Post-8 — Interaction debt audit + hot fixes from Luigi's first test (2026-05-06).** Commit `6c624a0`: PSN sync was placing every imported game in Backlog because `runSync` hardcoded `status: 'Backlog'` on create. The "Steam → On Hold if played" rule Andrea remembered came from a one-time `scripts/backfill-status.ts` run, not from the runner. Fixed: status now derived from total merged playtime (`> 0 → OnHold`, else `Backlog`) — platform-agnostic. Steam connect button on `PlatformDetailDesktop` used a bare `/api/auth/steam` path which on production resolves to `gamehoardr.com/api/auth/steam` (Vercel 404, `fra1::...`); same button on `PlatformDetailMobile` had no onClick at all. Both fixed via `API_BASE` prefix matching the LoginScreen pattern. Production data corrected via `scripts/backfill-status.ts`: 384 Backlog → OnHold, 287 (no playtime) stayed. Commit `de2090e`: Library single-shelf filter+sort port — desktop now matches the mobile-filtered view (state + handlers were already wired; only JSX missing). Then drafted `docs/INTERACTION_DEBT_PLAN.md` capturing two systematic audits (data-flow + interaction) and a 14-decision PR plan (PRs A–D).
- **Phase 8 — Mobile Parity, iOS-HIG & Accessibility (5 PRs, 2026-05-05).** Composite mobile UX score moved from ~3/10 to ~8/10. Headlines:
  - **PR 1** — typography scale + line-height tokens, `:focus-visible` styles, `prefers-reduced-motion`, chip/btn/field min sizes raised, `--paper-faint` text contrast fixed (`.t-faint` remapped to `--paper-dim`), inline `fontSize` literal sweep across 24 components.
  - **PR 2** — fake "9:41" status bar deleted, mobile tab bar fixed (4-col grid, safe-area insets, 2 px amber active border, press feedback, haptic tick), `viewport-fit=cover` + `100dvh`, `MobileHeader` search wired to `SearchOverlay`, tab-bar icons replaced with meaningful glyphs (`home`/`rows`/`clock`/`user`).
  - **PR 3** — full WCAG 2.1 AA pass: `eslint-plugin-jsx-a11y` (90 lint errors → 0), ~50 `<div onClick>` → `<button>`, semantic `<h1>`/`<h2>`/landmarks, `<label htmlFor>` everywhere, `role="dialog"` + focus traps + Escape on every modal, `useDocumentTitle` per route, skip-link, `Cover.tsx` alt-text strategy, `axe-core` E2E (12/12 passing), Lighthouse a11y threshold 90 → 95, rate limiter skips in dev, Playwright `webServer` starts both API + web.
  - **PR 4** — mobile parity port (15 items): `GameDetailMobile` is now a real editor (status bottom-sheet, notes editor, action buttons), `DashboardMobile` gets backlog picker + tappable now-playing + genre breakdown, `LibraryMobile` gets filter chips on filtered view + cover-density preference, `UpcomingMobile` gets scope toggle + DLC/remake labels, `PlatformDetailMobile` gets sync-frequency radios + scope checklist, `SettingsMobile` platform rows expanded. Skipped: view-mode toggle, activity log tab.
  - **PR 5** — empty / first-run states with CTAs across Dashboard / Library / Upcoming, retry buttons on every data-fetching screen, URL state for Library `?sort=`/`?view=` and Upcoming `?scope=`, pull-to-refresh on mobile data screens via `usePullToRefresh` hook + `PullableScroll` primitive, `navigate(-1)` audit complete.
- **scripts/list-users.ts and scripts/delete-users.ts** — debugging utilities for the User table; `delete-users.ts` carries a history block of what was deleted and when. Tested 2026-05-05 by purging 5 stale sign-ups (test/diag/unused accounts).
- Lint cleanup: 86 pre-existing errors → 0 errors (2 non-blocking HMR warnings remain). ESLint config gained a `scripts/**` override; test middleware mocks now use proper Express types; Express `Request`/`Response` are type-only imports across all routes; `auth.test.ts` mocks `dotenv/config` so OAuth env-dependence is deterministic regardless of local `.env`.
- Supabase RLS: enabled on all 7 public tables. Captured in migration `20260504100000_enable_rls_on_public_tables`. Closes 8 Supabase Security Advisor errors (rls_disabled_in_public + sensitive_columns_exposed on User.password).
- Phase 7 — Production deploy + Google OAuth + custom domain: API on Railway (`api.gamehoardr.com`), web on Vercel (`gamehoardr.com`), DNS at Porkbun, SSL auto-provisioned. Google OAuth client wired with new origins/redirect URIs. iOS Safari + Chrome incognito both work because both subdomains share `.gamehoardr.com` parent (cookies are first-party).
- Cross-origin auth fixes: SameSite=None+Secure cookies in production; `<RequireAuth>` wrapper bouncing unauthenticated users to /login; `VITE_API_URL` base URL prefix in api client; `app.set('trust proxy', 1)` for Railway's proxy; `?pgbouncer=true&connection_limit=5` on `DATABASE_URL` for Supabase transaction pooler (later raised from `1` to `5` — see Operational gotchas).
- Test suite restored to green: 4 stale mocks fixed (hltb fetch mock, platforms `$queryRaw`, auth `deleteMany`, TopBar Router wrapper); added Phase 3 integration tests for games/dashboard/stats/upcoming/igdb routes (38 new tests).
- Phase 6 testing deliverables completed: Playwright offline simulation (`apps/web/tests/e2e-offline/offline.spec.ts`) + Lighthouse CI workflow + thresholds config (`apps/web/lighthouserc.json`).
- `GET /api/games/counts` endpoint — sidebar shelf counts always accurate on every route.
- Shelf order: Playing → On Hold → Completed → Backlog → Dropped → Wishlist.
- Library shelf cards fill viewport width exactly via `ResizeObserver`; "view all" always occupies last slot.
- Game Detail: status picker + notes editing fully wired (`PATCH /api/games/:id`, optimistic update).
- HLTB rewritten: `howlongtobeat` npm package was broken (HLTB changed API); replaced with `hltbapi.codepotatoes.de/steam/{steamAppId}`.
- `steamAppId Int? @unique` added to `Game` model; stored during Steam sync; wired through HLTB background triggers.
- Full Steam backfill: 488 Steam app IDs matched, HLTB data fetched for ~300 games.
- PSN sync fully implemented (`psn-api` v2.18.0); `syncPsnLibrary` fetches paginated library with ISO 8601 duration parsing.
- PSN title cleaning: `cleanPsnTitle()` strips ®/™ and PS4/PS5 platform suffixes before IGDB search; 94% match rate (132/140).
- PSN HLTB backfill (`scripts/backfill-psn-hltb.ts`): Steam Store search for App IDs; 48 HLTB records added for cross-platform PSN games.
- Dashboard genres chart: bars now proportional to max count (`count / maxCount * 100%`) instead of hardcoded multiplier.
- Shareable receipt: OWNED ON + PROGRESS sections converted from `<pre>` to flexbox `.row` divs with CSS dotted spacers and self-sizing section header lines.

**Known gaps (low priority, none blocking):**
- **Relic dither SVG payload size (2026-06-01, surfaced during GD-PR4a backfill).** `Game.relicDitherSvg` is averaging **~775KB per row** (range 148KB → 1.08MB depending on image contrast/detail), not the 15-30KB I originally estimated in the plan. 87 Completed-game rows × ~775KB = ~70MB total in DB (PostgreSQL TOAST handles this transparently, no schema concerns). The user-visible cost is in the wire payload: every GameDetail response for a Completed game carries the SVG inline, so each `/api/games/by-igdb/:igdbId` response for an S4 game is now ~750KB-1MB raw / ~150-250KB compressed via HTTP gzip. Acceptable for now (Andrea's closed-beta scale), but worth flagging. **Optimization path if it surfaces as a perceptible UX hit during/after GD-PR4b:** split the SVG into a separate `GET /api/games/relic-dither/:gameId` endpoint with aggressive HTTP caching (immutable + long max-age, invalidate via query-string hash). GameDetail response carries just the URL; browser fetches + caches the SVG separately. ~half-day fix. Don't optimize preemptively — measure after GD-PR4b ships and inline SVGs first-paint cost is observable. **Why so large?** 120×68 = 8160 cells, each ~110 bytes for `<g class="rd-cell" style="animation-delay:Xms"><shape .../></g>` (animation-delay attribute ~25 bytes/cell alone = ~200KB pure overhead per SVG). Reducing requires either coarser grid (less detail), dropping per-cell animation in favor of CSS-mask sweep (loses radial-wave engrave effect — D7 locked), or lifting shared shape attributes to a per-bucket parent `<g>` (~30% reduction, no visual change).

- ~~**Hero artwork source-quality variance — stage 1 (B-Art-1)** (2026-06-01).~~ **Closed by B-Art-1 v3 (2026-06-01).** Was: `Game.heroImageUrl` populated deterministically from `artworks[0] ?? screenshots[0]` gave wildly variable quality (~60% great / ~40% screenshots or logo-art). **Final shipped picker (v3, after two iteration rounds with Andrea's eyeball):** two-stage, no algorithmic scoring inside the pool. Stage 1 picks the **FIRST** artwork that's not a cover-duplicate AND not portrait (`width < height`). Stage 2 falls through to the first non-cover-dup screenshot. IGDB array order wins inside stage 1 — community-curated editorial order beats scoring tiebreaks unreliably ("ship v3 as it is. We've burned enough cycles tuning a picker that fundamentally can't read taste."). **The iteration path** is instructive for future image-picker work: v1 single-pool scorer (aspect + resolution + cover-dup) → 103 swaps but pulled to screenshots when an artwork was non-16:9 → Andrea's eyeball: *"a lot of them are better in the old, and a lot are better in the new"*. v2 two-stage (artworks-first with portrait skip, scoring inside) → 51 swaps but still scored-reordered within artworks → Andrea named 3 regressions (LEGO Horizon, Phantom Blade 0, Deus Ex HR). v3 dropped in-pool scoring → 26 swaps, just cover-dup + portrait skips. **Lesson:** algorithmic scoring of editorial assets returns diminishing trust quickly; prefer filter+array-order over filter+score. `scoreHeroImage` stays exported for documentation but isn't used by `pickBestHeroImage` anymore. IGDB queries extended with `artworks.width / artworks.height / screenshots.width / screenshots.height / cover.image_id`. Unit tests pin every cover-dup + portrait edge case + array-order-wins lock. One-shot rescore script `scripts/rescore-game-hero-image.ts` re-evaluates existing Game rows in Library-overview scope; defensive — keeps existing URL when scorer returns null. Companion eyeball script `scripts/compare-hero-rescore.ts` emits side-by-side HTML for visual spot-checks before committing the real rescore. **Stage 2 (B-Art-2 — SteamGridDB integration)** stays a back-pocket option if cohort signal still demands after this lands. **`Game.heroImageUrl` powers TWO surfaces:** Library overview landscape cards (rendered directly as JPG) AND the OQ-GD-13 relic centerpiece (dithered). The two surfaces have slightly different optimal-image needs (Library wants pretty/colorful; relic wants high-contrast silhouettes); deferred — if some specific games look bad on the relic after GD-PR4b ships, add `Game.relicImageUrl` then.
- **Sync-time steamAppId mis-assignment across IGDB Game rows (2026-06-01, surfaced + partially fixed during GD-PR3 / R2 eyeball; Andrea reported Graveyard Keeper HLTB data wrong: 39h main only vs HLTB's actual 46h/56h/72h).** Probe diagnosis: two `Game` rows for the original + sequel — (a) `igdbId=27384, title='Graveyard Keeper'` (the original released 2018), Andrea's UserGame correctly points here; (b) `igdbId=397817, title='Graveyard Keeper II'` (the sequel, unreleased). But (b) had `steamAppId=599140` which is actually the ORIGINAL's Steam appid — sync mis-assigned it to the sequel row, and (a) was left with `steamAppId=null`. HLTB lookup via Steam appid populated (b)'s HltbData with the correct full numbers (46/56/72h) but they were attached to the wrong Game; (a) fell through to IGDB `time_to_beat` fallback (single-value impoverished, 39h main only). **Initial framing was wrong** — I described it as a "remap-class" issue and Andrea correctly pointed out her UserGame is already on the right Game; `[wrong game?]` remap wouldn't fix it. **Fix actually applied 2026-06-01:** one-off DB correction moved `steamAppId=599140` from row (b) to row (a), cleared on (b). Re-fetch of HLTB for (a) via the correct appid was attempted but `hltbapi.codepotatoes.de` is currently returning *"It looks like hltb changed their game again. Please wait for an update"* — third-party mirror in maintenance. Decision: wait for codepotatoes.de to come back rather than write manual values (Andrea's preference — no hand-curated data). **The general pattern (not just Graveyard Keeper):** sync-time matching can attach a platform-side ID to the wrong IGDB Game row when title search picks a similarly-named neighbour (sequel, regional variant, etc.). The corruption is at the Game-row level — `[wrong game?]` doesn't fix it because it assumes the UserGame is wrong, not the Game. **Detection path:** the value of steamAppId / psnConceptId / etc. on each Game row should be consistent with the row's igdbId — IGDB's external_games endpoint can verify. A future workstream could be a one-shot audit: for every Game with a platform-side ID, cross-check via IGDB external_games and flag/fix mismatches. **Tracking:** also flag `hltbapi.codepotatoes.de` outage state as an infrastructure dependency — when it's down, every game without an existing HltbData row falls through to the IGDB single-value fallback. Worth a status check before any HLTB backfill workstream.
- **IGDB artwork quality variance on Library overview landscape cards (2026-05-31, Andrea's eyeball + acceptance).** New `LibraryOverviewCard` uses IGDB `artworks[0]` (preferred) → `screenshots[0]` (fallback) → `coverUrl` (last resort). **Most games look great with artworks** (curated key art, proper landscape banners). **~10-15% look bad** — either community-uploaded logo-on-white JPGs (the `--ink-2` background can't help because JPG is opaque white, not transparent) or non-16:9 portrait posters that crop awkwardly in the 16:9 box. Specific games Andrea flagged: CivCity: Rome, Neverness to Everness, Sid Meier's Civilization III: Complete, Warlock, Warhammer 40,000: Dawn of War IV, Tropico 7. **Tried + rejected:** screenshots-first priority (Andrea: *"No god no, they look awful"* — screenshots are random in-game moments, rarely flattering hero images; uniform but worse on average than artworks). **Accepted v1 trade-off** (Andrea 2026-05-31): keep artworks-first, accept the ugly subset. **Path forward when revisiting:** integrate SteamGridDB (`steamgriddb.com/api`) — community-curated landscape hero art, much higher quality than IGDB artworks. Adds an API integration + a new image source per Game (e.g. `Game.steamgriddbHeroUrl`); fallback chain becomes `steamgriddb → IGDB artworks → IGDB screenshots → portrait cover`. Half-day-ish workstream when cohort signal demands.
- **`[change lens ▾]` UX is messy (2026-05-31, Andrea's eyeball post B-IGDB-3b2 sidebar pivot).** Andrea: *"its a bit of a mess honestly. but lets take some time after we finish the other things to revisit this."* What works today: status → tag pivot (when `?<dim>=` filter is set as secondary), tag → tag pivot (when target dim's secondary filter is set, old primary becomes secondary on new lens). What's NOT working well from a UX standpoint: (i) when on a tag lens with no secondary filter set, ALL options in the dropdown are disabled — opens to a useless greyed-out picker; (ii) tag → status pivot is deferred (no `?status=` chip on tag lenses yet); (iii) the discoverability of "this dropdown is contextual, you need a filter set first" is poor — no inline hint or empty-state message; (iv) the chip label `[change lens ▾]` doesn't telegraph the pivot mechanic — could read as a generic dropdown unrelated to current URL. **When revisiting:** consider (a) hiding the chip entirely when no pivots are available; (b) renaming label to something more action-oriented (e.g. `pivot to ▾` or `view as ▾`); (c) adding a hint chip when the dropdown opens with no enabled options (`pick a filter first to pivot`); (d) shipping status-as-secondary on tag lenses to unlock tag→status pivot; (e) reconsidering whether the change-lens affordance is even the right surface — maybe the secondary filter chips themselves should grow a "make this primary" affordance instead (right-click / long-press / shift-click on a FilterPopover value). Defer until other B-IGDB-3b2 follow-ups (game card restructure + status-as-secondary) ship.
- **Stash-audit-derived scope expansion bundle (2026-05-22, surfaced during F1 interaction-flow design).** Captured in full as `docs/USER_RESEARCH.md` O14. **CM12 (per-game-status + per-platform-wishlist) already landed 2026-05-22** — partially absorbed item (1) and resolved the most pressing real-world use case (GTA-style cross-platform wishlist on owned games). The remaining items to absorb into a future **S15 + B14 amendment pass** after F1 ships: (1) ~~owned-as-toggle vs. status-only model split~~ — **partially RESOLVED via CM12** for the per-platform-wishlist case; remaining edges (e.g. "completed on a borrowed copy", "bought but haven't started") still warrant the full owned-as-boolean exploration if cohort signal demands it, (2) status-contextual sub-statuses on GameDetail edit, (3) rating-as-data column + score input UI, (4) personal user-defined Collections entity + UI, (5) Series/Developer/Publisher reference-data entities + lateral-navigation "all games by X" UX, (6) Screenshots/UserGameMedia entity, (7) **Completions count / "times beaten" — UI placement inside F1's `[+ more details]` panel already locked; schema decision pending (column on UserGame vs. Session-log entries — bundled here)**, (8) Play date range / Session lifecycle. None blocks F1. Bundle as one strategy-revisit session post-F1.
- ~~**Admin page IA structurally fatigued.**~~ **Fixed 2026-05-29 (admin-IA redesign).** `/admin` split from a 1300-line monolith into 5 sub-routes (`/pending`, `/users`, `/codes`, `/feedback`, `/events`) under a sidebar layout with count badges + deep-link support. Mobile fallback preserved per I-D3 (desktop-only). See Recent Fixes entry + `docs/ADMIN_POLISH_PLAN.md` §A2.
- ~~**No delete capability on admin feedback rows.**~~ **Fixed 2026-05-29 (admin-IA redesign).** Shipped in the same workstream alongside the structural rework. `DELETE /api/admin/feedback/:id` admin-only endpoint + `ConfirmModal` `delete-feedback` variant (DELETE keyword) + `[delete]` button on each FeedbackRow with `stopPropagation` to keep row-expand independent.
- **No end-to-end auth-chain smoke test.** Every admin route test (`admin.test.ts`, `admin.feedback.test.ts`, `admin.events.test.ts`) mocks `requireUser` to unconditionally set `userId`, then exercises `requireAdmin` 404 for non-admins. The 401 path (no JWT at all, or invalid JWT, on a real admin route) is covered only by `middleware/user.test.ts` against a stub `/protected` route, never through the actual `/api/admin/*` mount. The two failure modes — `requireUser` 401 (unauthenticated) vs `requireAdmin` 404 (authenticated non-admin) — have separate code paths; verifying they don't drift via mocking-induced confusion would need a single real-HTTP-level smoke test. Predates F-series and TL-series; not currently a problem but worth doing in a focused test-infra workstream alongside the `.env.test` setupFiles work below.
- **Pre-existing OAuth connect-mode test failures in `apps/api/src/routes/auth.test.ts`.** Two tests at lines 391 (Steam connect-mode) and 417 (Google connect-mode) fail by redirecting to `/login?error=steam_failed`/`google_failed` instead of `/settings/*`. Surfaced when F1.1 (2026-05-13) added the workstream's first full-suite run since the I-series; independently reproducible by stashing later changes. **Predates the F-series and TL-series entirely** — neither workstream introduced or affected these. Filed here so future workstream close-outs don't keep re-diagnosing them. Root cause unknown; likely a mock drift between the OAuth callback handlers and the test's session-cookie / Prisma mock setup as the auth.ts route evolved over time. Small isolated fix workstream when convenient.
- **dotenv-via-transitive-import trap surfaced in TL1.2.** `apps/api/.env` carries the prod `JWT_SECRET`; any middleware/route test that transitively imports `@hoard/db` will load that prod value into `process.env.JWT_SECRET`, breaking tests that sign JWTs against `'dev-secret'` (the default fallback in `apps/api/src/middleware/user.ts`). Local fix in TL1.2: `jest.mock('dotenv/config', () => ({}))` at the top of the offending test file. **Long-term fix worth a small workstream:** introduce `apps/api/.env.test` with dev-shaped values (`JWT_SECRET=dev-secret`, dummy DATABASE_URL, etc.) loaded automatically via Jest `setupFiles`, eliminating the per-file mock requirement and giving us a proper test/dev/prod env separation. Same `.env.test.example` template that E1 wired for Playwright would generalize cleanly.
- **Express 4 async-handler rejections don't reach `app.use((err, ...))` without `express-async-errors`.** Surfaced in TL1.2's first attempt at testing `error.surfaced` via supertest — async route handlers that throw cause the response to hang because the rejection has nowhere to go. Workaround used in TL1.2: extracted `globalErrorHandler` to a named export and invoked it directly in tests. **Caveat for future work:** direct invocation covers the middleware's behaviour *given* an error object — it does NOT cover the plumbing that delivers errors to it. If a future workstream changes how errors reach the middleware (e.g. adds `express-async-errors`, or migrates to Express 5, or wraps handlers in a tryCatch helper), don't lean on the direct-invocation pattern as proof of end-to-end error flow.
- **E2E `useAutoSync` ambient noise (post-E1 follow-up, surfaced during Phase C step 6 diagnosis).** Seed-e2e.ts sets `Platform.lastSyncAt: minsAgo(8/12/6/15)` at seed-run time. As days pass, those values drift past the `HOURLY` `syncFrequency` threshold — every dashboard mount in E2E then triggers `/api/platforms/:code/sync` via `useAutoSync`. The sync hits Steam/PSN with fake test credentials, fails, and flips `Platform.syncStatus` from `'ok'` to `'error'`. Doesn't currently affect any test assertion (the seed's UserGame data is intact regardless), but is real ambient noise during full-suite runs and could in theory cascade into a hard-to-diagnose flake later. Mitigation candidate: bump seed `lastSyncAt` to `minsAgo(N)` where `N` is much larger than the `HOURLY` window (or recompute at seed time relative to NOW so it stays fresh). Not E1's job to fix; track as a future small PR. Filing a GitHub issue is Andrea's next action item with the title `[infra] E2E useAutoSync ambient noise — seed lastSyncAt drifts past HOURLY threshold over time`.
- Steam OpenID on production blocked by missing `API_URL` env var on Railway (discovered 2026-05-06 when Luigi tried to connect his Steam account and Steam redirected him back to `localhost:3001`). [auth.ts:40](../apps/api/src/routes/auth.ts#L40) defaults to `localhost:3001` when `API_URL` is unset; Steam's `return_to` + `realm` are derived from it. Fix: set `API_URL=https://api.gamehoardr.com` in Railway → API service → Variables. Documented in `docs/ENV.md`. **Status check (2026-05-12):** Luigi now shows 2 platforms (Steam + PSN) per `/admin` roster, so the env var may have been set since the discovery — needs confirmation. If still unset, the next Steam-on-prod attempt will hit the same redirect.
- ~~**PSN guided flow renders a React error on mobile.**~~ **Fixed 2026-05-21 (R2).** Original symptom: `/settings/platforms/PS/connect` mounted outside `AppShell` (deliberately full-bleed) while `PsnGuidedFlowMobile` used `MobileHeader → useSearchModal()`, and `SearchModalProvider` was mounted *inside* `AppShell`. Mobile users got the React render error "useSearchModal must be used inside <SearchModalProvider>". Discovered 2026-05-11 (Gaetano Iannì); workaround at the time was sending the desktop browser link. **Fix:** lifted `SearchModalProvider` from `AppShell.tsx` up to `App.tsx`, alongside `UserProvider`/`PreferencesProvider`, so it wraps the entire `<Routes>` tree regardless of layout topology. The provider is pure context with no rendering output — zero cost when no consumer mounts. Existing tests already mounted `SearchModalProvider` themselves so no test changes were required; 73/73 web tests across layout + admin + library + releases-recent + feedback green post-lift.
- Xbox / GOG library sync: stubs returning `[]`. Manual add covers the gap (same approach as Nintendo / Epic).
- `package.json#prisma` config block is deprecated in Prisma 6.19 — Prisma 7 will require `prisma.config.ts`. Migrate when convenient (warning only, not blocking).
- HLTB residual gap: ~37% of games have no time-to-beat data in either HLTB or IGDB (older indies, niche regional editions, DLC). Accepted silently per Rule 8.
- **Steam wishlist import is one-way** (no removal). If you remove a game from your Steam wishlist or buy it, the Hoard `UserGame(status='Wishlist')` + `WishlistRelease` rows stay until you manually un-star on the Releases page or GameDetail. Removal would need a `source` column on `UserGame` to safely identify which Wishlist entries came from Steam (vs. PSN-manual-star, vs. an earlier `POST /api/upcoming/:igdbId/wishlist`). Schema change deferred — first-version is import-only.
- **PSN / Xbox / GOG wishlists** are not synced. None of these platforms expose wishlist data via a usable public API (`psn-api` doesn't include it, OpenXBL doesn't, GOG's community endpoints don't). Manual `[+ wishlist]` toggles on the Releases page work for all platforms regardless.
- **Existing sync mismatches** (pre-2026-05-08 syncs may have wrong IGDB matches). The new `pickBestMatch` prevents the issue going forward; the in-app `[wrong game?]` button on GameDetail (desktop + mobile, sync-quality batch part 2) handles existing bad rows — opens `RemapGameModal` with an IGDB search dialog, preserves notes/status/playtime when remapping. `tsx scripts/audit-mismatches.ts` is still useful for surfacing platform-collision-class mismatches (Ragnarok-MMO style) you might not have noticed. The Slay-the-Spire-2 class (sequel-on-right-platform) isn't audit-detectable — needs user eyeballing, then the remap chip handles the fix. Full automation would require storing platform-side IDs (PSN titleId / Steam appid) per `UserGame` so re-sync can rebind by platform identity instead of IGDB identity — schema-change effort, deferred.
- Phase 8 success criteria deferred to pre-launch verification (not blocking):
  - Manual VoiceOver / TalkBack walkthroughs (axe-core covers structural a11y; manual screen-reader passes are pre-launch)
  - WAVE browser-extension audit (covered by axe-core's WCAG 2.1 A + AA tags)
  - Real-device pull-to-refresh test on iPhone + Android Chrome
  - OfflineBanner z-index audit on real devices
- Deferred features (intentional v2 punts, not bugs):
  - Quick-sync trigger from Sidebar / TopBar (Settings → Platforms has a real `sync all` button after PR C, but a sidebar one-click sync would be more discoverable)
  - Page-transition animations on route changes (skipped to preserve the terminal aesthetic's instant-feedback character)
  - Activity-log tab on `PlatformDetailMobile` (low value, lots of vertical space)
  - PlatformDetail still-unwired controls (auto-refresh toggle, scope checkboxes, reveal-NPSSO Btn) — kept visible per D11; build the backing User/Platform model fields when ready. **Sync-frequency radios are now wired** (2026-05-07 "feel alive" batch — see decision #30 in `AGENT.md` for the client-side cadence semantics).
  - Sidebar one-click sync (mentioned above)
- Pre-existing flaky E2E assertion tests (Pragmata / Death Stranding 2 in Upcoming agenda; ELDEN RING in GameDetail) hard-code titles that vary with the live IGDB feed. Real fix is to assert on selectors / regex patterns rather than literal game names — worth a quick test cleanup pass when convenient.
- Stub Settings sections (Library / Notifications / Privacy / Data export) currently render `ComingSoonPanel` — full implementations are v2 scope.
- `toPlatCode` (IGDB platform name → 2-letter code) now exists in 3 places: inline copies in `DashboardDesktop` + `DashboardMobile`, plus the canonical one in `screens/releases/utils.ts`. R3 + R5 deleted the `UpcomingDesktop` / `UpcomingMobile` copies. The two Dashboard copies are identical to the releases utils version — fold them in when convenient (promote `toPlatCode` to `apps/web/src/lib/utils.ts` and import everywhere). Not blocking.
