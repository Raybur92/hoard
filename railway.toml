# touch 2026-05-26 round 4 — Xbox playtime probe. Library import
# working (188 games landed); next: empirically verify OpenXBL Stats
# endpoint to see if per-title playtime is actually exposed before
# scoping the playtime side-pass.

[build]
builder = "nixpacks"
buildCommand = "npm run build --workspace=apps/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
