# touch 2026-05-26 — force Railway redeploy. Accept-Language fix
# landed (sync timing went 1s → 3s) but library still importing 0
# games; re-adding the OpenXBL response diagnostic gated on titles=0
# so we can see the actual response shape.

[build]
builder = "nixpacks"
buildCommand = "npm run build --workspace=apps/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
